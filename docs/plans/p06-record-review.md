# P06 기록 검토 — 기록 탭 · 라운드 종료 · 대기실 모드 표시

- **브랜치**: `feat/p06-record-review` (base: `main@09a0ddd`)
- **작업 기간**: 2026-07-26 ~ 2026-07-30
- **기획서 정본**: [포토로얄 규칙서 v1.0](https://minutimes.notion.site/37d989b36d0c80a89548ebbcef1ac87a)
- **선행 작업**: H04 라운드 운영(`docs/plans/h04-round-ops.md`), P05 사진 제출·판정(`docs/plans/p05-photo-submission-judging.md`)

---

## 0. 한 줄 요약

라운드 운영 화면의 **마지막 빈 탭(기록)을 채우고**, 그 김에 드러난 **"라운드가 끝나지 않는" 구조적 구멍**을 막았다. 부수적으로 대기실에서 **지금 무슨 모드인지** 볼 수 있게 했다.

작업은 세 덩어리다.

| | 작업 | 상태 |
|---|---|---|
| **A** | 라운드 운영 — 기록 탭 구현 | 커밋·푸시 완료 |
| **B** | 라운드 종료 처리 (재시작 → 종료) | 커밋·푸시 완료 |
| **C** | 대기실 차수·게임 모드 표시 | 커밋·푸시 완료 |
| **D** | 종료 흐름 보강 — 재실행 가드·기록 진입점·카메라 잠금·공용화 | 커밋·푸시 완료 |

---

## 1. 배경 — 기획서의 "라운드"가 무엇인지 확정한 것이 이 브랜치의 출발점

작업 도중 "라운드가 모드마다 여러 번 도는 건지, 라운드마다 모드가 바뀌는 건지"가 불명확해 기획서를 다시 읽고 결론을 냈다. **이 해석이 B 작업의 근거 전부**이므로 여기 남긴다.

### 결론: 라운드 = 게임 모드 1개를 담는 슬롯

기획서 §1 소요 시간 타임테이블이 결정적이다.

```
1차 팀편성 10분 → 1차 게임 준비 5분 → 1차 게임 20분
2차 팀편성 10분 → 2차 게임 준비 5분 → 2차 게임 20분
3차 팀편성 10분 → 3차 게임 준비 5분 → 3차 게임 20분
```

**"팀편성 → 준비 → 게임" 세트가 통째로 반복**된다. 여기서 두 가지가 읽힌다.

1. **게임 슬롯이 고정 개수**이고 각 20분이다 — 한 모드를 여러 슬롯에 걸쳐 늘려 하는 구조가 아니다.
2. **라운드마다 팀편성을 처음부터 다시 한다** — 모드가 안 바뀐다면 편성을 3번 다시 할 이유가 없다. §3의 모드들은 **요구하는 편성 단위가 서로 다르기 때문**이다.

| 모드 | 요구 편성 |
|---|---|
| 일반전 (§3.1) | 2인 1조 팀, 각 팀 개별 경쟁 |
| 꼬리잡기 (§3.2) | 알파벳 상성 체인 (A→B→C…) |
| 그룹전 (§3.3) / 왕잡기 (§3.4) | 완장 색 4그룹 연합 |
| 스태프 추격전 (§3.5) | 전원 동맹 vs 스태프 |
| 폭탄설치전 (§3.6) | 공격그룹 / 수비그룹 |
| 꼼꼬미 (§3.7) | 전체의 10% 술래 지정 |

편성이 모드에 종속되므로 "편성 + 게임"이 한 쌍으로 반복된다. 또 타임테이블이 모드명을 지정하지 않고 **"1차/2차/3차 게임"이라고만 쓴 것** 자체가, 모드는 상수가 아니라 **운영 중 진행자가 고르는 변수**라는 뜻이다.

### 모드 내부 반복은 꼼꼬미(§3.7) 하나뿐

§3.7에만 *"종료 및 라운드 재개"* 절이 있고 다음 술래 선정 규칙까지 명문화돼 있다. 나머지 모드에는 없다 — **부재가 곧 스펙**이다. 즉 "한 모드를 여러 라운드에 걸쳐 이어 한다"는 경로는 꼼꼬미를 빼면 존재하지 않는다.

> ⚠️ 기획서가 "라운드"를 두 층위로 쓴다. **① 외부 차수**(타임테이블 1차/2차/3차 = 코드의 `assignmentRound`)와 **② 모드 내부 판**(꼼꼬미의 술래 교대, 왕잡기 §3.4의 "즉시 라운드 패배"). 코드에는 ①만 있다.

---

## 2. 확정 스펙 (사용자 Q&A 결과)

| # | 사항 | 확정안 |
|---|---|---|
| 1 | 라운드 종료 후 동작 | **모드 안에서 이어가지 않고 끝낸다.** 종료 다음에 오는 것은 타이머 재시작이 아니라 재편성(대기실 복귀) |
| 2 | 진행할 모드 범위 | **§3.1~3.5 다섯 모드 전부 진행 필수** (일반전·꼬리잡기·그룹전·왕잡기·스태프 추격전) |
| 3 | 라운드당 게임 시간 | **20분 유지** (`ROUND_DURATION_DEFAULT_MS`) |
| 4 | 총 소요 시간 초과 | **감수한다.** 5모드면 245분(4시간 5분)으로 기획서 3시간을 65분 초과하지만 시간을 줄이지 않는다 |
| 5 | 모드 순서 고정 | **🚫 폐기.** 라운드 번호에서 모드를 자동 도출하는 안은 명시적으로 폐기됐다 |

### 5번 보충 — 다시 제안하지 말 것

의도한 진행 순서는 일반전 → 꼬리잡기 → 그룹전 → 왕잡기 → 스태프 추격전이지만, **그 순서를 코드에 표로 고정하는 안은 사용자가 폐기했다.** 모드는 라운드마다 진행자가 배정 보드에서 고르는 값으로 유지하고, `GameModePicker`도 그대로 둔다.

> §3.1~3.5가 한 덩어리인 이유: 제외된 §3.6(폭탄설치전)·§3.7(꼼꼬미)·§3.8(빠른생존)은 **전부 QR 스캔이 필수**다. 3.1~3.5는 카메라 + GM 판정만으로 성립한다. 즉 이 범위는 QR 인프라 없는 MVP 경계와 정확히 일치한다.

---

## 3. 작업 A — 라운드 운영 기록 탭

`RoundOpsPage`의 세 번째 탭이 "준비 중" 카드로 비어 있던 것을 채웠다.

### 데이터

`rooms/{code}/submissions` 컬렉션을 **전 라운드·전 상태로** 구독한다(판정 큐는 `status == 'pending'` + 현재 라운드만 보는 별개 구독).

```ts
interface SubmissionRecord extends Submission {
  targetTeam: string | null  // 확정(approved) 시 잡힌 팀 완장, 대기·반려는 null
  judgedAtMs: number | null  // 판정 시각, 대기 중이면 null
}
```

- **게으른 구독**: 사진이 data URL이라 무겁다 → enter에서 항상 열지 않고 **기록 탭이 처음 활성화될 때** `subscribeToRecordLog()`가 시작한다(스토어가 중복 시작을 막는다).
- **정렬은 클라이언트**: 복합 인덱스를 피하려고 라운드 내림차순 → 라운드 안에서는 최신 제출 순으로 클라이언트에서 정렬한다.

### 화면

| 상황 | 동작 |
|---|---|
| 이번 라운드의 대기 건 | 행을 누르면 **판정 시트**가 열려 그 자리에서 판정 |
| 판정 완료 / 지난 라운드 대기 건 | **읽기 전용 상세 시트**로 사진 확대 확인 |

지난 라운드 판정은 `firestore.rules`가 막으므로 판정 시트를 열어줘도 실패한다 — 그래서 애초에 열지 않는다.

**추가 파일**: `components/RecordLogList.vue`, `components/RecordDetailSheet.vue`, `recordStatusStyles.ts`, `api/submissions.ts`(`subscribeToSubmissionLog`), `stores/useRoundOpsStore.ts`(`subscribeToRecordLog`)

---

## 4. 작업 B — 라운드 종료 처리 ⭐ 이 브랜치의 핵심

### 문제

타이머가 0에 닿으면 **'라운드 시작' 버튼이 다시 나왔다.** 그런데 `startRound()`는 `round` 필드만 쓰고 **`assignmentRound`를 올리지 않는다.** 결과적으로 "2라운드"가 아니라 **같은 편성·같은 완장·같은 라운드 번호로 20분이 덧붙는** 동작이었다.

여기서 두 가지가 깨진다.

1. **미판정 킬샷이 새 20분에 섞인다.** 판정 게이트가 `record.round === assignmentRound`인데 라운드 번호가 그대로라, 직전 20분의 미판정 건이 새 타이머의 판정 큐에 계속 남는다.
2. **기록이 뭉갱친다.** 제출물이 `round`로만 구분되니 40분치가 1라운드로 집계된다.

여기에 확정 스펙 2번(5모드 전부 진행)을 더하면, 한 모드가 슬롯을 2개 먹는 순간 5모드를 돌릴 수 없다.

### 해결

**종료 상태(`isRoundEnded`)의 주 액션을 '라운드 시작' → '라운드 종료'로 교체**했다. 쓰기는 기존 `endGame()`과 동일하다(방을 `waiting`으로 되돌리고 `round` 삭제). **새 데이터 모델도 rules 변경도 필요 없었다** — 배관이 이미 다 있었기 때문이다.

```
타이머 0 → [라운드 종료] → status=waiting, round 삭제 (assignmentRound는 보존)
                          → 대기실 복귀
                          → "N+1차 팀 배정" CTA 노출 → 거기서 다음 모드 선택
```

세부 결정 3가지:

| 결정 | 이유 |
|---|---|
| 종료 상태에서 '게임 종료' 버튼을 **감춘다** | '라운드 종료'와 **쓰기가 완전히 동일**하다. 같은 일을 하는 버튼이 둘 보이면 진행자가 차이를 찾느라 멈춘다 |
| 미판정 킬샷이 남았을 때**만** 확인 다이얼로그 | 종료는 중단이 아니라 타임테이블대로의 다음 단계다. 다만 종료 후엔 rules가 지난 라운드 판정을 막아 **되돌릴 수 없으므로** 남은 건수를 밝힌다 |
| '먼저 판정하기'는 **판정 탭까지 데려간다** | 닫기만 하면 라벨이 약속한 동작과 어긋난다 (live-qa에서 발견해 수정) |

---

## 5. 작업 C — 대기실 차수·게임 모드 표시

### 문제

대기실 기본 화면에 **모드가 아예 없었다.** `room.gameMode`는 게스트가 배정받은 뒤 보는 카드에만 넘어갔다. 모드를 보려면 배정 편집 화면까지 들어가야 하는데, 그 화면에 들어가면 대기실이 사라진다.

작업 B 이후 이게 더 중요해졌다. 라운드를 종료하면 진행자가 **바로 이 화면으로 돌아오는데**, 버튼이 둘이다.

- **게임 시작** → 직전 차수·직전 모드를 **그대로 다시** 실행
- **N+1차 팀 배정** → 새로 편성하고 새 모드 선택

배정 보드의 모드 기본값이 "직전 라운드 모드"라, 진행자가 안 바꾸면 조용히 같은 모드가 반복된다. 화면에 표시가 없으면 '게임 시작'이 무슨 모드를 켜는지 알 수 없다.

### 해결

룸 카드에 한 줄을 추가했다. 새 카드도, 새 데이터도 필요 없다.

```
┌─────────────────────────────────┐
│  ROOM 4NYY            [대기 중] │
│  4차 라운드 · 왕잡기            │  ← 추가
│  ┌───────────────────────────┐  │
│  │      초대 링크 복사       │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

- **`assignmentRound > 0`일 때만 렌더한다.** `normalizeRoom`이 `gameMode` 없는 방을 기본값 `'normal'`로 채우므로, 배정 전에도 값은 '일반전'으로 읽힌다 — 그대로 보여주면 **진행자가 고르지도 않은 모드를 확정된 것처럼 알리게 된다.**
- **차수를 모드와 붙여 쓴다.** 하단 CTA가 "N+1차 팀 배정"이라, 카드가 "4차 · 왕잡기"라고 해야 두 버튼의 차이가 읽힌다.
- **이 줄을 보는 건 호스트와 "이번 라운드에 배정되지 않은 게스트"다.** 배정된 게스트는 룸 카드가 `RoundAssignmentCard`로 통째 대체되어 보지 못하고, 그쪽 규칙서가 모드명을 이미 보여준다(live-qa W-03 실측).

---

## 작업 D — 종료 흐름 보강 (계획 확정 후 커밋 4개로 추가)

A~C를 검증하는 과정에서 드러난 구멍을 같은 브랜치에서 막았다. 결정 요지만 남긴다.

| 보강 | 내용 | 커밋 |
|---|---|---|
| 같은 차수 재실행 가드 | `roundPlayMarker`(sessionStorage, **탭 스코프 best-effort**)에 '라운드 시작' **성공 시에만** 마크. 대기실 '게임 시작'이 마커를 보면 확인 다이얼로그를 띄워 '다음 팀 배정하기'를 우선 경로로 제시. 서버(rules) 강제는 후속 과제 | `ae6b4d0` |
| 기록 진입점 | 대기실에 '지난 라운드 기록 보기'(호스트·차수>0 전용) → `?tab=log` 딥링크. 기록 탭은 **waiting 상태에서도 열람 가능**(판정 탭은 playing 게이트 유지 — 판정 쓰기는 rules가 playing에서만 허용) | `ae6b4d0`, `db26802` |
| 공용화·구독 부하 | 판정/상세 시트 헤더를 `KillshotPhotoHeader`로 공용화, 기록 구독을 `orderBy(createdAt desc) + limit(200)`으로 상한, 첫 스냅샷 게이트(`recordsLoaded`)로 도착 전 "기록 없음" 오표시 제거 | `4290342`, `db26802` |
| 판정 실패 서버 확인 | 판정 실패 시 로컬 큐 추정 대신 `getSubmissionStatusFromServer`로 선판정 충돌/일반 오류를 구분 | `db26802` |
| 카메라 종료 잠금 | 타이머 0(displayState `ended`)이면 셔터 disabled + `submitPhoto` 가드 — 확인 화면을 열어 둔 채 00:00을 넘기는 경로까지 차단 | `6ece8af` |
| 머지 검토 반영 | waiting+차수>0인 방의 운영/판정 탭이 "게임이 아직 시작되지 않았어요"를 보여주던 모순 카피를 상태 사실에 맞게 교체(판정 배지·본문 불일치가 재실행 가드를 무력화하던 문제) | 2026-08-12 |

## 6. 검증

### 단위 테스트

**603개 전부 통과** (type-check ✅ / lint ✅, 2026-08-12 기준). 이 브랜치에서 늘어난 테스트가 A·B·C·D 전부를 커버한다.

### 브라우저 실동작 (live-qa)

main이 이 브랜치의 정확한 base라 **수정 전/후 dev 서버 2대를 띄워 같은 방·같은 상태를 대조**했다. 실제 Firebase에 붙었고, 판정은 화면 텍스트가 아니라 `rooms/{code}` 문서와 `submissions` 컬렉션을 REST로 직접 읽어 대조했다.

**10개 케이스 전부 통과.** [QA 보고서(스크린샷 12장 포함)](https://claude.ai/code/artifact/ae9f056a-c42e-435a-b23d-589a1a0b8762) · `docs/qa/round-finish-report.html`

작업 D 반영분은 **2026-08-10 live-qa 회차**(실 Firebase, 방 8CWJ)로 별도 실측했다 — 판정은 `docs/qa/record-review.md`·`docs/qa/waiting-room.md`에 TC 단위로 기입돼 있다(재실행 다이얼로그·딥링크·waiting 열람·ended 시간 조정 복구 등 12케이스 실측 통과).

수정 전 결함도 실측으로 재현했다:

```
[재시작 전] assignmentRound=1  submissions=[{status:"pending", round:"1"}]
[재시작 후] assignmentRound=1  submissions=[{status:"pending", round:"1"}]
            durationMs=1200000  ← 새 20분이 걸렸는데 차수가 그대로 → 미판정 건이 새 큐에 남음
```

QA 중 발견해 고친 것 2건: ① 게스트 시야에 대한 코드 주석이 사실과 달랐음 ② '먼저 판정하기'가 판정 탭으로 가지 않았음. 개선 후보 1건(다이얼로그에서 파괴적 액션의 색 무게가 안전 액션보다 강함)은 디자인 결정이라 남겨뒀다.

---

## 7. 남은 작업 · 미정 스펙

### 행사 전 필수 수정 (2026-08-12 머지 검토에서 확정)

머지 블로커는 아니지만 야외 행사 당일 전에 반드시 처리할 4건:

1. **쓰기 매달림 시 '라운드 종료' 무반응** — 다른 액션(pendingAction)이 오프라인으로 settle되지 않으면 `finishGame()`이 가드에서 조용히 false를 반환하고 다이얼로그만 닫힌다. 진행자에게 아무 피드백이 없다.
2. **캐시 신선도 미검사** — 종료 확인의 uncertain은 리스너 터미널 오류만 잡는다(`fromCache` 미사용). 음영지역에서 큐가 "0건"으로 보이면 확인 없이 종료된다.
3. **운영 탭 밖에는 종료 신호가 없음** — 타이머·상태 배지·종료 버튼이 전부 운영 탭 안에만 있어, 판정 탭에서 작업 중이면 20분이 끝나도 모른다.
4. ~~**게스트 콕핏에 출구 없음**~~ — **해결(2026-08-31, `fix/cockpit-round-end-exit`)**. 종료되면 셔터·아이템 자리를 `대기실로 돌아가기` 하나로 교체하고, 대기실의 콕핏 복귀 판정을 '라운드가 있다'에서 '남은 시간이 있다'(`isRoundLive`)로 좁혔다 — 버튼만 달면 대기실이 즉시 되밀어 내기 때문이다. 진행자가 시간을 더하면 자동 재진입도 살아난다. 자동 이탈은 넣지 않았다(확인 화면의 사진 유실 + 종료 판정이 기기 시계 기준). unit 636개 통과 + **2026-08-31 live-qa 실측**(수정 전/후 dev 2대 대조, 방 UTT7 — 수정 전에는 대기실로 나가면 `/waiting-room → /camera`로 되밀리는 것을 재현했다). 실기기 자연 종료·기기 시계 오차(M-05·M-06)는 미검증.

참고 사실관계: 미판정 킬샷의 영구 손실 시점은 라운드 종료가 아니라 **다음 배정 확정(assignmentRound +1)** 이다 — 같은 차수 재시작으로 판정이 부활한다(rules 판정 조건은 playing+차수 일치뿐). 종료·재실행 다이얼로그 문구를 손볼 때 이 기준을 따를 것.

### 별도 브랜치로 착수 (2026-08-12)

- **일시정지(올스탑) 중 촬영·제출 잠금** — P05부터 정지 중 촬영이 유효했던 구멍. 카메라 잠금 + rules `round.status == 'running'` 검사.
- **라운드별 게임 모드 기록** — `rooms.gameMode`가 배정마다 덮어써져 과거 라운드 모드가 유실된다. 배정 확정 시 `roundModes.{차수}`를 함께 커밋.

### 다음 작업 (별도 진행하기로 함)

**§3.2~3.5 네 모드 실제 구현** — 현재 `available: true`는 `normal` 하나뿐이라 다른 모드를 고를 수 없다.

1. `game-mode/modes/`의 `available` 플래그 열기
2. **모드별 편성 알고리즘 확장** — 지금 배정은 2인 1조만 된다. 꼬리잡기는 알파벳 상성 체인, 그룹전·왕잡기는 4색 그룹, 스태프 추격전은 전원 동맹이라 편성 단위 자체가 다르다. `game-mode/types.ts` 주석이 이미 이걸 후속 작업으로 적어뒀다.

이쪽이 라운드 종료 배선보다 훨씬 큰 작업이다.

### 기획 미정 (기획자 확인 필요)

| 항목 | 내용 |
|---|---|
| **라운드별 점수 누적** | 3~5라운드에 걸친 최종 순위 산정 규칙이 기획서 어디에도 없다. 모드마다 점수 단위가 달라(일반전 팀 점수 +30 / 왕잡기 그룹 감점 / 스태프 추격전 생존 비율 / 빠른생존 포인트 적립) 어떻게 하나로 합칠지 미정 |
| **꼼꼬미 멱등성 키** | §6.2의 멱등성 키가 `userId + targetId + roundNumber`인데, 꼼꼬미는 한 차수 안에서 판이 반복되므로 **같은 술래가 같은 플레이어를 2판에 걸쳐 잡으면 두 번째가 중복으로 무시된다.** 내부 판 번호가 키에 필요 |

---

## 8. 현재 상태 (2026-08-12 기준)

**[PR #21](https://github.com/limtaegeun/photo-royale/pull/21)** (base: `main`)이 열려 있고, A~D 전부 커밋·푸시 완료다.

```
bd7a8e4  docs: P06 반영분 TC와 live-qa 판정을 기록
6ece8af  feat: 라운드 종료 시 촬영과 제출을 잠근다
db26802  feat: 라운드 종료 확인과 기록 열람을 견고화한다
4290342  refactor: 기록 목록·판정 시트를 공용화하고 구독 부하를 줄인다
ae6b4d0  feat: 대기실에 같은 차수 재실행 가드와 기록 진입점을 추가
3ed0f38  docs: P06 계획 문서와 라운드 종료 QA 보고서를 추가
cc095da  fix: 먼저 판정하기가 판정 탭으로 이동하게 연결
226ad01  feat: 대기실 룸 카드에 확정 차수·게임 모드를 표시
de921a3  feat: 라운드 종료 후 다음 라운드 재편성으로 넘긴다
713ec21  docs: 기록 탭 QA 검증 문서를 추가
7209523  feat: 라운드 운영 기록 탭을 구현
75eaaf6  feat: 판정 이력 구독 API와 스토어를 추가
```

2026-08-12 머지 검토(에이전트 2계통 + 코드 재검증) 결과 **머지 가능** 판정 — 검토에서 나온 수정분(작업 D 표의 "머지 검토 반영" 행)과 이 문서 갱신이 후속 커밋으로 추가된다. 행사 전 필수 4건은 §7 참조.
