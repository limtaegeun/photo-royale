# P08 콕핏 슬롯 4개 — 기록 · 지도 · 아이템 2종 기획 (초안)

- **작성일**: 2026-09-20 초안 → **2026-09-20 기획자 답변 반영 — 결정 6건 전부 확정**(아이템은 MVP에서 제거 · 지도는 운영자 URL · 기록 시트 썸네일 허용). 노션 규칙서에는 반영하지 않는다
- **대상**: 카메라 콕핏(`CameraPage.vue`) 하단 컨트롤 4칸 — `1 아이템` · `2 아이템` · `3 지도` · `4 기록`. P04 와이어프레임에서 자리만 잡고 규칙서 v1.0에는 정의가 없다. 지금은 전부 비활성 버튼이다.
- **선행**: `p07-round-scoring.md`(원장 `rounds/{차수}`: teams·tally·hits·tails·credits·result), `p07-elimination.md`(라이프·탈락), #59(내 킬샷 판정 결과 구독 `subscribeToMySubmissions`)

---

## 0. 한 줄 요약

**MVP는 두 칸 — 기록과 지도.** 둘 다 이미 있는 데이터를 보여주는 일이라 rules 변경은 지도의 `mapImageUrl` 한 줄뿐이다. 아이템 2종(보호막·저격)은 기획자 결정(2026-09-20)으로 **MVP에서 제거**했다 — §2.3·2.4는 나중에 아이템을 넣을 때 출발점으로만 남긴다. 콕핏 하단 격자는 아이템 칸을 지우고 **지도 · 기록 2칸**이 된다.

## 1. 설계 원칙

1. **판정은 호스트가 한다** — 아이템도 참가자가 "쓰겠다"고 선언하고, 효과는 판정·정산 시점에 원장 규칙으로 계산한다. 참가자 기기가 점수를 만들지 않는다(현 모델과 같다).
2. **데이터는 원장 하나** — 새 컬렉션을 만들지 않는다. 아이템 상태는 `rounds/{차수}`(라운드 단위) 또는 `participants/{uid}`(게임 단위)에 필드로 얹고 rules 갈래를 한 줄씩 늘린다. 배포 순서는 rules 선배포 → 클라.
3. **야외·음영지역 전제** — 슬롯은 시트(BaseBottomSheet)로 열고 오프라인에서도 캐시로 읽히는 것만 보인다. 아이템 사용 쓰기는 다른 쓰기와 같이 큐잉되고 latency compensation으로 즉시 반영된다.
4. **모드가 슬롯을 소유** — 모드마다 켤 슬롯이 다르다(스태프 추격전에는 저격이 없다). `GameModeDefinition`에 슬롯 가용성을 적어 콕핏은 읽기만 한다(M4의 `targeting`·`killshotLock`과 같은 방식).
5. **자리만 있는 버튼을 남기지 않는다** — 기능이 없는 슬롯은 숨긴다. 4칸 격자는 있는 만큼만 채운다.

## 2. 슬롯별 제안

### 2.1 슬롯 4 — 기록 (내 팀 기록) · **P1, 즉시 가능**

| 항목 | 내용 |
|---|---|
| 목적 | "내가 올린 킬샷이 어떻게 됐나, 우리 팀은 지금 몇 킬인가, 지난 라운드 등급은"을 콕핏을 떠나지 않고 본다. 지금은 토스트(#59)가 지나가면 끝이다 |
| 화면 | 바텀시트 3단: ① **이번 라운드 내 킬샷** — 썸네일(본인 사진만, 확대·저장 없음)·시각·상태 배지(대기/인정 +잡은 팀/반려), ② **우리 팀 이번 라운드** — 킬 N(3배 포함 표시)·생존/탈락·(꼬리잡기) 편입 크레딧, ③ **지난 라운드** — `1R 2등급 · 2R 1등급 · 원점수 N · 누적 NP`(대기실 순위 시트의 `formatRounds`와 같은 문구) |
| 데이터 | 전부 있음 — `subscribeToMySubmissions`(#59), 현재 원장 `tally/hits/credits`(콕핏이 이미 구독), 지난 원장 `subscribeToRoundLedgers` + `computeStandings`(대기실이 이미 씀) |
| rules | 변경 없음 |
| 모드별 | 전 모드 켬. 스태프 추격전은 ①이 비어 있으니 "이 모드에는 킬샷이 없어요" 한 줄 |
| 공수 | 1~2바퀴(시트 1개 + 컴포넌트 재사용). 사진 데이터 URL이 실려 있으니 썸네일은 `img` 축소 표시로 충분 |

### 2.2 슬롯 3 — 지도 · **P2, 정적부터**

| 항목 | 내용 |
|---|---|
| 목적 | 행사장 경계·안전 구역·집결지를 뛰는 중에 한 번에 본다. 규칙 카드의 "도로 진입 금지"가 지도 위에 보여야 지켜진다 |
| 단계 1 (정적) | 방 문서 `mapImageUrl`(호스트가 방 만들 때 또는 대기실에서 URL 붙이기)을 콕핏 시트에 꽉 채워 보인다. 핀치 줌은 브라우저 기본. 이미지는 hosting에 같이 배포하는 정적 자산이거나 외부 URL — Storage 없이 간다 |
| 단계 2 (내 위치) | `geolocation` 1회 권한으로 내 점만 얹는다(공유 없음, 배터리 최소). 지도 이미지에 좌표 두 점(네 귀 위경도)을 호스트가 입력해야 하므로 방 설정 UI가 필요 — 규칙서에 지도 준비 절차를 추가해야 해서 기획 확인 대상 |
| 단계 3 (보류) | 팀원·상대 위치 공유는 하지 않는다 — 개인정보·배터리·"사진으로 잡는 게임"의 재미와 충돌 |
| rules | 단계 1: `rooms` 갈래에 `mapImageUrl`(string, 호스트만, waiting 중) 한 줄 |
| 모드별 | 전 모드 켬. 지도가 등록되지 않은 방은 슬롯을 숨긴다 |
| 공수 | 단계 1은 1바퀴, 단계 2는 2바퀴 + 기획 확인 |

### 2.3 (MVP 제외) 아이템 「보호막」 — 후일 참고용

| 항목 | 내용 |
|---|---|
| 효과 | 이번 라운드 우리 팀 **라이프 +1**. 2인 팀은 한 번 잡혀도 살고, 1인 팀은 세 번째 히트에 탈락한다 |
| 사용 | 게임당 팀 1회(참가자 누구나 대표로 누름). 라운드 시작 뒤 잡히기 전까지만 |
| 데이터 | `rounds/{차수}.shields: { [완장]: true }`(라운드 단위) + `participants/{uid}.itemsUsed.shield: 차수`(게임당 1회 근거). `livesOf`가 `shields[완장]`이면 +1 |
| 판정 | 판정 시트의 대상 행에 **보호막** 배지 — 호스트가 "한 번 더 잡아야 탈락"을 안다. 스태프 아웃 시트도 같다 |
| 점수 | 직접 효과 없음(살아남아 생존 5·다음 킬 기회를 얻는 간접 효과). 왕잡기의 왕(X 팀)에게 허용할지가 쟁점 — 왕 보호막은 그룹 −20을 한 번 막는 강한 카드다 |
| rules | `rounds` 갈래에 `shields` 선택 키(map, 참가자 본인 팀 완장만, 값 true, 1회) · `participants` 갈래에 `itemsUsed`(본인만) |

### 2.4 (MVP 제외) 아이템 「저격」 — 후일 참고용

| 항목 | 내용 |
|---|---|
| 효과 | 이 아이템을 켠 채 올린 **킬샷 1건이 인정되면 2배**(일반 10 → 20). 낙오 3배와는 겹치지 않는다(호스트가 3배를 고르면 3배가 우선) |
| 사용 | 게임당 팀 1회. 촬영 전에 켜고, 그 사진의 제출 문서에 `boost: true`가 붙는다. 반려되면 소모되지 않는다(다음 사진에 다시 켤 수 있다) |
| 데이터 | `submissions/{id}.boost: true`(제출자만 쓰는 선택 키) → 승인 시 `multiplier: 2` 허용(`KillMultiplier` 1·2·3) → `tally.{완장}.doubleKills` |
| 판정 | 판정 시트에 **저격** 배지. 호스트는 배율을 고르지 않는다 — 2배는 자동, 3배 토글을 켜면 3배 |
| 점수 | 그룹 동료 킬(+5)은 건수 기준이라 그대로. 왕잡기 왕 사냥(30)과 겹치면 30 유지(저격 소모 안 함) |
| 모드별 | 스태프 추격전 꺼짐(킬 없음). 그 외 켬 |
| rules | `submissions` create에 `boost` 선택 키, update(판정)에 `multiplier in [1,2,3]`, `rounds` 집계 갈래는 map 검사라 변경 없음 |

### 2.5 배치 안

| 칸 | 지금 라벨 | 결정(2026-09-20) | 기능 없을 때 |
|---|---|---|---|
| 1 | 아이템 | **삭제** — MVP에 아이템 없음 | — |
| 2 | 아이템 | **삭제** | — |
| 3 | 지도 | 지도 (방에 `mapImageUrl`이 있을 때) | 숨김 |
| 4 | 기록 | 기록 (항상) | — |

격자는 셔터 좌우 한 칸씩(왼쪽 지도, 오른쪽 기록)이 된다. 지도가 없는 방은 기록 한 칸만 남는다.

## 3. 단계와 공수

| 단계 | 내용 | rules | 공수(바퀴) | 전제 |
|---|---|---|---|---|
| **1** | 기록 시트(2.1) + 아이템 칸 삭제·격자 2칸(2.5) | 없음 | 2 | 없음 — 바로 시작 가능 |
| **2** | 지도 단계 1 — 운영자가 URL로 등록(`mapImageUrl`) + 시트 | 1줄 | 1 | 없음(출처는 URL로 확정) |
| 3 | 지도 단계 2(내 위치) | 없음 | 2 | 지도 좌표 입력 UI·규칙서 절차 |
| — | 아이템(보호막·저격) | 3곳 | 4~5 | **MVP 제외** — 넣기로 하면 §2.3·2.4에서 다시 시작 |

## 4. 기획자 결정 (2026-09-20 1차)

| # | 질문 | 결정 |
|---|---|---|
| 1 | 아이템을 넣을 것인가 | **MVP에서 제거.** 슬롯 1·2 삭제, 격자는 지도·기록 2칸 |
| 2 | (아이템) 사용 횟수 단위 — 게임 전체에 1번인지, 라운드마다 1번인지 | 1번 결정으로 **해당 없음** |
| 3 | (아이템) 보호막을 왕잡기의 왕 팀도 쓸 수 있게 할지 | 1번 결정으로 **해당 없음** — 보호막·저격은 규칙서에 없는 내 제안이었다 |
| 4 | (아이템) 저격 2배와 낙오 3배가 같은 킬샷에 겹치면 3배만 줄지 6배로 곱할지 | 1번 결정으로 **해당 없음** |
| 5 | 지도 이미지 출처 | **운영자가 URL로 등록**(방 문서 `mapImageUrl`) |
| 6 | 기록 시트의 사진 노출 — 내가 찍은 킬샷에는 상대 참가자의 얼굴이 찍혀 있다. 지금은 호스트의 기록 탭에서만 그 사진을 다시 볼 수 있는데, 기록 시트를 만들면 **찍은 사람 본인의 폰에서도** 자기 사진을 다시 보게 된다. 그래도 되는지 | **보여준다**(2026-09-20 확정) — 본인이 찍은 사진만, 썸네일 크기로만(확대·저장 없음). 배지만으로는 여러 장 중 어느 사진이 반려됐는지 알 수 없어 사진이 답이다 |

## 5. 하지 않는 것

- 위치 공유(팀원·상대) — §2.2 단계 3.
- 아이템 자체 — 2026-09-20 결정으로 MVP 밖. 넣기로 하면 획득 메커니즘(QR·현장 카드) 없이 시작 시 팀당 1장으로 시작하는 안(§2.3·2.4)에서 출발한다.
- 참가자 기기에서 점수 계산·상태 변경 — 원칙 1.
