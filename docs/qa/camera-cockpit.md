# 카메라 콕핏 QA Test Cases

- 브랜치: `feat/p04-camera-cockpit`
- 작성일: 2026-07-25
- 전수 재검증: 2026-07-26 — 기존 완료 표시는 근거로 재사용하지 않고 모든 TC를 새 실행으로 다시 판정한다.
- 크리티컬리티: 야외 실시간 게임 핵심 화면 — 카메라·타이머·팀 식별 실패 시 진행 불가
- 후속 플로우: `live-qa`에서 P1 → P2 → P3 검증 후 P0 리허설 게이트

## 작업 범위 요약

| 영역                 | 변경 내용                                                         | 근거                                                                             |
| -------------------- | ----------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| 실시간 HUD           | 게임 모드 목표, 남은 시간, 내 팀·팀원, 생존 팀 수, 최신 공지 표시 | `src/features/camera/CameraPage.vue`                                             |
| 카메라 조작          | 중앙 소형 조준점, 킬샷 셔터, 촬영 실패 안내                       | `src/features/camera/CameraPage.vue`, `useCameraStream.ts`, `usePhotoCapture.ts` |
| 킬샷 확인            | 촬영 프레임 미리보기, 다시 찍기, 제출 준비 중 상태                | `src/features/camera/CameraPage.vue`                                             |
| 후속 기능 자리       | 아이템 1·2, 지도 3, 기록 4 비활성 mock                            | `src/features/camera/CameraPage.vue`                                             |
| 실시간 구독 수명주기 | 방·참가자·최신 공지 구독과 화면 이탈 시 해제                      | `src/features/camera/CameraPage.vue`                                             |
| 공용 버튼 확장       | HUD variant, content size, circle shape 및 기존 버튼 회귀          | `src/shared/components/BaseButton.vue`                                           |
| 단위 회귀            | HUD, 공지 확장, 촬영·재촬영, 권한, 라우팅 검증 보강               | `src/features/camera/__tests__/CameraPage.spec.ts`                               |

## 코드에서 도출한 크리티컬 리스크

1. 방·참가자·공지 `onSnapshot`의 영구 오류는 한 번 안내한 뒤 입장 화면으로 안전하게 이동한다. (H-01)
2. 카메라는 HTTPS/localhost와 사용자 권한이 필수이며 iOS/Android의 후면 카메라 선택 결과가 다를 수 있다. (A-02, H-02)
3. 생존 탈락 데이터 모델이 아직 없어 `N팀 생존`은 이번 차수에 배정된 고유 팀 수를 표시한다. (B-03)
4. 킬샷은 브라우저 메모리의 Blob URL만 만들며 기기 갤러리 저장·호스트 전송은 의도적으로 하지 않는다. (E-02, F-01)
5. 상·하단 HUD가 작은 화면/큰 글자에서 뷰파인더를 과도하게 가릴 수 있다. (G-01, G-02)
6. `BaseButton`의 패딩·배경은 사용처 클래스 덮어쓰기가 아닌 `padding` prop과 `shutter` variant로 선택한다. (J-01, J-02)

## 테스트 환경·사전 조건

- local: 현재 워크트리 Vite dev 서버, 실제 Firebase 프로젝트
- baseline: `main`은 HUD 이전 상태이므로 기능 대조가 필요한 대표 케이스만 사용
- 계정: 호스트 1명, 참가자 2명 이상. 참가자는 같은/다른 팀 조합 포함
- 방 상태: `status=playing`, `assignmentRound>=1`, `round.status=running`
- 브라우저: 데스크톱 Chrome fake camera + iOS Safari/Android Chrome 실기기 후속 확인
- 카메라 자동화: Chrome `--use-fake-device-for-media-stream --use-fake-ui-for-media-stream`
- 네트워크: Firestore Listen/Write 응답과 DOM을 함께 판정. 콘솔 직접 수정은 rules를 우회하므로 rules 근거로 사용하지 않음

## A. 진입·카메라 권한

| ID          | 우선순위 | 상태           | 사전조건               | 절차                      | 기대 결과                                                           |
| ----------- | -------- | -------------- | ---------------------- | ------------------------- | ------------------------------------------------------------------- |
| A-01 [unit] | P1       | ✅ 완료        | 진행 중 방의 참가자    | `/camera/{roomCode}` 진입 | 후면 카메라 스트림이 전체 화면에 재생되고 `킬샷 촬영` 버튼이 보인다 |
| A-02 [unit] | P1       | ⚠️ 부분/프록시 | 카메라 권한 거부       | 콕핏 진입                 | `카메라 권한이 거부되었습니다`와 `다시 시도`가 표시된다             |
| A-03 [unit] | P2       | ⚠️ 부분/프록시 | 카메라 장치 없음       | 콕핏 진입                 | `사용할 수 있는 카메라를 찾지 못했습니다`가 표시된다                |
| A-04 [unit] | P2       | ⚠️ 부분/프록시 | 카메라 점유 오류       | 콕핏 진입                 | `카메라를 켜지 못했습니다`와 `다시 시도`가 표시된다                 |
| A-05 [unit] | P1       | ⚠️ 부분/프록시 | 권한 거부 후 허용 가능 | `다시 시도` 선택          | 스트림이 활성화되고 셔터가 표시된다                                 |

## B. 목표·팀·타이머 HUD

| ID          | 우선순위 | 상태           | 사전조건                             | 절차                  | 기대 결과                                                                       |
| ----------- | -------- | -------------- | ------------------------------------ | --------------------- | ------------------------------------------------------------------------------- |
| B-01 [unit] | P1       | ✅ 완료        | 일반전, running 라운드               | 콕핏 확인             | `일반전`, `상대 완장 알파벳을 찍어 제출하세요.`, `남은 시간`과 `mm:ss`가 보인다 |
| B-02 [unit] | P1       | ✅ 완료        | 내 팀 A, 팀원 하린                   | 콕핏 확인             | `팀 A · 하린`이 팀 색상과 텍스트로 함께 표시된다                                |
| B-03 [unit] | P1       | ✅ 완료        | 이번 차수 A/B 두 팀 배정             | 콕핏 확인             | `2팀 생존`이 표시된다                                                           |
| B-04        | P1       | ✅ 완료        | 호스트가 타이머 진행/일시정지/조정   | 두 화면에서 시간 변경 | 참가자 시간이 서버 round 상태를 따라 갱신되고 일시정지 시 경고색이다            |
| B-05        | P2       | ⚠️ 부분/프록시 | 이번 차수 미배정 또는 사용자 매칭 전 | 콕핏 확인             | `팀 확인 중`으로 안전하게 수렴한다                                              |
| B-06        | P2       | ✅ 완료        | 1인 팀                               | 콕핏 확인             | `팀 {완장} · 나 홀로 생존`으로 표시된다                                         |

## C. 공지

| ID          | 우선순위 | 상태           | 사전조건                         | 절차                                      | 기대 결과                                                                                             |
| ----------- | -------- | -------------- | -------------------------------- | ----------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| C-01 [unit] | P2       | ✅ 완료        | 공지 없음                        | 콕핏 확인                                 | `새로운 공지가 없습니다.`가 표시된다                                                                  |
| C-02        | P1       | ✅ 완료        | 호스트·참가자 동시 접속          | 호스트가 긴 공지 전송                     | 참가자 HUD가 최신 공지로 실시간 갱신된다                                                              |
| C-03 [unit] | P1       | ✅ 완료        | 공백 없는 최대 길이 공지 존재    | 공지 행 선택                              | 펼치면 문자열 중간에서도 줄바꿈되어 전체 본문이 보이고 `공지` 헤더와 아이콘은 행 상단에 고정된다      |
| C-04        | P3       | ⚠️ 부분/프록시 | 키보드 사용                      | Tab 후 Enter/Space                        | overflow 공지 행에 포커스가 가고 키보드로 확장·축소된다                                               |
| C-05        | P1       | ✅ 완료        | 짧은/긴 공지·접힘 상태           | `scrollWidth/clientWidth`와 스타일 실측   | 짧은 본문은 정적이고 긴 본문만 흐르며 아이콘은 접힘·펼침 모두 오른쪽 끝에 고정된다                    |
| C-06        | P1       | ❌ 실패        | 짧은 공지                        | 공지 행 클릭 전후 ARIA·아이콘·높이 비교   | 클릭해도 펼침 상태, 화살표 방향, 행 정렬과 높이가 바뀌지 않는 true no-op이다                           |
| C-07        | P1       | ✅ 완료        | 긴 공지 최초 표시                | 0초·초기 정지 후·중간 위치를 시간별 측정  | 최초에는 잠시 멈춘 뒤 약 24px/s로 이동해 게임 중에도 읽을 수 있다                                     |
| C-08        | P1       | ✅ 완료        | 긴 공지가 마지막 문구에 도달     | 한 사이클 경계 전후 transform 비교        | 끝에 잠시 머문 다음 반대 방향으로 흐르지 않고 첫 문구 위치에서 새 사이클을 시작한다                   |
| C-09        | P1       | ✅ 완료        | 긴 공지를 펼친 상태              | 접은 직후와 다음 사이클 시작을 각각 측정  | 접은 직후에는 대기 없이 즉시 흐르고, 그 다음 반복부터는 일반 최초 정지 구간이 다시 적용된다           |
| C-10        | P2       | ✅ 완료        | `prefers-reduced-motion: reduce` | 짧은/긴 공지와 펼침 상태 확인             | 자동 이동 없이 한 줄 말줄임으로 유지되며, 펼치면 전체 내용이 보이고 아이콘·레이아웃이 흔들리지 않는다 |

## D. 조준점·하단 조작부

| ID          | 우선순위 | 상태    | 사전조건      | 절차                | 기대 결과                                                                              |
| ----------- | -------- | ------- | ------------- | ------------------- | -------------------------------------------------------------------------------------- |
| D-01        | P1       | ✅ 완료 | 카메라 활성   | 중앙 확인           | 작은 원형 조준점이 정중앙에 있고 카메라 피사체를 과도하게 가리지 않는다                |
| D-02 [unit] | P2       | ✅ 완료 | 카메라 활성   | 하단 확인           | 왼쪽은 `1 아이템 → 3 지도`, 오른쪽은 `2 아이템 → 4 기록` 세로 배치이며 모두 비활성이다 |
| D-03        | P3       | ✅ 완료 | 모바일 뷰포트 | 각 컨트롤 크기 측정 | 셔터와 공지 터치 영역은 최소 48px이고 safe-area를 침범하지 않는다                      |

## E. 촬영·킬샷 확인

| ID          | 우선순위 | 상태           | 사전조건                           | 절차                        | 기대 결과                                                                          |
| ----------- | -------- | -------------- | ---------------------------------- | --------------------------- | ---------------------------------------------------------------------------------- |
| E-01 [unit] | P1       | ✅ 완료        | 카메라 활성                        | `킬샷 촬영` 선택            | 현재 프레임이 캡처되고 `킬샷 확인`, `다시 찍기`, `제출 준비 중` 화면이 즉시 열린다 |
| E-02        | P1       | ✅ 완료        | 킬샷 확인 화면                     | 다운로드/갤러리 변화를 관찰 | 파일 다운로드가 발생하지 않고 사진은 브라우저 미리보기에만 존재한다                |
| E-03 [unit] | P1       | ✅ 완료        | 킬샷 확인 화면                     | `다시 찍기` 선택            | Blob URL이 해제되고 같은 카메라 스트림의 콕핏으로 돌아간다                         |
| E-04 [unit] | P2       | ✅ 완료        | 비디오 메타데이터 0 또는 Blob 실패 | 셔터 선택                   | `촬영에 실패했습니다. 다시 시도해주세요`가 role=alert로 표시된다                   |
| E-05        | P2       | ⚠️ 부분/프록시 | 셔터 연속 입력                     | 빠르게 2회 선택             | 앱이 중단되지 않고 마지막 캡처 한 장만 미리보기로 유지한다                         |

## F. 의도된 미구현·회귀

| ID          | 우선순위 | 상태           | 사전조건       | 절차                           | 기대 결과                                                              |
| ----------- | -------- | -------------- | -------------- | ------------------------------ | ---------------------------------------------------------------------- |
| F-01 [unit] | P1       | ✅ 완료        | 킬샷 확인 화면 | 제출 영역 확인                 | 공격팀 선택 UI가 없고 `제출 준비 중`은 비활성이라 네트워크 쓰기가 없다 |
| F-02 [unit] | P1       | ✅ 완료        | 게임 종료      | 호스트가 방을 waiting으로 전환 | 참가자가 `/waiting-room/{roomCode}`로 돌아간다                         |
| F-03 [unit] | P1       | ⚠️ 부분/프록시 | 방 삭제        | 콕핏에서 스냅샷 null 수신      | `방을 찾을 수 없어요.` 안내 후 입장 화면으로 이동한다                  |
| F-04 [unit] | P2       | ⚠️ 부분/프록시 | 콕핏 이탈      | 다른 화면으로 이동             | 카메라 트랙과 방·참가자·공지 구독이 모두 해제된다                      |

## G. 모바일·접근성·레이아웃

| ID   | 우선순위 | 상태           | 사전조건                     | 절차                | 기대 결과                                                                                  |
| ---- | -------- | -------------- | ---------------------------- | ------------------- | ------------------------------------------------------------------------------------------ |
| G-01 | P1       | ⚠️ 부분/프록시 | 320×568, 390×844, 430×932    | 짧은/긴/펼친 공지로 각 뷰포트 확인 | 상단 HUD·조준점·하단 조작부가 겹치지 않고, 공지 아이콘은 우측 끝·헤더는 펼침 시 상단을 유지한다 |
| G-02 | P2       | ⚠️ 부분/프록시 | 브라우저 200% 확대/큰 글자   | 콕핏·확인 화면 확인 | 핵심 목표·시간·셔터·다시 찍기에 접근 가능하고 가로 스크롤이 없다                           |
| G-03 | P3       | ⚠️ 부분/프록시 | 스크린리더/접근성 트리       | 콕핏 검사           | 셔터 이름은 `킬샷 촬영`, 시간은 `게임 남은 시간`, 사진 alt는 `방금 촬영한 킬샷`이다        |
| G-04 | P2       | ✅ 완료        | 밝은/복잡한 fake camera 영상 | HUD 육안 확인       | 상·하단 전체 배경 없이 정보 단위의 작은 `bg-scrim-strong`만 사용하고 텍스트를 읽을 수 있다 |

## H. 강인성·수명주기

| ID          | 우선순위 | 상태           | 사전조건                  | 절차                               | 기대 결과                                                                   |
| ----------- | -------- | -------------- | ------------------------- | ---------------------------------- | --------------------------------------------------------------------------- |
| H-01 [unit] | P1       | ✅ 완료        | 콕핏 접속 중              | 인증 상실 등 영구 Listen 오류 발생 | 오류를 한 번 안내하고 입장 화면으로 이동해 stale HUD를 계속 노출하지 않는다 |
| H-02        | P1       | 🚫 불가        | iOS Safari·Android Chrome | 화면 잠금 5분 후 복귀              | 카메라와 타이머가 현재 상태로 복구되거나 명확한 재시도 화면을 보인다        |
| H-03        | P2       | ⏸ 미검증       | 콕핏                      | 진입/이탈 10회                     | 카메라 트랙·Firestore Listen이 누적되지 않는다                              |
| H-04        | P2       | ⚠️ 부분/프록시 | 백그라운드 30초           | 복귀                               | 타이머가 즉시 서버 앵커 기준 현재 값으로 보정된다                           |

## I. 종합 리허설

| ID   | 우선순위 | 상태         | 사전조건                     | 절차                                                                     | 기대 결과                                                                      |
| ---- | -------- | ------------ | ---------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------ |
| I-01 | P0       | ⚠️ 부분 통과 | 호스트 1·참가자 2+, 실카메라 | 방 생성→입장→팀 배정→게임 시작→라운드 시작→공지→촬영→다시 찍기→게임 종료 | 역할별 화면 전환, 실시간 HUD, 킬샷 확인, 대기실 복귀가 한 판에서 모두 성공한다 |

## J. 공용 BaseButton 회귀

| ID   | 우선순위 | 상태      | 사전조건              | 절차                                  | 기대 결과                                                                 |
| ---- | -------- | --------- | --------------------- | ------------------------------------- | ------------------------------------------------------------------------- |
| J-01 | P1       | 🔄 수정/재검증 대기 | 카메라 활성           | 공지·슬롯·셔터 computed style 측정     | 공지는 좌우 12px, 슬롯과 셔터는 좌우 0px이며 사용처의 패딩 의도가 적용된다 |
| J-02 | P1       | 🔄 수정/재검증 대기 | 카메라 활성           | 셔터 배경·내부 원 크기와 비율 측정     | 셔터는 `bg-scrim-weak`, 내부 원은 56×56px 원형으로 찌그러지지 않는다       |
| J-03 | P1       | ✅ 완료   | HUD 공지·슬롯 렌더    | 높이·클릭 영역과 disabled 스타일 측정 | 공지는 최소 48px, 슬롯은 64×64px 원형이며 disabled HUD 색을 유지한다       |
| J-04 | P2       | ⚠️ 부분/프록시 | 기존 BaseButton 화면  | 로그인·대기실·운영 화면 CTA 스팟 체크 | 기존 primary/neutral/ghost 버튼의 높이·radius·disabled 외관이 유지된다     |

## 커버리지 매트릭스

| 영역            |  TC 수 |    P0/P1 | unit 커버 | live-qa 중점              |
| --------------- | -----: | -------: | --------: | ------------------------- |
| A 진입·권한     |      5 |      0/3 |         5 | 실제 권한·후면 카메라     |
| B HUD           |      6 |      0/4 |         3 | 실시간 타이머·팀 데이터   |
| C 공지          |     10 |      0/7 |         2 | 실시간·속도·반복·펼침 UX   |
| D 조작부        |      3 |      0/1 |         1 | 시각 크기·safe-area       |
| E 촬영          |      5 |      0/3 |         4 | 실제 canvas·Blob·연타     |
| F 미구현·회귀   |      4 |      0/3 |         4 | 네트워크 무쓰기·구독 해제 |
| G 모바일·접근성 |      4 |      0/1 |         0 | 뷰포트·접근성 트리        |
| H 강인성        |      4 |      0/2 |         0 | 실기기·네트워크·수명주기  |
| I 리허설        |      1 |      1/0 |         0 | 전체 한 판                |
| J 공용 버튼     |      4 |      0/3 |         0 | computed style·기존 회귀  |
| **합계**        | **46** | **1/27** |    **19** |                           |

## 당일 운영 체크리스트

- HTTPS에서 카메라 권한을 사전 허용하고 후면 카메라가 선택되는지 확인한다.
- 호스트와 참가자 기기 시간이 자동 설정인지 확인한다.
- 팀 배정·라운드 시작·공지 1건·촬영/재촬영을 스모크한다.
- 카메라 복구 실패 시 브라우저 재진입, HUD 구독 실패 시 대기실 재입장을 백업 절차로 안내한다.
- `I-01` 실패 시 게임 시작을 보류한다.

## 알려진 한계

- 킬샷 호스트 제출과 공격팀 선택은 이번 범위가 아니다. `제출 준비 중`으로 비활성 표시한다.
- 아이템 1·2, 지도 3, 기록 4는 후속 PR용 비활성 mock이다.
- 탈락/생존 상태 저장 모델이 없어 생존 팀 수는 현재 차수에 배정된 고유 팀 수다.
- 자동화 브라우저는 데스크톱 Chrome fake camera만 보장한다. iOS/Android 실기기는 별도 현장 검증이 필요하다.

## 2026-07-25 live-qa 결과

- 환경: local `http://127.0.0.1:5173`, headless Chrome fake camera, 실제 Firebase 방 `NF5U`
- 계정: 호스트 1명 + 참가자 1명(1인 팀), 320×568 및 390×844 뷰포트
- 실연동 완료: 라운드 시작 후 양쪽 `19:57` 동기화(B-04), 공백 없는 100자 공지 실시간 반영·확장 줄바꿈(C-02~03), 게임 종료 후 `/waiting-room/NF5U` 복귀(F-02)
- 레이아웃 완료: 320/390px 가로 넘침 없음, 상·하단 전체 반투명 배경 없음, 조준점·하단 조작부 화면 내 배치(D-01~03, G-04)
- 촬영 제한: fake camera의 비디오 메타데이터가 0이라 실제 미리보기 대신 실패 안내를 실동작 확인(E-04). 성공·재촬영은 단위 테스트 프록시(E-01, E-03)
- 발견 후 수정: `BaseButton padding="compact|none"`과 `variant="shutter"`를 추가해 공지·슬롯·셔터가 공용 API에서 원하는 패딩과 배경을 직접 선택하도록 변경(J-01/J-02). 단위 테스트 통과, computed style 브라우저 재검증은 대기
- 불가/잔여: iOS Safari·Android Chrome 잠금 복귀(H-02), 반복 진입 Listen 누수(H-03), 430px·200% 확대·실기기 검증(G-01~03)
- Firebase 변경 이력: 기존 QA 방 `NF5U`에서 새 라운드 시작, 100자 공지 1건 전송, 게임 종료로 방 상태를 waiting으로 변경. `.qa-shots/camera-cockpit-current/manifest.json`에 기록함
- P0 판정: **부분 통과** — 호스트 1명·참가자 1명으로 시작→타이머→공지→촬영 오류 안전 처리→종료 복귀 체인은 통과했으나, 실카메라 촬영 성공과 참가자 2명+ 실기기 리허설은 남아 있다.

## 머지 게이트 판정

- 정적 검증: `type-check`, `lint`, 전체 단위 테스트 495개, production build 통과
- 브라우저 검증: 핵심 HUD·실시간 공지·게임 종료 복귀 통과. 기존 P1 `J-01`, `J-02` 수정 완료 후 브라우저 재검증 대기, P0 부분 통과
- 코드 컨벤션: `round-ops/index.ts` public API 경유, 헤더 컨테이너 `gap` 사용으로 feature·레이아웃 규칙을 충족함
- 출시 강인성: 영구 Listen 오류 안전 이탈(H-01)은 단위 검증했으며 iOS/Android 실기기 잠금 복귀(H-02), 반복 진입 Listen 누수(H-03)는 미검증
- 결론: 정적 게이트 통과. 기존 P1 HUD 스타일 회귀 수정은 완료했으며 J-01/J-02 브라우저 재검증과 2명 이상 실기기 P0 리허설이 필요하다.

## 2026-07-26 공지 폭 focused live-qa

- 환경: local `http://127.0.0.1:5173`, headless Chrome 390×844, 실제 Firebase QA 방 `NF5U`; baseline은 main에 HUD가 없어 local 단독 검증
- C-05 실패: 버튼은 358px·좌우 패딩 12px로 정상이지만 내부 텍스트 viewport/track이 694.8px로 팽창했다. 애니메이션 시작 시 가시 폭은 225.8px(잘못 계산된 viewport의 32.5%)이고 본문·펼침 아이콘이 버튼 밖에 있어 라벨 `공지`만 보였다
- 시간 대조: 2.5초 뒤 가시 비율 75.3%, 5초 뒤 81.7%로 본문이 늦게 들어온다. 페이지 자체 가로 스크롤은 390px로 발생하지 않았다
- 양성 케이스: 펼친 상태는 가용 폭 277.5/277.5px, 애니메이션 없음, 전체 줄바꿈 정상
- 대조군: reduced-motion에서는 애니메이션 없음·가시 비율 100%이나 내부 viewport 자체는 여전히 694.8px라 구조적 폭 문제는 남는다
- 원인: `BaseButton`의 슬롯 래퍼가 `inline-flex` shrink-to-content로 695px까지 늘어나고, 카메라 페이지의 `w-full` 공지 콘텐츠가 이 과대 폭을 기준으로 계산된다. 슬롯 래퍼에 `min-w-0`/폭 제약이 없어 화살표도 함께 버튼 밖으로 밀린다
- 데이터 상태: 기존 방을 playing으로 전환해 라운드를 시작한 뒤 검증 종료 시 waiting으로 복원했다. 새 계정·방·공지는 생성하지 않았다
- 근거: `.qa-shots/camera-notice-width/` 및 `docs/qa/camera-cockpit-report.html`

### 수정 후 재검증

- C-05 완료: 접힌 공지를 정적 한 줄 말줄임으로 변경했다. 390×844에서 렌더 직후·2.5초·5초 모두 본문 가시 폭 `277.5/277.5px`, `transform: none`, `animation: none`을 유지했다.
- C-03 양성: 공지 행을 누르면 가용 폭 `277.5px` 안에서 공백 없는 100자 URL 전체가 여러 줄로 표시되고 화살표가 위로 회전했다.
- 대조군: reduced-motion에서도 일반 모션과 동일한 정적 말줄임이며 레이아웃 폭과 페이지 폭(390px)에 변화가 없었다.
- 실제 Firebase 방 `NF5U`의 기존 긴 공지를 재사용했으며 새 계정·방·공지는 생성하지 않았다. 검증을 위해 게임을 시작했다가 종료해 방을 waiting으로 되돌렸다.

### 조건부 애니메이션 최종 재검증

- 실제 overflow를 `scrollWidth > clientWidth`로 판정한다. 짧은 공지는 정적으로 표시하고 넘치는 공지만 초과 거리만큼 좌우로 애니메이션한다.
- 390×844 실제 Firebase 긴 공지에서 가용 폭 `277.5px`, 본문 `scrollWidth 694.5px`, 이동 거리 `417px`, `notice-marquee` 활성화를 확인했다.
- 애니메이션 시작부와 끝부분 모두 펼치기 아이콘은 오른쪽 끝에 고정되고 페이지 가로 폭은 390px를 유지했다.
- 펼침 후 `animation: none`, 가용 폭 `277.5px`, 전체 URL 줄바꿈을 확인했다. 짧은 공지 대조군은 새 백엔드 데이터를 만들지 않고 단위 테스트에서 `scrollWidth 100px < clientWidth 240px`로 검증했다.
- 읽기 속도 조정: 이동 속도를 약 `24px/s`로 낮추고 양 끝 10% 구간을 정지시켰다. `alternate`를 제거해 마지막 문구 도달 후 다음 반복은 첫 문구에서 다시 시작한다. 390px 실측에서 2.5초 약 60px, 5초 약 123px 이동했다.
- 펼친 상태의 내부 행은 `items-start`로 전환되어 왼쪽 `공지` 헤더와 오른쪽 펼침 아이콘이 여러 줄 본문의 상단에 고정된다.

## 2026-07-26 전체 전수 재검증

- 코드리뷰 3개 패스와 독립 검증에서 P1 공지 UX 결함 2건을 확정했다. 짧은 공지 클릭이 no-op이 아니며(C-06), 펼침 후 접기용 즉시 재개 클래스가 첫 사이클 뒤에도 유지된다(C-09).
- 정적 게이트를 모두 새로 실행했다. Vitest 48개 파일·500개 테스트, lint, type-check, production build, `git diff --check`가 통과했다.
- 실제 Firebase QA 방 `NF5U`에서 게임과 라운드를 새로 시작하고 짧은 공지 1건·긴 공지 1건을 전송했다. 종료 후 방은 waiting으로 복원했으며 새 계정·방은 만들지 않았다.
- C-06 실패: 390×844에서 짧은 공지 `곧 시작합니다.`는 overflow 0px·animation none이었지만 클릭 후 `aria-expanded=false→true`와 화살표 펼침 상태가 발생했다.
- C-07/C-08 통과: 긴 공지 초과 거리는 512px, duration 25.33초였다. 최초 0~10% 정지 뒤 이동 구간은 약 24px/s였고, 90~100% 끝 정지 후 101%에서 `translateX(0)`으로 새 반복을 시작했다.
- C-09 실패: 접은 직후 0.7초에 약 17.8px 이동해 즉시 재개는 충족했지만, 다음 반복에서도 `notice-marquee-resumed`가 유지되어 최초 정지 구간이 복원되지 않았다.
- C-09 수정: 첫 `animationiteration`에서 즉시 재개 상태를 해제해 이후 반복은 일반 marquee의 시작 정지를 사용하도록 변경했다. CameraPage 단위 테스트 17개·type-check·lint 통과, 브라우저 시간축 재검증은 대기한다.
- C-09 브라우저 재검증 완료: 접은 직후 125ms에 resumed marquee가 `-2.81px` 이동했고, 첫 반복 경계 후 일반 marquee로 전환된 367ms 지점에는 `translateX(0)` 시작 정지가 적용됐다. 콘솔 오류 없이 게임 종료 후 방은 waiting으로 복원됐다.
- C-03/C-05/C-10 및 G-01 통과: 펼치면 전체 본문이 4줄로 보이고 헤더·아이콘 상단 정렬, 접힘 아이콘 오른쪽 끝 고정, reduced-motion 정적 표시, 320/390/430px 페이지 가로 넘침 없음이 확인됐다.
- H-03 자동화 통과: 카메라↔대기실 진입/이탈 10회 후 콘솔·page error 없이 HUD가 갱신됐다. 실제 Listen 수를 서버 측에서 계수한 것은 아니므로 누수 부재 판정은 브라우저 관찰 범위다.
- E-04/F-02 통과: fake camera 비디오 메타데이터가 0인 환경에서 촬영 실패 role=alert가 표시됐고, 게임 종료 뒤 참가자가 `/waiting-room/NF5U`로 복귀했다.
- P0 I-01은 부분 통과다. 호스트 1명+참가자 1명 체인은 재실행했으나 실카메라 촬영 성공, 참가자 2명 이상, iOS/Android 실기기 잠금 복귀는 현재 자동화 환경에서 검증하지 못했다.
- 현재 증거: `.qa-shots/camera-cockpit-rerun-2026-07-26/`, 원시 계측 `result.json`·`capture-result.json`, 인라인 이미지 보고서 `docs/qa/camera-cockpit-report.html`.

## 2026-07-26 머지 게이트 판정

- C-09는 수정 후 브라우저 재검증까지 통과했다. 그 외 승인 여부는 사용자 결정에 따른다.
- 수정 후에는 46개 TC 전체를 다시 새 실행으로 판정하고, 특히 짧은 공지 true no-op과 접은 뒤 두 번째 반복의 시작 정지를 재검증해야 한다.
