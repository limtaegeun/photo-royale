# QA Test Cases — 대기실 (waiting-room)

- **브랜치**: `feat/p02-waiting-room` (main 대비, 미커밋 작업 포함)
- **작성일**: 2026-07-18
- **갱신**: 2026-08-09 `feat/p06-record-review` (main 대비 미커밋 반영분) — 라운드 재실행 세션 가드(다이얼로그·`roundPlayMarker`)·`지난 라운드 기록 보기` 진입점 추가에 따라 I·O·P 영역에 신규 TC 반영 (I-06~I-16, O-05~O-06, P-05)
- **컨텍스트**: 오프라인 이벤트(야외 실기기 진행)용 — **이벤트 당일 장애 허용 불가**. 기능 정상 동작·환경 강인성·동시성 검증을 최우선으로 한다.
- **후속 플로우**: 이 문서의 TC를 기반으로 `live-qa` 스킬로 실기기(브라우저) 검증 진행

## 작업 범위 요약 (TC 도출 근거)

| # | 작업 | 주요 파일 |
|---|---|---|
| 1 | Firebase 초기화 shared 승격 (auth → shared/api) | `src/shared/api/firebase.ts` |
| 2 | Firestore 보안 규칙 — rooms/participants | `firestore.rules` |
| 3 | P01 진입 화면: 방 만들기·초대 코드 입장·공유 링크 자동 입장 | `src/features/entry/components/EntryPage.vue` |
| 4 | 인증 플로우 `?code=` 쿼리 보존 | `LoginPage.vue` / `SignupPage.vue` / `useAuthGuard.ts` |
| 5 | 내가 만든 방 목록 (이어하기 + 바텀 시트) | `src/features/waiting-room/components/MyRoomList.vue` |
| 6 | P02 대기실: 멱등 입장·호스트(진행자) 모델·실시간 명단·레디·초대 링크·playing 전이 | `WaitingRoomPage.vue` / `useWaitingRoomStore.ts` / `api/rooms.ts` |
| 7 | 참가자 칩 (팀 보더·성별 이름 색·레디 상태·접근성 라벨) | `components/PlayerChip.vue` |
| 8 | 프로필 성별 조회 | `src/features/auth/api/profile.ts` |
| 9 | 라우트 `/waiting-room/:roomCode` (requiresAuth) | `src/router/index.ts` |
| 10 | 공용: BaseBadge outline, 닫기 버튼 SVG, gender 토큰, 버튼 pointer 커서 | `src/shared/**` |
| 11 | 라운드 재실행 세션 가드 — 같은 차수로 `게임 시작` 재클릭 시 확인 다이얼로그 | `src/features/waiting-room/roundPlayMarker.ts` / `WaitingRoomPage.vue` |
| 12 | 대기실 → 라운드 운영 기록 탭 진입점(`지난 라운드 기록 보기`, 호스트·배정 후 전용) | `src/features/waiting-room/WaitingRoomPage.vue` |

## 코드에서 도출한 이벤트-크리티컬 리스크 (중점 검증 근거)

1. **구독 에러 핸들러 부재**: `subscribeToRoom`/`subscribeToParticipants`가 `onSnapshot`에 에러 콜백을 넘기지 않는다 → 세션 만료·권한 오류 시 화면 갱신이 **조용히 중단**될 수 있다 (O-01, M-03).
2. **Firestore 지연 보상(latency compensation)**: 오프라인 쓰기가 로컬 스냅샷에 먼저 반영된다 → 오프라인에서 레디가 "된 것처럼" 보이지만 서버·타 기기엔 미반영일 수 있다 (G-05, M-01).
3. **playing 전이 레이스**: rules상 participants create는 방 status와 무관하게 허용 → 시작 직후 늦게 입장한 게스트의 동선 확인 필요 (E-12, N-04).
4. **혼동 문자**: 코드 알파벳은 0/O·1/I/L 제외지만 입력 정규화는 대문자화뿐 → 사용자가 `0`을 치면 not-found로만 수렴 (A-11).
5. **비보안 컨텍스트 의존**: `crypto.getRandomValues`(방 생성)·`navigator.clipboard`(링크 복사)는 HTTPS 전제 (M-07).
6. **세션 로컬 재실행 가드의 기기·탭 범위 한계**: `roundPlayMarker`는 `sessionStorage` 기반이라 가드가 "이 탭"에만 유효하다 — 진행자가 새 탭이나 다른 기기에서 대기실을 열면 마커가 없어 같은 차수 재실행 확인 다이얼로그 없이 즉시 재시작된다. 서버(rules) 강제는 하지 않는 best-effort UX 가드다(O-05·O-06, 알려진 한계 #8).

## 테스트 환경·사전 조건

- 로컬 dev: `npm run dev` (Firebase env `.env.local` 필요 — Auth/Firestore 연결). 이벤트 리허설(P군)은 실제 배포 환경에서 재수행
- **계정**: ① 호스트 A, ② 게스트 B(성별 male), ③ 게스트 C(성별 female), ④ 신규 가입용 미사용 이메일, ⑤ gender 없는 계정(E-09용) — 스케일 테스트(N군)는 게스트 계정 추가 확보
- **기기 매트릭스** (M군·P군 필수): iOS Safari 실기기 1대 + Android Chrome 실기기 1대 + 데스크톱 Chrome(호스트/관제용). 인앱 브라우저(카카오톡)로 공유 링크 수신 1회 이상
- 멀티 클라이언트: 브라우저 프로필/시크릿 창 병행 + 실기기 2대 이상
- Firestore 데이터 직접 조작(레거시 데이터·status 전이 시뮬레이션)은 Firebase 콘솔(admin 권한, **rules 미적용**) 사용
- 보안 규칙 TC(J군)는 클라이언트 SDK 콘솔 스크립트 또는 rules emulator로 검증
- 네트워크 시뮬레이션: DevTools 오프라인/Slow 3G 스로틀 + 실기기 비행기 모드 토글
- **라운드 재실행 가드 마커는 대기실 단독으로 재현 불가**: `markRoundPlayed`는 대기실이 아니라 **라운드 운영 화면에서 `라운드 시작`이 성공했을 때** 호출된다(`useRoundOpsStore`, round-ops 소관). I-08~I-11·O-05·O-06을 실기기로 검증하려면 대기실→라운드 운영→라운드 시작→대기실 복귀(라운드 종료)의 전체 체인이 필요하다 — 단위 테스트는 마커를 `markRoundPlayed()` 직접 호출로 주입해 이 의존을 끊는다
- 표기: **[unit]** = 단위 테스트가 이미 커버 → live-qa에서는 통합 관점 재확인만, 무표기 = live-qa 중점 대상

우선순위: **P0** 이벤트 당일 리허설(전 항목 통과 필수) / **P1** 핵심 플로우·강인성(막히면 이벤트 불가) / **P2** 주요 엣지·에러 경로 / **P3** 표시·접근성·회귀

---

## A. 진입 화면(P01) — 로그인 상태

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| A-01 [unit] | P1 | A 계정 로그인, `/` 진입 | 화면 확인 | 닉네임 인사(`{닉네임}님, 준비됐나요?`), 초대 코드 입력(placeholder `초대 코드를 입력해주세요`), `입장하기`(primary), `또는` 구분선, `새로운 방 만들기`(ghost) 렌더. 로그인/계정 만들기 버튼 없음 |
| A-02 [unit] | P1 | A-01 | `새로운 방 만들기` 클릭 | Firestore `rooms/{code}` 생성(hostUid=A, status=waiting, createdAt 서버시각). `/waiting-room/{code}`로 push 이동. 코드는 4자, `0/O/1/I/L` 미포함 대문자+숫자 |
| A-03 | P2 | A-01, 네트워크 오프라인 | `새로운 방 만들기` 클릭 | `방을 만들지 못했어요. 잠시 후 다시 시도해 주세요.` 표시, 이동 없음. 온라인 복구 후 재시도 성공 |
| A-04 | P2 | A-01 | `새로운 방 만들기` 빠르게 2회 클릭 | 방 1개만 생성 — 누른 CTA는 라벨 유지 + 스피너(`data-loading`), 반대 CTA(`입장하기`)는 disabled (2026-07-25 `pendingAction` 전환 반영, `button-loading.md` C-01/C-08 참조) |
| A-05 [unit] | P1 | 존재하는 방 코드 `AB2C` | 입력란에 ` ab2c `(소문자+공백) 입력 → `입장하기` | 정규화(`AB2C`)되어 방 존재 확인 후 `/waiting-room/AB2C` 이동 |
| A-06 [unit] | P2 | A-01 | 코드 미입력 상태로 `입장하기` 클릭 | 버튼은 활성 상태 유지, `초대 코드를 입력해 주세요.` 안내(role=alert). 이동·요청 없음 |
| A-07 [unit] | P1 | 존재하지 않는 코드 | `ZZZZ` 입력 → `입장하기` | `해당 초대 코드의 방을 찾을 수 없어요.` 표시, 이동 없음 |
| A-08 | P2 | 네트워크 오프라인 | 코드 입력 → `입장하기` | `입장에 실패했어요. 잠시 후 다시 시도해 주세요.` 표시 |
| A-09 | P3 | A-01 | 입력란에 5자 이상 타이핑 | maxlength=4로 4자 제한. 소문자 입력이 대문자로 표시(uppercase 스타일), 가운데 정렬·자간 확대 |
| A-10 | P3 | A-07 수행 직후 | 올바른 코드로 재제출 | 이전 에러 문구가 사라지고 정상 입장 |
| A-11 | P2 | 코드에 `O`가 없는 방(예: `AB2C`) | 사용자가 `0`(숫자)으로 잘못 입력 → `입장하기` | not-found 안내로 수렴, 화면 오류 없음. **혼동 문자 자동 치환은 없음** — 알려진 한계 #4로 기록, 이벤트 안내 시 "링크 공유 우선" 가이드 |
| A-12 | P2 | A 로그인 | 존재하지 않는 코드로 `/?code=QQQQ` 접속 | 자동 리다이렉트가 대기실 not-found 화면(`방을 찾을 수 없어요` + `입장 화면으로`)으로 안전하게 수렴, 무한 리다이렉트·백지 화면 없음 |

## B. 진입 화면 — 비로그인

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| B-01 [unit] | P1 | 로그아웃, `/` 진입 | 화면 확인 | `로그인`(primary)·`계정 만들기`(ghost) CTA만. 인사·코드 입력·방 만들기·내가 만든 방 섹션 없음 |
| B-02 [unit] | P2 | B-01 | 각 CTA 클릭 | `/login`, `/signup`으로 push 이동 (쿼리 없음) |

## C. 공유 링크(`/?code=`) 플로우

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| C-01 [unit] | P1 | A 로그인, 방 `AB2C` 존재 | `/?code=ab2c` 접속 | 진입 화면 스킵, 즉시 `/waiting-room/AB2C`로 **replace** 이동. 뒤로가기 시 entry로 무한 루프 없음 |
| C-02 [unit] | P1 | 로그아웃 | `/?code=AB2C` 접속 → `로그인` → B 계정 로그인 | 로그인 CTA가 `?code=AB2C`를 들고 감 → 로그인 성공 → `entry?code=AB2C` replace → 자동으로 대기실 입장 |
| C-03 [unit] | P1 | 로그아웃 | `/?code=AB2C` → `계정 만들기` → 신규 가입 완료 | 가입 성공 후 자동으로 대기실 `AB2C` 입장 |
| C-04 [unit] | P2 | C-02 진행 중 | 로그인 ↔ 회원가입 화면 상호 이동 | 양방향 이동에도 `?code=` 유지 |
| C-05 [unit] | P2 | A 로그인 상태 | `/login?code=AB2C` 직접 접속 | guestOnly 가드가 `entry?code=AB2C`로 보내고, 이어서 자동 대기실 입장 |
| C-06 | P3 | A 로그인 | `/?code=`(빈 값) 접속 | 자동 입장 없음, 일반 진입 화면 노출 |

## D. 내가 만든 방 목록 (MyRoomList)

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| D-01 | P2 | 만든 방 0개 계정 로그인 | `/` 확인 | `내가 만든 방` 섹션 자체가 렌더되지 않음 |
| D-02 | P2 | 만든 방 1개 | `/` 확인 | `ROOM {code} 대기실로 돌아가기` 버튼만. `지난 방 모두 보기` 없음 |
| D-03 | P1 | 만든 방 2개 이상 | `/` 확인 → `지난 방 모두 보기 (N)` 클릭 | 가장 최근 방이 돌아가기로 승격. 바텀 시트에 전체 목록 최신순(`ROOM {code} · {M월 D일 HH:MM}`) |
| D-04 | P3 | 방 생성 직후 뒤로가기로 `/` 복귀 | 목록 확인 | 방금 만든 방이 목록 맨 앞. createdAt 미반영(서버 타임스탬프 지연) 시 `방금` 표기 |
| D-05 | P1 | D-02/D-03 | 돌아가기 또는 시트 항목 탭 | 해당 방 대기실로 이동, 호스트 화면(팀 배정 시작 CTA) 정상 |
| D-06 | P2 | 목록 조회 실패 상황(오프라인 진입 등) | `/` 확인 | 섹션이 조용히 숨겨지고 화면·다른 CTA는 정상 동작 |
| D-07 | P2 | status=playing인 지난 방 존재 | 돌아가기/시트에서 해당 방 탭 | 대기실 진입 즉시 `playing` 스냅샷으로 전환됨 — 내가 만든 방이므로 호스트 경로인 `/round-ops/{code}`로 이동. **현 스펙 동작 확인** (이미 끝난 방 목록 노출은 개선 후보로 기록) |

## E. 대기실 입장·화면 상태

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| E-01 [unit] | P1 | B(게스트) 로그인, 방 `AB2C` | 코드 입장 | `participants/{B.uid}` 생성(nickname=displayName, gender=male, isReady=false, joinedAt). 룸 카드(`ROOM AB2C`, `대기 중` outline 배지, `참가자 N명 · 준비 N명`), 초대 링크 복사 버튼, 명단, 안전 수칙 카드+동의 안내문, CTA `확인하고 준비 완료` |
| E-02 | P1 | E-01에서 레디 완료 상태 | 새로고침(및 재방문) | 재입장 멱등: 참가자 문서 중복 생성 없음, **레디 상태 보존**(`준비 완료` disabled), joinedAt 유지로 명단 순서 불변 |
| E-03 [unit] | P1 | A(호스트) 로그인 | 자기 방 입장 | 참가자로 등록되지 않음(진행자 모델), 명단에 본인 없음, CTA는 `팀 배정 시작`(accent)만, 게스트용 동의 안내문·준비 CTA 없음 |
| E-04 [unit] | P3 | 느린 네트워크(스로틀) | 대기실 진입 직후 | `대기실에 입장하는 중…`(role=status) 로딩 표시 후 joined 화면 전환 |
| E-05 [unit] | P1 | 로그인 상태 | `/waiting-room/ZZZZ`(없는 코드) 직접 접속 | `방을 찾을 수 없어요` + 안내문 + `입장 화면으로` 버튼 → 클릭 시 entry로 replace |
| E-06 [unit] | P2 | 오프라인 | 대기실 진입 | `입장에 실패했어요` + 네트워크 안내 + `입장 화면으로` 버튼 |
| E-07 [unit] | P2 | 방 `AB2C` 존재 | `/waiting-room/ab2c`(소문자 URL) 접속 | 정규화되어 같은 방 입장, 화면에 `ROOM AB2C` 표기 |
| E-08 | P2 | 로그아웃 | `/waiting-room/AB2C` 직접 접속 | requiresAuth 가드로 `/login` 이동. **알려진 한계**: 이 경로는 code 쿼리가 보존되지 않아 로그인 후 entry로 감(자동 재입장 안 됨) — 현 스펙 확인, 개선 후보로 기록 |
| E-09 [unit] | P2 | `users/{uid}` gender 없는 계정(또는 조회 실패) | 게스트 입장 | 입장이 막히지 않고 진행. 참가자 문서에 gender 필드 자체가 없음, 명단 이름은 기본 텍스트 색 |
| E-10 [unit] | P2 | 게스트가 대기실에 있는 중 | 콘솔에서 방 문서 삭제 | 화면이 `방을 찾을 수 없어요`로 수렴 |
| E-11 | P2 | 느린 네트워크 | 입장 처리 중(`입장하는 중…` 로딩) 새로고침 / 뒤로가기 | 콘솔 에러·중복 참가 문서·좀비 구독 없이 재입장 또는 entry 복귀. 재입장 시 정상 joined |
| E-12 | P1 | 방 `AB2C`가 이미 status=playing | 신규 게스트 B가 코드/링크로 입장 | **라운드가 이미 시작된 방**이면 입장 직후 `/camera/{코드}`로 전환. **아직 라운드 시작 전**이면 대기실에 머문다(방치가 아니라 의도된 대기 — 호스트가 `라운드 시작`을 누르면 전이). rules상 참가 문서 생성 허용, 화면 멈춤·에러 없음 |
| E-13 | P2 | 방 2개(`AB2C`, `XY3Z`) 존재 | B가 `AB2C` 입장 → 뒤로가기 → `XY3Z` 입장 | 이전 방 구독 완전 해제(leave), 명단·카운트에 이전 방 데이터 섞임 없음, `ROOM XY3Z` 정확 표기 |

## F. 실시간 명단·참가자 칩

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| F-01 | P1 | A(호스트)·B·C 3클라이언트 | B, C 순차 입장 | 호스트·기존 참가자 화면에 새 참가자가 **실시간** 추가(입장 순 정렬), `N명 입장`·`참가자 N명` 카운트 동기 갱신 |
| F-02 | P1 | F-01 | B가 레디 → C 화면 관찰 | C·호스트 화면에서 B 칩이 대기(warning)→준비(success)로 실시간 전환, `준비 N명`·`N명 레디` 갱신 |
| F-03 [unit] | P2 | 게스트 0명 방 | 호스트 입장 | 명단 대신 `아직 입장한 참가자가 없어요. 초대 링크를 공유해 보세요.` 안내 |
| F-04 [unit] | P2 | 콘솔에서 `participants/{호스트uid}` 문서 수동 생성(레거시 데이터 재현) | 대기실 화면 확인 | 호스트 문서는 명단·카운트에서 제외됨 |
| F-05 [unit] | P2 | B(male)·C(female) 입장 | 칩 확인 | 이름 색: 남 파랑(`text-gender-male`)·여 빨강(`text-gender-female`)·성별 없음 기본색. 팀 미배정이라 중립 보더. 레디 dot·라벨(준비/대기) 병기 |
| F-06 [unit] | P3 | F-05 | 접근성 트리/aria-label 확인 | `{이름} · {남성/여성} · {팀명/팀 미배정} · {준비/대기}` 형식 — 색약 대응 라벨 병기 |
| F-07 | P3 | 12자 닉네임 계정 입장 | 칩 확인 | 긴 이름 truncate, 칩 레이아웃 안 깨짐 |
| F-08 | P3 | 참가자 7명+ | 명단 확인 | 3열 그리드 유지, 명단이 넘칠 때만 세로 스크롤 발생(콘텐츠 적을 땐 스크롤 없음) |
| F-09 | P2 | B 계정을 두 기기에서 동시 로그인 | 두 기기 모두 같은 방 입장 → 한 기기에서 레디 | 참가자 문서는 1개(멱등), 양 기기 상태 실시간 동기(둘 다 `준비 완료` disabled), 명단 중복 없음 |
| F-10 | P2 | 게스트 20명(이벤트 정원 규모) 입장 상태 | 호스트·게스트 화면 확인 | 명단 전원 렌더·스크롤 정상, 카운트 정확(`참가자 20명`), 렌더 지연·프레임 드랍 없이 조작 가능 |

## G. 준비 완료(레디)

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| G-01 [unit] | P1 | B 게스트 입장 | `확인하고 준비 완료` 클릭 | 참가자 문서 isReady=true. 버튼이 `준비 완료`로 바뀌며 disabled. 카운트·타 클라이언트 실시간 반영 |
| G-02 [unit] | P2 | 오프라인 | 레디 클릭 | `준비 완료 처리에 실패했어요. 다시 시도해 주세요.`(role=alert) → 온라인 복구 후 재시도 성공 시 에러 사라짐 |
| G-03 [unit] | P2 | B 게스트 | CTA 빠르게 연타 | setReady 요청 1회만 (isConfirmingReady 가드) |
| G-04 | P3 | 레디 완료 상태 | 해제 시도(UI 탐색) | 해제 수단이 없음(스펙). Firestore 직접 false 업데이트는 rules가 거부(J-05와 연계) |
| G-05 | P1 | B 대기실 접속 중 | 비행기 모드 ON → 레디 클릭 → 30초 후 비행기 모드 OFF | **지연 보상 주의**: 오프라인 중 내 화면에 준비가 먼저 보이거나 버튼이 대기 상태로 남을 수 있음 — 어느 쪽이든 ① 앱이 멈추지 않고 ② 재연결 후 서버·타 클라이언트(호스트 화면)에 레디가 최종 반영되거나 명확한 재시도 안내가 남아야 함. **타 클라이언트 기준으로 최종 판정** |

## H. 초대 링크 복사

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| H-01 [unit] | P1 | 대기실 입장(호스트/게스트 공통) | `초대 링크 복사` 클릭 | 클립보드에 `{origin}/?code={CODE}` 복사, `초대 링크를 복사했어요.` success 토스트. 복사된 링크로 C-01/C-02 흐름 연결 확인 |
| H-02 | P2 | 클립보드 권한 차단(브라우저 설정) | 복사 클릭 | `복사에 실패했어요. 주소를 직접 공유해 주세요.` danger 토스트, 화면 정상 유지 |

## I. 게임 시작 전이·이탈

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| I-01 [unit] | P1 | A·B·C 대기실 접속 중 | 콘솔에서 방 status를 `playing`으로 변경 | **호스트(진행자)만** `/round-ops/{code}`로 이동한다. 게스트는 대기실에 머물며, 호스트가 `라운드 시작`을 눌러 **남은 시간이 있는** `round`가 생겨야 `/camera/{code}`로 전이한다(진행자에게 카메라 권한 프롬프트가 뜨면 실패). 타이머가 0인 `round`는 전이시키지 않는다 — I-17 참고 |
| I-02 [unit] | P2 | 대기실 접속 중 | 뒤로가기로 entry 복귀 | 구독 해제(leave) — 이후 명단 변경이 콘솔 에러 없이 무시됨. entry 화면 정상 |
| I-03 | P1 | 호스트 대기실, 배정 확정 전(차수 0) | 하단 CTA 확인 | `팀 배정 시작`만 노출 — `게임 시작` 버튼 없음(배정 없이 게임을 시작할 수 없다) |
| I-04 [unit] | P1 | 호스트, 배정 1차 확정 완료 | `게임 시작` 클릭 | 방 status가 playing으로 전이되고 **호스트만** `/round-ops/{code}`(라운드 운영)로 이동한다. 게스트는 배정 카드를 유지하다가 호스트의 `라운드 시작` 이후 `/camera/{code}`로 이동. 실패 시 role=alert `게임을 시작하지 못했어요. 다시 시도해 주세요.`로 재시도 가능. (전제: 이 차수를 플레이한 세션 마커가 없는 최초 시작 — 마커가 있으면 재실행 확인 다이얼로그로 먼저 분기한다, I-08 참고) |
| I-05 [unit] | P1 | 대기실 입장 로딩 중 | 즉시 뒤로가기 → 다른 방 입장 | 이전 방 구독이 뒤늦게 생기지 않고(입장 세대 가드) 새 방 명단만 표시. 콘솔 에러 0 |
| I-06 [unit] | P2 | 없음 | `markRoundPlayed('AB2C', 1)` 호출 후 `hasPlayedRound` 조회 | 같은 방코드+차수(`AB2C`,1)는 true. 다른 방코드(`ZZ99`,1) 또는 같은 방의 다른 차수(`AB2C`,2)는 false — 마커는 방+차수 조합 단위로 완전히 격리된다 |
| I-07 [unit] | P2 | `Storage.prototype.setItem`이 throw하도록 모킹(Safari 프라이빗 모드 재현) | `markRoundPlayed('AB2C', 1)` 호출 → 이후 `hasPlayedRound('AB2C', 1)` 조회 | `markRoundPlayed`가 예외를 던지지 않고 조용히 무시(호출부 크래시 없음), `hasPlayedRound`는 false로 수렴 — 가드가 없을 뿐 실행은 막지 않는다(best-effort) |
| I-08 [unit] | P1 | 호스트, 배정 확정(차수1), 이 차수를 이미 플레이한 세션 마커 존재(`markRoundPlayed('AB2C',1)` — 실제로는 라운드 운영에서 `라운드 시작` 성공 시 자동 기록, round-ops 소관) | `게임 시작` 클릭 | `startPlaying()`이 즉시 호출되지 않는다. 확인 다이얼로그 노출 — title `이번 차수는 이미 진행했어요`, description `같은 차수로 다시 시작하면 지난 라운드의 판정하지 않은 킬샷이 판정 큐에 다시 나타나고, 새 기록이 같은 라운드로 합쳐져요.` |
| I-09 [unit] | P1 | I-08 상태에서 다이얼로그 열림 | `그대로 다시 시작`(ghost, md) 클릭 | 다이얼로그가 닫히고 `startPlaying()`이 호출된다(같은 차수 `AB2C`로 재시작) |
| I-10 [unit] | P1 | I-08 상태에서 다이얼로그 열림 | `다음 팀 배정하기`(primary, lg) 클릭 | 다이얼로그가 닫히고 배정 보드(`팀 편성`)가 열린다. `startPlaying()`은 호출되지 않는다 |
| I-11 [unit] | P1 | 호스트, 배정 확정(차수1), 이 차수를 플레이한 마커 없음(최초 시작, 또는 오시작 복구처럼 라운드가 실제로 시작되지 않은 경우) | `게임 시작` 클릭 | 다이얼로그 없이 기존과 동일하게 `startPlaying()`이 즉시 호출된다 |
| I-12 | P2 | I-08 상태에서 다이얼로그 열림 | 두 CTA가 아닌 다른 경로(우상단 × 닫기, Esc, 배경 클릭)로 다이얼로그를 닫음 | `startPlaying()`도 배정 보드 열기도 발생하지 않고 대기실에 그대로 머문다. `게임 시작`을 다시 누르면 다이얼로그가 다시 뜬다(상태 소실 없음) |
| I-13 [unit] | P2 | 호스트, 배정 확정 전(차수 0) | 화면 확인 | `지난 라운드 기록 보기` 버튼이 렌더되지 않는다 |
| I-14 [unit] | P1 | 호스트, 배정 확정 후(차수>0) | `지난 라운드 기록 보기`(ghost, md) 확인 후 클릭 | 버튼 노출. 클릭 시 `{ name: 'round-ops', params: { roomCode: 'AB2C' }, query: { tab: 'log' } }`로 push 이동 |
| I-15 | P2 | 게스트, 배정 확정 후(차수>0) | 화면 확인 | `지난 라운드 기록 보기` 버튼이 보이지 않는다(호스트 전용 CTA 영역이라 게스트 화면 자체에 렌더되지 않음) |
| I-16 | P2 | 직전 라운드에 미판정 킬샷이 남은 상태에서 라운드 종료 → 대기실 복귀 → I-09처럼 `그대로 다시 시작` 선택 | 라운드 운영 판정 탭에서 큐 확인 (교차 화면) | 직전 라운드의 미판정 킬샷이 판정 큐에 다시 나타난다 — **의도된 우회의 대가, 버그 아님**(알려진 한계 #8). 판정 큐 표시 로직 자체의 세부 TC는 `docs/qa/round-ops.md`/`photo-submission-judging.md` 소관, 여기서는 재등장 사실만 확인 |
| I-17 [unit] | P1 ✅live | 방 `status='playing'`, `round`는 남아 있으나 타이머가 0(호스트가 아직 게임 종료를 누르지 않음) | 게스트가 콕핏의 `대기실로 돌아가기`로 대기실 진입 | 콕핏으로 되밀리지 않고 대기실에 머문다. `라운드 종료` 배지와 `진행자가 게임을 마칠 때까지 기다려 주세요. 다음 라운드가 시작되면 촬영 화면이 자동으로 열려요.`가 뜨고 배정 카드(완장·팀원)는 그대로 남는다 (A-4, `docs/qa/camera-cockpit.md` M-02) |
| I-18 [unit] | P1 ✅live | I-17 상태 | 진행자가 시간 추가(`round.durationMs` 증가) | 남은 시간이 다시 생겨 게스트가 `/camera/{code}`로 자동 재진입한다(수동 조작 없이 방 스냅샷만으로 복귀) |
| I-19 [unit] | P1 ✅live | 방 `status='playing'`, 라운드가 일시정지(paused, 남은 시간 보존) | 게스트 대기실 진입 동선 확인 | 정지는 남은 시간이 보존되므로 `/camera/{code}`로 전이한다 — 정지를 이유로 대기실에 머물지 않는다 |
| I-20 [unit] | P1 ✅live | 호스트가 방 `status='playing'`·타이머 0인 상태에서 대기실 URL 직접 진입 | 화면 확인 | `라운드 종료` 안내 배너가 **호스트에게는 뜨지 않는다**(게스트 전용). 호스트는 그대로 `/round-ops/{code}`로 replace 이동한다 |
| I-21 | P1 ✅live | 방 `status='playing'`, 라운드 진행 중(남은 시간 있음) | 게스트가 대기실 URL 직접 진입 | 즉시 `/camera/{code}`로 전이한다 — 대기실에 머무는 창이 생기지 않는다(복귀 판정이 방 스냅샷 기준이라, 스냅샷 도착 전 잠깐 대기실이 보이더라도 도착 즉시 넘어간다) |
| I-22 [unit] | **P1** | 방 `status='playing'`이지만 호스트가 `라운드 시작`을 아직 누르지 않음(`round` 없음) | 게스트 대기실 화면 확인 | `라운드 종료` 배너가 **뜨지 않는다** — 배정 카드만 보인다. 진행자가 인원을 세고 공지하는 이 구간에 종료 안내가 뜨면 게스트는 게임이 끝난 줄 안다. 안내 조건은 `!isRoundLive`가 아니라 `isRoundOver`(라운드가 있는데 남은 시간이 0)다 |

## J. Firestore 보안 규칙 (SDK 스크립트/에뮬레이터 검증)

| ID | 우선 | 검증 대상 | 기대 결과 |
|---|---|---|---|
| J-01 | P1 | rooms create | 허용: 본인 hostUid + status=waiting + createdAt=서버시각 + 코드 `^[A-HJKMNP-Z2-9]{4}$` + 필드 3개만. 거부: 타인 hostUid, status≠waiting, 임의 createdAt, 잘못된 코드 형식(소문자·5자·`0` 포함), 추가 필드 |
| J-02 | P1 | rooms list | 허용: `where hostUid == 본인` 쿼리. 거부: 조건 없는 전체 목록, 타인 hostUid 쿼리 (전체 방 코드 수집 차단) |
| J-03 | P1 | rooms update | 허용: 호스트 본인의 status만 waiting→playing. 거부: 게스트의 전이, playing→waiting 역전이, status 외 필드 변경, hostUid 변경 |
| J-04 | P1 | participants create | 허용: 본인 uid 문서 + isReady=false + joinedAt=서버시각 + 닉네임 2~12자(한글·영문·숫자) + gender 생략 또는 male/female. 거부: 타인 uid 문서, isReady=true로 생성, 없는 방(roomCode)에 생성, 1자/13자/특수문자 닉네임, gender 임의값, 스키마 외 필드 |
| J-05 | P1 | participants update | 허용: 본인 문서 isReady→true만. 거부: 타인 문서 수정, isReady→false(해제), nickname/gender/team 변경 |
| J-06 | P2 | delete | rooms·participants 모두 delete 거부 (호스트 포함) |
| J-07 | P1 | 비로그인 접근 | rooms get/list, participants read 모두 거부 |

## K. 공용 컴포넌트·스타일 회귀

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| K-01 [unit] | P3 | 대기실 룸 카드 | `대기 중` 배지 확인 | BaseBadge outline appearance — 투명 배경 + info 색 텍스트·테두리 |
| K-02 | P3 | 바텀 시트(D-03)·토스트(H-01) 열기 | 닫기 버튼 확인 | × 아이콘이 SVG로 원형 배경 정중앙 정렬(베이스라인 어긋남 없음), 닫기 동작 정상 |
| K-03 | P3 | 데스크톱 | 각 버튼 hover | 활성 버튼 커서 pointer, disabled 버튼(레디 완료 등)은 pointer 아님 |
| K-04 | P3 | 루트에 `data-theme="light"` 강제 | 성별 이름 색 확인 | 라이트 토큰(male=blue-600, female=red-600)으로 전환, 대비 유지 |
| K-05 | P2 | 로그아웃 | code 쿼리 **없이** 로그인/가입/로그아웃 기본 플로우 일주 | 회귀 없음: 성공 시 entry로 replace(뒤로가기로 인증 화면 복귀 안 됨), SignupPage 하단 버튼 텍스트가 `로그인`으로 표기 |

## L. 접근성·모바일

| ID | 우선 | 절차 | 기대 결과 |
|---|---|---|---|
| L-01 | P3 | 모바일 뷰포트에서 주요 CTA(입장하기·방 만들기·레디·링크 복사) 터치 영역 측정 | 최소 터치 타겟 48px 이상 |
| L-02 | P3 | 대기실 콘텐츠 적은 상태에서 스크롤 확인 | 불필요한 세로 스크롤 없음(헤더 포함 dvh 초과 없음) |
| L-03 | P3 | 스크린리더/접근성 트리에서 에러·로딩 확인 | 에러 문구 role=alert, 입장 로딩 role=status로 노출 |

## M. 네트워크·기기 강인성 (실외 이벤트 환경 — 신규 강화)

야외 LTE/5G 약전계, 화면 잠금, 장시간 대기가 기본 조건이다. **모두 실기기로 검증한다.**

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| M-01 | P1 | B 실기기 대기실 접속 | 비행기 모드 30초 ON → OFF (간헐 연결 재현). 그 사이 C가 입장·레디 | 재연결 시 **수동 새로고침 없이** 명단·카운트가 최신 상태로 자동 복구. 놓친 입장·레디가 모두 반영. 콘솔 에러·백지 없음 |
| M-02 | P1 | B 실기기 대기실 접속 | 화면 잠금/홈 이동으로 5분+ 백그라운드 → 복귀. 백그라운드 중 호스트가 게임 시작 + **라운드 시작**까지 진행 | 복귀 즉시 구독이 살아나거나 재수립되어 **라운드 시작 전이를 놓치지 않고** `/camera/{code}`로 이동. 대기실에 갇힘 없음(round-ops.md R-13) |
| M-03 | P1 | B 대기실에서 60분+ 체류 (이벤트 집결 대기 재현) | 체류 후 레디 클릭, 명단 갱신 관찰 | Auth ID 토큰 자동 갱신으로 구독·쓰기 모두 지속 동작. 1시간 경과 후에도 레디 성공·실시간 반영 (리스크 #1 구독 중단 여부 중점 관찰) |
| M-04 | P1 | iOS Safari 실기기 + Android Chrome 실기기 | 각 기기에서 P-01 풀 시나리오 수행 | 두 플랫폼 모두 전 구간 통과. 플랫폼 고유 이슈(사파리 클립보드 제스처 요구, 뷰포트/키보드 겹침) 없음 |
| M-05 | P2 | 카카오톡으로 초대 링크 전송 | 인앱 브라우저에서 링크 열기 → 로그인 → 자동 입장 | 인앱 브라우저에서 세션 생성·유지, 자동 입장까지 정상. 실패 시 외부 브라우저 열기 안내를 이벤트 운영 가이드에 반영 |
| M-06 | P2 | 시크릿 모드 | 로그인 → 대기실 입장 → 새로고침 | Auth persistence로 세션 유지, 재입장 멱등 정상 |
| M-07 | P2 | HTTP(비보안 컨텍스트, 예: LAN IP 접속) | 방 만들기·링크 복사 시도 | `crypto.getRandomValues`(방 생성)·`navigator.clipboard`(복사)의 비보안 컨텍스트 동작 확인 — 복사는 danger 토스트로 우아한 실패. **이벤트 본번은 HTTPS 필수**임을 운영 전제로 문서화 |
| M-08 | P3 | 저사양/배터리 세이버 모드 실기기 | 명단 갱신·전이 관찰 | 실시간 반영 지연이 있어도 수 초 내 수렴, 조작 불능 없음 |

## N. 동시성·스케일 (이벤트 정원 기준 — 신규 강화)

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| N-01 | P1 | 게스트 계정 20개(이벤트 정원) | 동시(수 초 내) 입장 러시 | 전원 참가 문서 생성 누락 없음, 모든 클라이언트 명단 20명 정합, joinedAt 순 정렬 안정, 카운트 일치 |
| N-02 | P1 | N-01 상태 | 전원 거의 동시에 레디 | `준비 20명`으로 최종 수렴 — 레디 유실·카운트 불일치 없음 (호스트 화면 기준) |
| N-03 | P2 | 호스트 계정 여러 개 | 방 10개+ 연속/동시 생성 | 코드 중복 없음(트랜잭션 존재 검사), 모든 방 정상 입장 가능 |
| N-04 | P1 | 호스트 시작 직전 | status playing 전이와 **동시에** 늦은 게스트 입장 (레이스 재현 수회 반복) | 늦은 게스트가 대기실에 방치되지 않음 — 어느 타이밍이든 camera로 합류하거나 명확한 상태 안내. 화이트스크린·무한 로딩 없음 (E-12 연계) |
| N-05 | P2 | 게스트 1명이 레디 클릭 직후 | 1초 내 호스트가 시작 전이 | 마지막 레디가 유실되지 않고 서버 반영된 채 전원 게임 화면 전환(게스트 camera / 호스트 round-ops) |

## O. 세션·수명주기 (신규 강화)

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| O-01 | P1 | B가 탭 2개: 대기실 + entry | entry 탭에서 로그아웃 | **리스크 #1 직결**: 대기실 탭이 조용히 죽는지 관찰 — 구독 권한 상실 시 화면 상태·콘솔 에러 기록. 최소 기준: 크래시·백지 없음, 조작 시 명확한 실패 피드백. 갱신 중단이 확인되면 개선 후보(구독 에러 핸들러)로 보고 |
| O-02 | P2 | B 게스트 | 레디 클릭 **직후**(1초 내) 새로고침 | 재입장 시 레디 상태가 서버 기준으로 정확히 복원 (`준비 완료` disabled 또는 미반영 시 재시도 가능 상태) — 어정쩡한 중간 상태 없음 |
| O-03 | P2 | B 게스트 | entry ↔ 대기실 진입/이탈 5회+ 반복 | 구독 누수 없음(leave 후 재enter), 반복할수록 콜백 중복 실행·카운트 오류·성능 저하 없음 |
| O-04 | P3 | 전체 플로우 임의 지점 | 브라우저 뒤로가기/앞으로가기 연타 | 히스토리 내비게이션 전 구간에서 화면 상태 일관, replace 구간(인증 성공·자동 입장·게임 전이: camera/round-ops)은 뒤로가기로 재진입 안 됨 |
| O-05 | P1 | 호스트 실기기: 라운드 운영에서 `라운드 시작` 성공(마커 기록) → 라운드 종료 → 대기실 복귀 | **같은 탭**에서 새로고침 → `게임 시작` 클릭 | 새로고침 후에도 `sessionStorage` 마커가 유지되어 재실행 확인 다이얼로그가 뜬다(리스크 #6 정상 동작 확인 — 페이지 재로드에도 가드가 살아있어야 함) |
| O-06 | P2 | O-05와 같은 방·차수, **새 탭** 또는 **다른 기기**에서 같은 호스트 계정으로 대기실 접속 | `게임 시작` 클릭 | 마커가 없어 다이얼로그 없이 즉시 재시작된다 — **알려진 한계로 확인**(버그 아님. `sessionStorage`는 탭 스코프, 서버 rules 미강제, 진행자 기기 1대 운영을 전제한 UX 가드, 알려진 한계 #8) |

## P. 이벤트 당일 종합 리허설 (P0 — 전 항목 통과 없이는 이벤트 불가)

개별 TC가 모두 통과해도 **연결된 한 판**이 통과해야 한다. 본번 배포 환경 + 실기기로 수행한다.

| ID | 시나리오 | 통과 기준 |
|---|---|---|
| P-01 | **풀 플로우 체인**: 호스트(데스크톱) 방 생성 → 초대 링크 복사 → 게스트1(iOS, 기존 계정 로그인 경유 자동 입장) + 게스트2(Android, **신규 가입** 경유 자동 입장) + 게스트3(코드 수동 입력 입장) → 전원 레디 → 시작(콘솔 전이) → 게스트 전원 camera + 호스트 라운드 운영 동시 전환 | 전 구간 오류 0건, 각 단계 3초 내 반응, 명단·카운트 상시 정합 |
| P-02 | **중도 이탈 복구**: 게스트가 레디 후 브라우저 강제 종료 → 공유 링크로 재접속 | 재입장 멱등 — 레디 상태 그대로, 명단 순서 유지, 중복 없음 |
| P-03 | **오입장 복구**: 오타 코드 not-found → `입장 화면으로` → 올바른 코드 재입장 → 레디 | 잔여 상태 오염 없이 정상 합류 |
| P-04 | **당일 스모크**(이벤트 시작 직전 수행용): 방 생성 → 게스트 1명 입장 → 레디 → 전이 4단계 1회전 | 10분 내 완료, 전 단계 정상 — 실패 시 이벤트 시작 보류 기준 |
| P-05 | **재편성 체인**: 라운드 종료 → 대기실 복귀 → `게임 시작` 재클릭(재실행 확인 다이얼로그 발동 확인, I-08) → `다음 팀 배정하기` → 보드에서 새 모드 선택·확정(차수 N+1) → 대기실 복귀(룸 카드에 `N+1차 라운드 · {새 모드}` 표기) → `게임 시작` → **가드 없이**(새 차수라 마커 없음) 정상 진행 → 라운드 운영 전환 | 전 구간 오류 0건, 각 전환 3초 내, 차수·모드 표기가 매 단계 실제 상태와 정확히 일치. 재실행 가드가 새 차수에서는 절대 발동하지 않음 |

---

## 커버리지 매트릭스 요약

| 영역 | TC 수 | P0/P1 | 단위 테스트 커버 | live-qa 중점 |
|---|---|---|---|---|
| A 진입(로그인) | 12 | 4 | 5 | 방 생성 중복·오프라인·혼동 문자·자동 입장 실패 수렴 |
| B 진입(비로그인) | 2 | 1 | 2 | 회귀만 |
| C 공유 링크 | 6 | 3 | 5 | E2E 전 구간 연결(링크 복사→가입→자동 입장) |
| D 내 방 목록 | 7 | 2 | 0 | **전 항목 live-qa 필수** (playing 방 재입장 포함) |
| E 대기실 입장 | 13 | 5 | 8 | 새로고침 멱등(E-02)·playing 방 입장(E-12)·방 전환(E-13) |
| F 실시간 명단 | 10 | 2 | 5 | **멀티 클라이언트 실시간(F-01·F-02)·동일 계정 2기기(F-09)·정원 렌더(F-10)** |
| G 레디 | 5 | 2 | 3 | **오프라인 레디 지연 보상(G-05)** |
| H 초대 링크 | 2 | 1 | 1 | 실 클립보드·권한 거부 |
| I 시작 전이 | 16 | 9 | 13 | 실 Firestore status 전이·**재실행 가드 전체 체인**(마커 기록은 round-ops 소관)·다이얼로그 다중 닫기 경로(I-12)·재등장 교차 확인(I-16) |
| J 보안 규칙 | 7 | 6 | 0 | **전 항목 필수** (rules 자동 테스트 없음) |
| K 공용 회귀 | 5 | 0 | 1 | 시각 확인 |
| L 접근성 | 3 | 0 | 0 | 시각·AT 확인 |
| M 네트워크·기기 강인성 | 8 | 4 | 0 | **전 항목 실기기 필수** — 간헐 연결·백그라운드·장시간·크로스 브라우저 |
| N 동시성·스케일 | 5 | 3 | 0 | **전 항목 필수** — 입장 러시·동시 레디·시작 레이스 |
| O 세션·수명주기 | 6 | 2 | 0 | **전 항목 필수** — 구독 중단 리스크(O-01)·재실행 가드 새로고침 유지·**새 탭 스코프 한계**(O-05·O-06) |
| P 당일 리허설 | 5 | 5 (P0) | 0 | **본번 환경 + 실기기, 이벤트 게이트** — 재편성 체인(P-05) 포함 |
| **합계** | **112** | **P0 5 + P1 44** | | |

## 이벤트 당일 운영 체크리스트 (QA 결과와 별개로 당일 필수)

1. 이벤트 시작 60분 전 **P-04 스모크** 1회전 — 실패 시 시작 보류
2. 본번 URL이 HTTPS인지 확인 (클립보드·crypto 전제, M-07)
3. 진행자(호스트) 기기 + 백업 기기 1대에 사전 로그인·방 코드 확보
4. 참가자 안내문에 "코드 직접 입력 시 숫자 0/알파벳 O 없음" 명시 (A-11) — 링크 공유를 기본 채널로
5. 인앱 브라우저 이슈 발견 시(M-05) "기본 브라우저로 열기" 안내 준비

## 알려진 한계 (QA 시 버그가 아닌 스펙)

1. **E-08**: 대기실 URL 직접 딥링크는 미인증 시 code가 보존되지 않아 로그인 후 자동 재입장이 안 된다 (공유 채널은 `/?code=` 단일이라 정상 경로에선 발생 안 함) — 개선 후보.
2. ~~**I-03**: 호스트 `팀 배정 시작`은 자리만 확정된 미연결 CTA~~ → 해소. 배정 확정 후 `게임 시작` CTA가 `startGame`에 연결됐다(I-04).
3. 레디 해제·방 삭제·퇴장(참가자 문서 삭제) 플로우는 이후 단계 스펙 — rules도 의도적으로 거부.
4. **A-11**: 혼동 문자(0↔O 등) 자동 치환 없음 — not-found 안내로만 수렴. 개선 후보(normalizeRoomCode에서 치환).
5. **O-01/리스크 #1**: 구독에 에러 콜백이 없어 세션 만료·권한 오류 시 갱신이 조용히 멈출 수 있음 — QA에서 실측 후 개선 후보로 승격 판단.
6. **D-07/E-12**: playing 방이 목록에 남고 재입장 시 즉시 게임 화면(게스트 camera / 호스트 round-ops)으로 전환 — 의도 여부 기획 확인 필요.
7. ~~**라운드 운영(`/round-ops/:roomCode`)은 이번 단계에서 화면 골격만** — 타이머·올스탑·시간 조정·공지·판정은 동작하지 않고 컨트롤은 준비 중 토스트만 띄운다.~~ → **해소**. 이후 브랜치에서 타이머·판정 큐·기록 탭이 순차 구현됐다(`docs/qa/round-ops.md`/`camera-cockpit.md`/`photo-submission-judging.md`/`record-review.md` 참고, 각각 별도 TC 문서). 대기실의 `지난 라운드 기록 보기`(I-14)는 이 중 **기록 탭**(`?tab=log`)으로 진입하는 진입점이다. 게스트가 URL을 직접 입력했을 때의 호스트 전용 가드 여부는 `round-ops.md` 소관.
8. **라운드 재실행 세션 가드는 탭·기기 스코프**: `roundPlayMarker`는 `sessionStorage` 저장이라 새 탭·다른 기기·시크릿 창에서는 가드가 사라져 같은 차수가 확인 없이 재실행될 수 있다(O-06). 서버(rules) 강제는 하지 않는다 — 진행자 기기 1대 운영을 전제한 UX 안내이며, 다중 기기 강제는 후속 과제.

---

## live-qa 검증 결과 (2026-07-18 최초 · 2026-07-19 증거 하드닝 재수행) — 96 TC 전수

> **2026-07-19 재수행**: 신뢰도 강화를 위해 전 TC를 새 환경·새 계정으로 처음부터 다시 수행하고, UI TC는 스크린샷을 캡처해 QA 담당(에이전트)이 직접 육안 확인(텍스트 단언 오탐 0), 규칙·스케일은 HTTP 응답을 증거로 남겼다. 이 과정에서 **F-08(3열 그리드)·E-09(성별 없는 참가자 기본색)**가 추론/프록시 → 직접 확인으로 승격. HTML 보고서(Artifact)에 TC별 완료 여부·방법·불가 사유를 정리했다. 재수행 생성 데이터는 사용자 확인 후 전량 삭제 완료.

- **환경**: local dev `http://localhost:5175` + 실제 Firebase 백엔드(`photo-royale-scc`). 신규 기능이라 main 대응 코드 없음 → **local 단독**.
- **방식**: CDP Chrome 3인스턴스(계정별 프로파일 분리) + Auth/Firestore **REST**(실토큰) + 배포된 `firestore.rules` 직접 호출. 회원가입·REST signUp로 계정 26개 생성 후 구동.
- **집계**: **완료 76 · 부분/프록시 13 · 불가(실기기) 7 = 96.** 기능 결함 **0건**.
- **HTML 보고서**(TC별 완료 여부·방법·불가 사유): `docs/qa/`가 아닌 Artifact로 게시 — 세부는 그 표를 단일 진실원으로 본다.
- **QA 생성 데이터**: 검증 종료 후 **전량 삭제 완료**(rooms 6·프로필 문서·Auth 계정 26 — Firestore admin CLI + Identity Toolkit `accounts:delete`).

### 신규 TC (2026-08-09 갱신 추가 — 2026-08-10 live-qa 1차 수행)

라운드 재실행 세션 가드·`지난 라운드 기록 보기` 진입점 도입으로 이번 갱신에서 추가된 **I-06~I-16·O-05·O-06·P-05**는 위 검증 회차(2026-07-18/19) 이후 추가된 TC라 아래 완료/부분/불가 집계에 포함되어 있지 않다. **2026-08-10 live-qa 회차**(local dev·실 Firebase·headless Chrome 2대, 방 8CWJ — 전체 체인: 게임 시작→라운드 시작→라운드 종료→대기실 복귀) 결과:

- **✅ 실측 통과 (실데이터)**: I-08(재실행 확인 다이얼로그 — title·description 전문 일치), I-10(`다음 팀 배정하기` → 배정 보드 열림·시작 미호출), I-11(마커 없는 최초 시작은 다이얼로그 없이 즉시 시작), I-14(`지난 라운드 기록 보기` 노출 + `?tab=log` 딥링크로 기록 탭 활성 진입)
- **⚠️ 부분**: O-05 — 새로고침 후 sessionStorage 마커 잔존(`"1"`)은 실측 확인, 새로고침 직후 `게임 시작` 재클릭으로 다이얼로그 재발동까지는 미수행
- **✅ unit 갈음**: I-06·I-07·I-09·I-13 (마커 격리·프라이빗 모드 예외·`그대로 다시 시작` 경로·차수 0 버튼 미렌더 — 단위 테스트 10건이 고정)
- **⏸ 미검증**: I-12(비CTA 닫기 경로), I-15(게스트 화면 버튼 부재 — 이번 런 게스트는 콕핏 체류), I-16(`그대로 다시 시작`의 미판정 재등장 — 의도된 우회라 실행하지 않음), O-06, P-05

### 완료 (76) — 실동작/실토큰 응답으로 확인

- **A(진입)** A-01~12 전부: 방생성(코드 4자·금지문자 없음)·정규화·not-found·오프라인 생성 실패(8s)·3연타 1방·maxlength4 대문자·`/?code=QQQQ` not-found 수렴 등
- **B** B-01·02 · **C(공유링크)** C-01~06 전부(로그아웃→로그인/가입 후 자동입장, guestOnly 가드, 빈 code)
- **D** D-01·02·03·05·07 · **E** E-01·02·03·04·05·07·08·10·12·13
- **F(실시간)** F-01~10 전부(3클라 실시간 명단·레디, 레거시 호스트 제외, 성별색, aria, 12자 truncate, 동일계정 2기기, 20명 렌더)
- **G** G-01·03·04 · **H** H-01·02 · **I** I-01·02·03
- **J(보안규칙)** J-01~07 전부 = **26 체크** 모두 스펙대로(호스트 전이만 허용, 타인문서·레디해제·삭제·미인증·전체목록 전부 403)
- **K** K-01~05 전부(outline 배지·시트 닫기·pointer·라이트 성별색·회귀) · **L** L-01·03
- **N(스케일)** N-01~05 전부(20 동시입장/레디 누락·유실 0, 코드충돌 409, 시작 레이스) · **O** O-01~04 전부

### 부분/프록시 (13) — 간접 커버·발견사항·환경 제약

| TC | 사유 |
|---|---|
| G-02 · G-05 | **발견**: 오프라인 레디는 즉시 에러가 아니라 Firestore 지연보상으로 낙관적 준비 표시 → 재연결 시 flush. TC의 "즉시 에러" 기대와 상이(앱 멈춤 없음) |
| O-01 관련 | 구독 `onSnapshot` **에러 콜백 부재**를 코드+런타임 확인. auth 상실 시 크래시는 없으나 조용한 갱신 중단 위험 → **에러 콜백 추가 권고** |
| E-06 | dev 서버 오프라인 풀로드 불가(`ERR_INTERNET_DISCONNECTED`)로 통합 재현 제한, 스토어 error 경로 [unit] 커버 |
| E-09 | gender 없는 참가자(REST) 입장 허용+기본색 렌더 확인, 앱 signup 변형은 [unit] |
| E-11 | O-02·O-03로 강건성 대체 확인 |
| D-04 · D-06 · L-02 | 상대시간 라벨·오프라인 목록 숨김·정밀 스크롤 측정은 코드/시각 근거로 대체 |
| M-06 | Auth persistence 새로고침 유지 확인, 정식 시크릿 모드 별도 미측정 |
| P-01~04 (P0) | 개별 조각 전부 통과, **본번 배포 환경+실기기 단일 연속 리허설은 배포 후 게이트** |

### 불가 (7) — 실기기/물리 환경 필요

M-01·02·03·04(iOS/Android 실기기 간헐연결·백그라운드·장시간·크로스브라우저) · M-05(카카오톡 인앱) · M-07(비보안 컨텍스트 HTTP) · M-08(저사양 실기기). 자동화 환경 밖 — 이벤트 리허설 시 실기기로 수행.

### 발견 사항 (스펙상 버그 아님 — 개선 후보)

1. **G-02/G-05 지연보상**: 오프라인 레디는 낙관적으로 준비 표시되고 재연결 시 서버 반영 — "즉시 실패 안내"를 기대하면 어긋난다. 안내 UX 재검토 여지.
2. **O-01 구독 에러 콜백 부재**(리스크 #1 실재): 세션·권한 오류 시 조용한 갱신 중단 위험 → `subscribeToRoom`/`subscribeToParticipants`에 에러 콜백 추가 권고.

---

## 2026-08-31 검증 결과 — 서비스 전체 full QA (배포본 `a46ab3f`)

환경: `https://photo-royale-scc.web.app`, 390×844 / 320×720. 계정 3개(호스트 1 · 게스트 2), 방 `RQAU`.
진입 → 가입 → 방 생성 → 공유 링크 입장 → 레디 → 배정 → 게임 → 판정 → 기록 → 종료까지 한 줄로 관통했다.

### 통과한 영역

- **A·B·C(진입)**: 비로그인 화면에 코드 입력·프로필 아이콘이 없고, `/?code=`가 로그인·회원가입을 거쳐도 보존된다.
  미인증 딥링크는 `/login?redirect=…`로 목적지를 보존해 보낸다.
- **A-05·A-09**: 소문자 `rqau` 입장 정규화, 실제 타이핑 4자 제한 + `text-transform: uppercase`.
- **A-06·A-07·A-11**: 빈 코드 / 없는 코드 / 혼동문자(`RQA0`) 안내 문구 모두 정확.
- **D**: 내 방 목록 바텀시트(`role=dialog`), ESC 닫힘.
- **E·F·G·H**: 대기실 렌더, 실시간 명단(호스트 제외), 레디 후 `준비 완료` disabled, 초대 링크 복사 토스트.
- **I**: 게임 시작 전이가 역할별로 갈린다(호스트 `/round-ops`, 게스트는 라운드 시작 전까지 대기실 유지).
- **L(모바일)**: 320px에서 가로 스크롤 없음, 44px 미만 터치 타겟 없음, 텍스트 넘침 없음.
- **team-assignment A**: 참가자 0명에 `팀 배정 시작` → `참가자가 없어요.` 토스트로 차단.
- **start-readiness 가드**: 배정 확정이 레디를 리셋하고 `게임 시작`이 disabled로 잠긴 뒤, 전원 재레디에서 풀린다.

### 리스크 #1이 실제로 재현됐고, 같은 회차에 닫았다 (P1 → 수정 완료)

이 문서 상단 "코드에서 도출한 이벤트-크리티컬 리스크" 1번(**구독 에러 핸들러 부재**)이 실측으로 확인됐다.

같은 계정의 다른 탭에서 로그아웃하자 대기실에서:

1. 15초를 기다려도 로그인 화면으로 가지 않는다
2. `확인하고 준비 완료`가 눌리는데 **아무 일도 일어나지 않는다** — 토스트도 없고 명단도 `0명 준비` 그대로
3. 콘솔에만 `Uncaught Error in snapshot listener: FirebaseError: [code=permission-denied]` 2건
   (방 구독·참가자 구독)
4. 새로고침해야 `/login?redirect=/waiting-room/RQAU`로 간다(라우터 가드 자체는 정상)

**같은 상황에서 다른 화면은 막아낸다** — 라운드 운영은 `myId === null` watch로 1초 내 이탈하고,
카메라 콕핏은 구독 `onError`로 토스트 후 진입 화면으로 보낸다. **대기실만 두 수단이 다 없다**
(`useWaitingRoomStore.ts:177·182`가 `onError`를 넘기지 않는다 — API는 세 번째 인자로 이미 받는다).

참가자는 준비했다고 믿고 진행자 화면엔 미준비로 남는다. 토큰 갱신 실패·세션 만료도 같은 상태를 만든다.

**수정**(`fix/waiting-room-session-loss`): 대기실에도 라운드 운영과 같은 `myId` watch를 넣어,
로그인이 사라지면 목적지를 보존해 로그인 화면으로 보낸다. 회귀 테스트 2건을 더했고, 그 과정에서
이 결함이 단위 테스트에 잡히지 않던 이유(spec의 auth 모킹이 반응형이 아니었다)도 함께 고쳤다.
상세는 `profile.md`의 S-05 항목.

### 🟡 회원가입 에러 문구에 role이 없다 (P3, 접근성)

진입 화면의 코드 오류는 `alert atomic live="assertive"`로 제대로 노출되는데,
회원가입 화면의 필드 오류는 `<p class="text-caption text-danger">`로만 렌더된다.
입력에 `aria-invalid="true"`는 붙지만 `aria-describedby`가 없어 **스크린리더가 어떤 오류인지 읽지 못한다**.
같은 서비스 안에서 두 화면의 처리가 다르다.
