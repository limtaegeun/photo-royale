# 라운드 운영(round-ops) QA Test Case

- **브랜치**: `feat/h04-round-control` (PR #16, HEAD `31d6dfd`)
- **작성일**: 2026-07-25 (5차 — 운영 실동작·게스트 전이·게임 종료·최신 UI 회귀)
- **크리티컬리티**: 오프라인 이벤트 당일 실시간 진행 — **장애 허용 불가**. 호스트 기기는 진행 지휘의 단일 지점이고, 이번 변경으로 **참가자 전원의 타이머 정본이 이 화면의 쓰기에 달렸다**. 1차(스켈레톤)에서는 컨트롤이 아무것도 쓰지 않아 오작동해도 게임에 영향이 없었지만, 이제는 잘못된 쓰기 하나가 전원의 남은 시간을 바꾼다.
- **후속 플로우**: 이 문서를 입력으로 `live-qa` 수행 → 결과를 같은 문서에 기록
- **최종 결과**: **141 TC 중 완료 131 / 부분 2 / 환경상 불가 8 / 실패 0**, P0 4/4 통과. 공유 보고서: [round-ops-report.html](./round-ops-report.html)
- **선행 문서**: [waiting-room.md](./waiting-room.md)(게임 시작 전이 I군), [team-assignment.md](./team-assignment.md)
- **계획 문서**: `docs/plans/h04-round-ops.md`

> **이번 작업의 본질**: 1차에서는 "호스트가 카메라가 아니라 운영 화면으로 가는가"가 전부였고 화면은 예시 상수를 표시하는 골격이었다. 2차는 그 골격에 **서버 정본(rooms/{code}.round)** 을 연결한 것이다. 따라서 QA 1순위는 **① 타이머 상태 전이가 서버·타 클라이언트에 정확히 반영되는가 ② 신규 보안 규칙이 정확히 열리고 닫히는가 ③ 야외 환경(오프라인·화면 잠금·장시간)에서 타이머가 어긋나지 않는가** 다.

---

## 1. 작업 범위 요약

### 신규 (untracked)

| # | 레이어 | 파일 | 내용 | 담당 TC |
|---|---|---|---|---|
| 1 | API | `src/features/round-ops/api/round.ts` | `ROUND_DURATION_DEFAULT_MS`(20분)·`ROUND_ADJUST_STEP_MS`(1분)·`ROUND_DURATION_MAX_MS`(3시간) + `startRound`/`pauseRound`/`resumeRound`/`adjustRound`. 모든 전이가 `writeRound`로 수렴 — `startedAt: serverTimestamp()`로 매번 재앵커하고 `round` 맵만 통째로 쓴다 | A, B, F |
| 2 | API | `src/features/round-ops/api/notices.ts` | `NOTICE_TEXT_MAX_LENGTH`(100) + `sendNotice`(addDoc append) / `subscribeToLatestNotice`(orderBy createdAt desc, limit 1) | C, F |
| 3 | composable | `src/features/round-ops/composables/useRoundTimer.ts` | `computeRemainingMs`(순수) / `formatRemaining`(mm:ss, 초 올림) / `resolveDisplayState` / `useRoundTimer`(1초 tick + `visibilitychange` 즉시 재계산 + unmount 정리) | A, M-03 |
| 4 | store | `src/features/round-ops/stores/useRoundOpsStore.ts` | 방·명단·최근 공지 3구독 + `start`/`pause`/`resume`/`adjustBy`/`applyAdjust`/`submitNotice`, `pendingAdjustMinutes`(로컬), `isActionPending`, `actionError` | A, B, C, D, O |
| 5 | 컴포넌트 | `components/RoundTimerCard.vue` | hero 타이머(`text-hero` mono tabular-nums) + 요약 `라운드 N · {상태} · M팀 배정` | A, G |
| 6 | 컴포넌트 | `components/TimeAdjustCard.vue` | −1분/+1분 스테퍼(로컬 누적) + `반영`(대기값 0이면 disabled) + `role="status"` 캡션 | B |
| 7 | 컴포넌트 | `components/NoticeCard.vue` | 최근 공지 1건 미리보기(`{상대시각} · {본문}`) / 빈 상태 + `공지 보내기` | C |
| 8 | 컴포넌트 | `components/NoticeSheet.vue` | `BaseBottomSheet` + `BaseInput`(maxlength 100) + 카운터 + `전송` | C |
| 9 | 상수 | `roundStateStyles.ts` | `ROUND_STATE_LABEL`(대기/LIVE/일시정지/종료) + `ROUND_STATE_TONE`(neutral/success/warning/neutral) | G |
| 10 | 단위 테스트 | `__tests__/round.spec.ts`(12) `notices.spec.ts`(6) `useRoundTimer.spec.ts`(12) `useRoundOpsStore.spec.ts`(20) | 신규 50 케이스 | [unit] 표기 |

### 수정

| # | 레이어 | 파일 | 변경 내용 | 담당 TC |
|---|---|---|---|---|
| 11 | 화면 | `src/features/round-ops/RoundOpsPage.vue` | 스켈레톤 전면 교체 — 예시 상수·고지문·`notifyPreparing`·**판정 대기 행 삭제**, store 연결, 상태별 렌더, 호스트 가드 redirect, 공지 시트, **하단 탭 `sticky bottom-0`** 전환(safe-area 인셋을 탭 바가 직접 보유) | A~D, G |
| 12 | 데이터 | `src/features/waiting-room/api/rooms.ts` | `RoundState` 타입 신설, `RoomInfo.round` 추가, `toRoundState`/`toRoomInfo` 매핑 추출(getRoom·subscribeToRoom 공용). 알 수 없는 status·필드 부재는 `null`로 수렴 | A, E-06 |
| 13 | 공개 API | `src/features/waiting-room/index.ts` | `isAssignedInRound`·`subscribeToRoom`·`subscribeToParticipants` + 타입 4종 export | — |
| 14 | 보안 규칙 | `firestore.rules` | **`isValidRound()` 함수 신설**, rooms update **3번째 갈래**(`hasOnly(['round'])` + `status=='playing'` + 스키마 검증), **`match /notices/{noticeId}` 신설**(read: 로그인 / create: 호스트+화이트리스트+1~100자 / update·delete: false) | **F군 전체** |
| 15 | 공용 컴포넌트 | `src/shared/components/BaseToastProvider.vue` | 토스트 뷰포트 하단 여백 `calc(safe + 1rem)` → `calc(safe + 6rem)` — **전역 영향** | H-04, I-04 |
| 16 | 단위 테스트 | `round-ops/__tests__/RoundOpsPage.spec.ts` | 3케이스 → **18케이스** 전면 재작성 | [unit] 표기 |
| 17 | 단위 테스트 | `waiting-room/__tests__/rooms.spec.ts` 외 3 | `round` 매핑 3케이스 추가 + 기존 픽스처에 `round: null` | I-05 |

**전체 단위 테스트: 456 passed (48 files)** — type-check·lint·build 포함 4종 통과(2026-07-25).

---

## 2. 코드에서 도출한 크리티컬 리스크

| # | 리스크 | 근거(코드) | 담당 TC |
|---|---|---|---|
| **R-1** | **🔴 rules 미배포 시 운영 전면 마비** — `round` 갈래와 `notices` 갈래가 **둘 다 신규**다. 구버전 ruleset에서는 라운드 시작·정지·공지가 **전부 403**이고, 화면은 에러 토스트만 반복한다. 1차 QA에서 같은 사고(배정 확정 403) 전례 | `firestore.rules:32-47, 118-124, 190-210` | F-01·F-11, P0-04, 체크리스트 0 |
| **R-2** | **구독 에러 콜백 없음** — `subscribeToRoom`/`subscribeToParticipants`/`subscribeToLatestNotice` 모두 `onSnapshot(query, onNext)` 형태로 **error 핸들러가 없다**. 권한 상실·세션 만료 시 화면이 **조용히 갱신 중단**되고, 첫 스냅샷 전에 실패하면 `phase`가 `loading`에 머물러 `라운드 상태를 불러오는 중…`이 영구 표시된다 | `rooms.ts:242`, `notices.ts:56` | O-01, O-04 |
| **R-3** | **남은 시간은 뷰어 기기 시계로 계산** — 앵커(`startedAt`)만 서버 시각이고 `computeRemainingMs`는 `Date.now()`를 쓴다. 호스트 폰 시계가 5분 틀리면 **호스트가 보는 남은 시간 자체가 5분 어긋난다**(참가자 화면은 아직 없어 대조 불가) | `useRoundTimer.ts:26-34` | M-04 |
| **R-4** | **serverTimestamp 지연 보상** — 쓰기 직후 로컬 스냅샷의 `startedAt`은 null이고, 이때 `computeRemainingMs`는 경과를 0으로 본다(= 총량 그대로 표시). 온라인에서는 수백 ms 뒤 정정되지만, **오프라인에서는 서버 확정 전까지 타이머가 멈춘 것처럼 보인다** | `rooms.ts:118`, `useRoundTimer.ts:32` | M-02 |
| **R-5** | **일시정지 값은 클릭 순간의 클라 계산값** — `pauseRound(code, computeRemainingMs(round, Date.now()))`. 네트워크가 느리면 실제 반영까지의 지연만큼 참가자 기준 남은 시간이 늘어난다(스펙상 허용, 크기 실측 필요) | `useRoundOpsStore.ts:158-160` | A-11, M-01 |
| **R-6** | **시간 조정 대기값은 창별 로컬 상태** — `pendingAdjustMinutes`는 Pinia 로컬. 호스트가 두 창을 열면 각 창이 별도 대기값을 갖고, 각각 반영하면 **합산 적용**된다 | `useRoundOpsStore.ts:50` | B-09, N-02 |
| **R-7** | **토스트 뷰포트 전역 변경** — 하단 여백을 96px로 올렸다. round-ops 탭 바는 해소되지만 **모든 화면의 토스트 위치가 바뀐다**(대기실·입장·프로필·배정 보드) | `BaseToastProvider.vue:17-19` | H-04, I-04 |
| **R-8** | **하단 탭 sticky 전환** — `sticky bottom-0` + `bg-canvas`. 스크롤 중 카드가 탭 뒤로 지나갈 때 배경 비침·겹침, 그리고 safe-area 적용 위치(탭 바가 직접 보유)가 실기기에서만 검증 가능 | `RoundOpsPage.vue:111, 236` | G-04, H-05 |
| **R-9** | **종료는 서버에 남지 않는다** — remaining ≤ 0은 클라이언트 판정이고 문서에는 여전히 `status: 'running'`이다. 뷰어마다 시계가 다르면 종료 시점이 갈릴 수 있고, 새로고침해도 '종료'는 재계산으로만 성립한다 | `useRoundTimer.ts:70` | A-07, A-08 |
| **R-10** | **게스트 가드가 대기실로 되돌릴 때 재입장이 일어난다** — `waiting` 방에서 게스트를 `/waiting-room/{code}`로 replace하면 대기실 `enter()`가 다시 돈다(멱등이라 레디는 보존되어야 한다) | `RoundOpsPage.vue:76-83` | D-02 |
| **R-11** | **공지 길이 계산 이중 기준** — 화면 카운터·`maxlength`는 JS UTF-16 길이, rules `size()`는 유니코드 문자 수. 이모지 입력 시 카운터가 실제보다 크게 셈(전송은 통과) | `NoticeSheet.vue:48-53` vs `firestore.rules:206` | C-07 |
| **R-12** | **`isActionPending`은 라운드 쓰기만 가드** — 공지 전송은 별도 플래그(`isSendingNotice`)라 **라운드 조작과 공지 전송이 동시에 나갈 수 있다**. 서로 다른 문서라 충돌은 없으나 오프라인 큐 순서 확인 필요 | `useRoundOpsStore.ts:104-110, 214` | N-03 |

---

## 3. 테스트 환경·사전 조건

| 항목 | 내용 |
|---|---|
| **🔴 사전 배포** | **`firebase deploy --only firestore:rules` 선행 필수**(R-1). 미배포 상태로는 A·B·C·F군 전체가 403으로 실패한다. 배포 전이면 F-01 한 건으로 먼저 확인하고 시작한다 |
| 계정 | 호스트 1(방 소유자) / 게스트 2+ / 제3자 1(비참가 로그인) — 1차 QA 계정(`qahost7251@`·`qaguest7251@example.com`) 재사용 가능, 신규 필요 시 `/signup`으로 생성(닉네임 유니크) |
| 데이터 상태 | ① `status: playing` + `assignmentRound ≥ 1` + 배정된 participants 있는 방(주 검증 대상) ② `status: waiting` 방(D-08) ③ 존재하지 않는 코드(D-03) |
| 기기 매트릭스 | 데스크톱 Chrome 3 프로파일(호스트/게스트/제3자) + 뷰포트 에뮬레이션(320 / 390×664 / 390×750 / 430×860 / 1440) + **iOS Safari·Android Chrome 실기기**(M-06, H-05) |
| 멀티 클라이언트 | 별도 `user-data-dir` + 포트(9222/9223/9224). 실시간 판정은 **항상 타 클라이언트 화면 기준**(Firestore 지연 보상) |
| 네트워크 | CDP `Network.emulateNetworkConditions`(offline / 3G) + 실기기 비행기 모드 |
| 백엔드 조작 | **Firestore REST + 실제 사용자 idToken**(rules 판정 포함). 콘솔은 admin이라 rules 미적용 — F군 검증에 사용 금지 |
| 타이머 판정법 | ① 화면 텍스트(`mm:ss`) 2회 샘플링으로 감소 확인 ② REST `GET rooms/{code}`로 `round.{status,startedAt,durationMs,pausedRemainingMs}` 직접 확인 ③ 두 판정이 어긋나면 서버 문서가 정본 |
| 시계 오차 재현 | CDP `Emulation.setDeviceMetricsOverride`로는 불가 — 실기기 시스템 시각 변경 또는 `page.evaluate`로 `Date.now` 패치(patched 시각 기준 계산 확인) |
| 구독 누수 판정 | DevTools Network에서 `Listen/channel` 스트림 수 카운트(진입/이탈 반복 전후 비교) |

---

## 4. TC — A. 라운드 타이머 상태 전이 (핵심)

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| A-01 [unit] | P1 | 호스트, playing 방, `round` 없음 | 운영 탭 진입 | 배지 `대기`(neutral outline), 타이머 `20:00`(secondary 색), 요약 `라운드 N · 대기 · M팀 배정`, **`라운드 시작` 풀폭 1개만**, 시간 조정 카드 **없음** |
| A-02 | P1 | A-01 상태 | `라운드 시작` 클릭 | 배지 `LIVE`(success), 타이머 success 색으로 20:00부터 1초씩 감소, 2열(일시정지/재개) + 시간 조정 카드 등장. REST: `round = {status:'running', startedAt:<서버시각>, durationMs:1200000}`, **`pausedRemainingMs` 키 없음** |
| A-03 | P1 | LIVE, 남은 시간 임의 | `일시정지` 클릭 | 배지 `일시정지`(warning), 타이머 warning 색으로 **30초 관찰 동안 값 불변**, 일시정지 disabled / 재개 활성. REST: `status:'paused'`, `pausedRemainingMs == durationMs == 정지 시점 남은 ms(±2초)` |
| A-04 | P1 | 일시정지 상태 | `재개` 클릭 | 정지 시점 값부터 다시 1초씩 감소. REST: `status:'running'`, **`pausedRemainingMs` 키 소멸**, `startedAt` 갱신 |
| A-05 | P1 | LIVE, 남은 18:00 | 새로고침(F5) | 같은 화면 복원 + 남은 시간이 **경과분을 반영해 이어짐**(±2초). 20:00으로 되돌아가면 실패 |
| A-06 | P1 | 일시정지, 남은 05:00 | 새로고침 | 일시정지 상태·`05:00` 그대로(경과 반영 안 됨) |
| A-07 | P2 | LIVE, `durationMs`를 REST로 5초로 축소 | 0 도달까지 관찰 | `00:00` + 배지 `종료`(neutral) + 요약에 `종료` + `라운드 시작` 재노출 + 시간 조정 카드 소멸. **이 구간에 Firestore 쓰기 0건**(종료는 클라 판정, R-9) |
| A-08 | P2 | 종료 상태 | `라운드 시작` 클릭 | 새 20분 카운트다운. **라운드 번호는 그대로**(재배정 없음 — §15 한계 1) |
| A-09 | P2 | LIVE | `일시정지` 5회 연타 | Firestore 쓰기 **1건만**(`isActionPending` 가드), 상태·값 정상, 에러 토스트 없음 |
| A-10 | P3 | LIVE | 판정 → 기록 → 운영 탭 왕복(10초 소요) | 복귀 시 타이머가 **왕복 시간만큼 줄어 있음**(페이지 setup의 tick이 유지됨), 끊김·리셋 없음 |
| A-11 | P2 | LIVE | 일시정지 → 1초 내 재개 | 남은 시간 손실 ≤ 2초(R-5 스큐 실측 — 실측값을 결과에 수치로 기록) |
| A-12 | P3 | 호스트, playing, `round.status`를 REST로 `'bogus'`로 오염 | 화면 관찰 | 손상값은 `null`로 수렴 → **시작 전 상태**로 표시되고 `라운드 시작`으로 복구 가능(크래시 없음) |

## 5. TC — B. 시간 조정

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| B-01 [unit] | P1 | LIVE | 시간 조정 카드 관찰 | 캡션 `-1분 / +1분으로 조정한 뒤 반영을 누르면 참가자 전원에게 적용돼요.`, `반영` **disabled** |
| B-02 [unit] | P1 | LIVE | `+1분` 2회 클릭 | 캡션 `대기 변경값: +2분 · 반영 시 모든 참가자에게 적용`, **Firestore 쓰기 0건**, 타이머 값 불변 |
| B-03 | P1 | B-02 상태 | `반영` 클릭 | 남은 시간 **+2분 즉시 반영**, 캡션이 안내 문구로 복귀(`반영` 재-disabled). REST: `durationMs`가 (직전 남은 시간+120000)으로 재앵커, `startedAt` 갱신 |
| B-04 | P1 | LIVE | `-1분` 1회 → `반영` | 남은 시간 −1분. REST 동일 규칙 |
| B-05 | P2 | 일시정지(05:00) | `+1분` → `반영` | **정지 상태 유지**(저절로 재개되지 않음), 표시 `06:00`. REST: `status:'paused'`, `durationMs == pausedRemainingMs == 360000` |
| B-06 | P2 | LIVE, 남은 00:30 | `-1분` → `반영` | `00:00` + 종료 표시로 즉시 전환(0 클램프). REST: `durationMs: 0` (음수 아님) |
| B-07 | P3 | LIVE | `+1분` → `-1분` | 대기값 0 → 캡션 안내 문구 복귀 + `반영` disabled |
| B-08 | P2 | LIVE, 오프라인 전환 | `+1분` → `반영` | 화면은 마지막 스냅샷 유지. **온라인 복귀 시 큐가 나가 반영**되거나(SDK 큐) 에러 토스트 `요청을 처리하지 못했어요. 다시 시도해 주세요.` + **대기값 유지**. 둘 중 어느 쪽인지 실측해 기록 |
| B-09 | P3 | 호스트 창 2개(같은 계정·같은 방), 둘 다 LIVE | 창1 `+1분` 반영 → 창2 `+1분` 반영 | 총 +2분(창별 대기값 — R-6, §15 한계 5). 창2의 대기값이 창1 반영으로 초기화되지 않음을 확인 |

## 6. TC — C. 공지

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| C-01 [unit] | P1 | playing, 공지 이력 없음 | 운영 탭 관찰 | 행 `공지 전송` + 캡션 `아직 보낸 공지가 없어요.` + 버튼 `공지 보내기` |
| C-02 | P1 | 위와 동일 | `공지 보내기` → 시트에 `보급품 A 지점` 입력 → `전송` | 시트 제목 `공지 보내기` / 설명 `참가자 전원에게 즉시 전달됩니다.` / placeholder `예) 보급품 A 지점에 배치했습니다`. 전송 후 **시트 닫힘** + 토스트 `공지를 보냈어요.`(success) + 카드 캡션 `방금 · 보급품 A 지점` |
| C-03 | P1 | C-02 직후 | REST `GET rooms/{code}/notices` | 문서 1건 `{text, createdAt}` **정확히 2필드**, `createdAt`은 서버 시각 |
| C-04 | P2 | 공지 1건 존재 | 두 번째 공지 전송 | 카드가 **최신 1건으로 교체**, 컬렉션에는 2건 남음(append-only) |
| C-05 | P2 | 시트 열림 | 빈 입력 / 공백만 입력 | `전송` **disabled**(공백만도 disabled) |
| C-06 | P2 | 시트 열림 | 120자 붙여넣기 | 입력이 **100자에서 끊김**, 카운터 `100/100` |
| C-07 | P3 | 시트 열림 | 이모지 10개 + 한글 입력 후 전송 | 전송 성공(rules 통과). 카운터가 이모지를 2로 세는 것은 알려진 한계(R-11, §15 한계 8) |
| C-08 | P2 | 시트에 문구 입력 후 닫기(배경/닫기 버튼) | 다시 `공지 보내기` | 입력 **초기화**(지난 문구 잔존 시 오전송 위험) |
| C-09 | P2 | 오프라인 | 공지 전송 | 시트 유지 + 입력 보존 + 에러 토스트(또는 SDK 큐 반영) — B-08과 같은 방식으로 실측 기록 |
| C-10 | P3 | 공지 1건 전송 후 2분 대기 | 카드 관찰 | 캡션이 `방금 · …` → `2분 전 · …`으로 자동 갱신(타이머 tick 공유) |

## 7. TC — D. 호스트 가드·라우트·딥링크

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| D-01 [unit] | P1 | 게스트 계정, **라운드가 시작된** `playing` 방 | `/round-ops/{코드}` 직접 진입 | **`/camera/{코드}`로 replace**. 운영 화면 컨트롤 노출 0, **Firestore 쓰기 0건**. (3차에서 목적지 규칙 변경 — §19.3 T-10) |
| D-02 [unit] | P1 | 게스트, 방은 `waiting` | 직접 진입 | **`/waiting-room/{코드}`로 replace**, 대기실 정상 표시. 재입장이 멱등이라 **기존 레디 상태 보존**(R-10) |
| D-03 [unit] | P1 | 로그인 상태 | `/round-ops/ZZZZ`(없는 코드) | 토스트 `방을 찾을 수 없어요.`(danger) + **`/`(entry)로 replace** |
| D-04 | P1 | 로그아웃 | `/round-ops/{코드}` | `/login?redirect=/round-ops/{코드}` (기존 authGuard 회귀) |
| D-05 | P1 | 호스트, LIVE | 새로고침 | 화면·타이머 복원(A-05와 동일 판정), 로그인 화면으로 튕기지 않음 |
| D-06 | P2 | 호스트 | `/round-ops/hjjy`(소문자) | `ROOM HJJY` 대문자 정규화 표기 + 정상 구독 |
| D-07 | P3 | 호스트 | `/round-ops/ab2c-!@#<img src=x onerror=alert(1)>` | 크래시·XSS 없음(`img` 요소 미생성). 가드가 없는 방으로 판정해 D-03 경로 |
| D-08 | P2 | 호스트, 방은 `waiting` | 직접 진입 | `게임이 아직 시작되지 않았어요` + `대기실에서 팀 배정을 확정하고 게임을 시작하면 라운드를 운영할 수 있어요.` + `대기실로` 버튼 → 대기실 이동. 컨트롤·타이머 카드 **없음** |
| D-09 | P3 | replace로 진입한 상태 | 뒤로가기 | 대기실이 아니라 이전 화면(`replace` 스펙 — §15 한계 7) |

## 8. TC — E. 실시간 동기화 (멀티 클라이언트)

> 판정은 **항상 타 클라이언트 화면 기준**. 조작한 창의 즉시 반영은 지연 보상일 뿐 증거가 아니다.

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| E-01 | P1 | 호스트 창 2개(같은 계정·같은 방) | 창1에서 `라운드 시작` | 창2가 **1초 내** LIVE 전환 + 남은 시간 일치(±2초) |
| E-02 | P1 | 두 창 LIVE | 창1 `일시정지` | 창2도 일시정지 + 같은 남은 시간에서 고정 |
| E-03 | P1 | 두 창 LIVE | 창1 `+2분` 반영 | 창2 남은 시간 +2분 반영(±2초) |
| E-04 | P1 | 두 창 | 창1에서 공지 전송 | 창2 카드가 `방금 · {본문}`으로 갱신 |
| E-05 | P2 | 호스트 운영 화면 + 배정 데이터 | REST로 participants의 `assignedRound`/`team` 변경 | 요약의 `M팀 배정`이 실시간 갱신 |
| E-06 | P2 | LIVE | REST로 `round` 필드 삭제 | 화면이 **시작 전 상태**(대기 배지 + `라운드 시작`)로 복귀, 크래시 없음 |
| E-07 | P3 | 운영 중 | REST/콘솔로 방 문서 삭제 | 토스트 `방을 찾을 수 없어요.` + entry로 이동(D-03과 동일 경로) |

## 9. TC — F. 보안 규칙 (신규 갈래 — REST + 실제 사용자 토큰)

> **허용 1 + 거부 N**이 원칙. 자동 테스트가 없으므로 전부 P1. `PATCH https://firestore.googleapis.com/v1/projects/photo-royale-scc/databases/(default)/documents/rooms/{code}?updateMask.fieldPaths=round` 형태로 검증.

| ID | 우선 | 요청 주체 / 페이로드 | 기대 결과 |
|---|---|---|---|
| F-01 | P1 | 호스트 / playing 방 + `{status:'running', startedAt:<timestamp>, durationMs:1200000}` | **200** (앱 정상 동작의 전제 — 실패 시 즉시 중단하고 rules 배포 확인) |
| F-02 | P1 | **게스트** / 동일 페이로드 | **403** |
| F-03 | P1 | 호스트 / **`waiting` 방**에 round 쓰기 | **403** |
| F-04 | P1 | 호스트 / `round` + `status` 동시 변경 | **403** (`hasOnly(['round'])`) |
| F-05 | P1 | 호스트 / `durationMs: "1200000"`(문자열) · `-1` · `10800001` 3종 | **각 403** |
| F-06 | P1 | 호스트 / `status:'running'`인데 `pausedRemainingMs` 포함 | **403** |
| F-07 | P1 | 호스트 / `status:'paused'`인데 `pausedRemainingMs` 누락 | **403** |
| F-08 | P1 | 호스트 / `status:'stopped'` | **403** |
| F-09 | P1 | 호스트 / round에 임의 필드(`{... , note:'x'}`) | **403** |
| F-10 | P1 | 호스트 / `startedAt: 1700000000`(숫자) | **403** |
| F-11 | P1 | 호스트 / `POST rooms/{code}/notices` `{text:'집합', createdAt: request.time}` | **200** |
| F-12 | P1 | **게스트** / 동일 공지 생성 | **403** |
| F-13 | P1 | 호스트 / `text` 101자 | **403** |
| F-14 | P1 | 호스트 / `text: ''` | **403** |
| F-15 | P1 | 호스트 / 공지에 임의 필드 추가 · `createdAt`을 임의 시각으로 | **각 403** |
| F-16 | P1 | 호스트 / 기존 공지 PATCH · DELETE | **각 403**(이력 불변) |
| F-17 | P2 | 게스트 / `GET rooms/{code}/notices` 구독 | **200** (게스트 수신 UI 후속 대비) |
| F-18 | P1 | 회귀 / ① 배정 확정(participants+room 배치) ② `waiting→playing` ③ `playing→waiting` | ①**200** ②**200** ③**403** — 기존 두 갈래가 신규 갈래 추가로 깨지지 않았는지 |

## 10. TC — G. 화면 렌더·탭

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| G-01 [unit] | P1 | 상태 4종 각각 | 배지·타이머 색·요약 확인 | 대기=neutral/secondary · LIVE=success/success · 일시정지=warning/warning · 종료=neutral/secondary. 요약은 항상 `라운드 N · {라벨} · M팀 배정` |
| G-02 | P1 | 2차 배정 확정된 방(1차 잔재 참가자 포함) | 요약의 팀 수 확인 | **이번 라운드(`assignmentRound`) 배정자만** 집계 — 직전 라운드 잔재는 제외 |
| G-03 | P2 | 운영 탭 | 판정 → 기록 → 운영 | `판정 화면 준비 중` / `기록 화면 준비 중` + `이 탭은 다음 단계에서 구현돼요. 지금은 운영 탭만 동작합니다.` / 복귀 시 컨트롤 복원 |
| G-04 | P2 | 390×664 뷰포트, LIVE | 콘텐츠 스크롤 | **하단 탭이 항상 화면에 보인다**(sticky) + 카드가 탭 뒤로 비쳐 보이지 않음(bg-canvas) — **1차 B-06 회귀 해소 확인** |
| G-05 | P2 | 데스크톱 | 폭 320~1440 | `max-w-md` 유지, 가로 스크롤 없음(`scrollW == clientW`) |
| G-06 | P3 | 느린 네트워크(3G) | 진입 직후 | `라운드 상태를 불러오는 중…`(role=status) 표시 후 데이터 도착 시 교체 |
| G-07 | P3 | 스켈레톤 회귀 | 화면 전체 텍스트 | `운영 기능은 준비 중이에요…` 고지문 **없음**, `판정 대기`/`3건` 행 **없음**, `18:42` 예시값 **없음** |

## 11. TC — H. 접근성·모바일·테마

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| H-01 | P2 | 운영 탭 | 터치 타겟 측정 | 일시정지·재개·라운드 시작 **56**, −1분/+1분/반영 **48**, 공지 보내기 **36 + `::before` 히트 48**, 하단 탭 40(§15 한계 6) |
| H-02 | P2 | 스크린리더 | `+1분` 클릭 | 시간 조정 캡션이 `role="status"` 라이브 영역으로 대기값 변경을 안내 |
| H-03 | P2 | 색약 시뮬레이션 | 상태 4종 | 색 외에 **텍스트 라벨 병기**(대기/LIVE/일시정지/종료)로 구분 가능 |
| H-04 | P2 | LIVE, 컨트롤 클릭해 토스트 발생 | 토스트 표시 중(4초) 하단 탭 클릭 | **탭 전환 성공** — 토스트가 탭을 덮지 않음(**1차 C-05 회귀 해소**, R-7) |
| H-05 | P3 | iOS 실기기 | 노치·홈 인디케이터 | sticky 탭 바가 홈 인디케이터에 가리지 않음(safe-area를 탭 바가 직접 보유 — R-8) |
| H-06 | P3 | `data-theme="light"` | 화면 확인 | 라이트 토큰 전환 + 타이머/배지 대비 유지 |

## 12. TC — I. 회귀

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| I-01 | P1 | 대기실, 배정 1차 확정 | 호스트 `게임 시작` → `라운드 시작` | 호스트는 `게임 시작` 즉시 `/round-ops/{코드}` + **`getUserMedia` 0회**. 게스트는 **`라운드 시작` 이후에** `/camera/{코드}` + 뷰파인더(3차에서 시점 변경 — §19.3 T-01·T-02) |
| I-02 | P1 | 호스트 | 팀 배정 시작 → 확정 | `팀 배정을 확정했어요.` + 게스트 배정 카드(F-18①과 짝) |
| I-03 | P1 | 게스트 | 카메라 화면 | 뷰파인더·촬영 기존대로 |
| I-04 | P2 | **대기실·입장·프로필 각 화면** | 토스트 발생(초대 링크 복사 등) | 토스트가 **하단 CTA를 가리지 않고**, 화면 밖으로 나가지도 않음(R-7 전역 영향 확인) |
| I-05 | P1 | — | `type-check` / `lint` / `test:unit` / `build` | 4종 통과(현재 456 tests) |
| I-06 | **P1** ✅live | 호스트, 라운드 진행 중 | 카운트다운 · 일시정지/재개 · ±시간 조정 · `라운드 종료`를 순서대로 확인 | **리팩터 회귀 확인**(2026-08-31 `fix/cockpit-round-end-exit`). 남은 시간 계산이 `waiting-room`의 `computeRoundRemainingMs`로 위임됐다 — 운영 화면의 `mm:ss` 표기, 정지 중 시간 고정, 조정 후 재앵커, 종료 쓰기가 이전과 동일해야 한다. 공개 시그니처(`computeRemainingMs`)는 그대로라 호출부 코드는 무변경. **2026-08-31 실측 전항 통과** — 카운트다운 19:57→19:53, 정지 중 19:52 고정, 재개 19:50→19:46, 시간 조정 서버 `durationMs` 1191361→1243734 반영, 종료 시 `status=waiting`·`round` 삭제(서버 문서 REST 대조). **서버 시각 보정 도입 후 재실측(2026-08-31, 방 98DP)** — 카운트다운 19:43→19:39, 정지 중 19:38 고정, 재개 19:35→19:31, 시간 조정 1177371→1229563 전항 통과 |

## 13. TC — M/N/O. 강인성 (환경·스케일·수명주기)

### M. 환경·기기

| ID | 우선 | 절차 | 기대 결과 |
|---|---|---|---|
| M-01 | P1 | LIVE 중 비행기 모드 30초 → 복귀 | 오프라인 동안 **타이머는 계속 흐른다**(로컬 계산). 복귀 후 서버 값과 재수렴(±2초), 콘솔 에러 0 |
| M-02 | P1 | 오프라인 상태에서 `라운드 시작` → 60초 후 온라인 | **관찰 대상(R-4)**: 오프라인 동안 타이머가 20:00에 멈춰 보이는지, 온라인 복귀 후 서버 확정 시각 기준으로 점프하는지 실측. 최종적으로 문서와 화면이 일치하면 통과 |
| M-03 | P1 | LIVE에서 화면 잠금/백그라운드 5분 → 복귀 | 복귀 **즉시**(visibilitychange) 남은 시간이 5분 줄어 있음. 몇 초 뒤에야 갱신되면 실패 |
| M-04 | P2 | 호스트 기기 시스템 시각을 +5분으로 변경 | 남은 시간이 5분 적게 표시(R-3 실측). **알려진 한계의 크기를 수치로 기록** |
| M-05 | P2 | 60분+ 체류(LIVE 유지) | 토큰 갱신으로 구독 생존, 타이머 오차 ≤ 5초, 조작 정상 |
| M-06 | P2 | iOS Safari + Android Chrome 실기기 | 타이머·컨트롤·시트·토스트 정상, 레이아웃 깨짐 없음 |
| M-07 | P3 | 카카오톡 인앱 브라우저로 진입 | 로그인·운영 화면 동작(또는 외부 브라우저 안내) |

### N. 동시성·스케일

| ID | 우선 | 절차 | 기대 결과 |
|---|---|---|---|
| N-01 | P2 | 참가자 25명(정원) 배정된 방에서 운영 | 명단 구독 중에도 타이머 1초 tick 지연 없음, 요약 팀 수 정확 |
| N-02 | P2 | 호스트 창 2개에서 **동시에** 일시정지/재개 | 마지막 쓰기 승, 두 창이 같은 상태로 수렴(스냅샷), 문서 스키마 유효(F군 조건 만족) |
| N-03 | P2 | 시간 반영과 공지 전송을 거의 동시에(R-12) | 두 쓰기 모두 성공, 화면 상태 정상(서로 다른 문서) |
| N-04 | P3 | 공지 5건 연속 전송 | 카드가 최신 1건 유지, 컬렉션 5건, 토스트 누적되되 레이아웃 정상 |

### O. 세션·수명주기

| ID | 우선 | 절차 | 기대 결과 |
|---|---|---|---|
| O-01 | P1 | 운영 화면 유지한 채 **다른 탭에서 로그아웃** | **R-2 실측**: 구독이 조용히 멈추는지, 화면이 어떤 상태로 남는지 기록. 이후 컨트롤 클릭 시 에러 토스트가 뜨는지 / 새로고침 시 `/login?redirect=` 유도되는지 |
| O-02 | P1 | 대기실 ↔ 운영 화면 5회 왕복 | Firestore Listen 스트림 수가 증가하지 않음(구독 누수 0), 메모리·CPU 정상 |
| O-03 | P2 | `라운드 시작` 직후 **즉시** 새로고침 | 서버에 반영된 상태로 복원(쓰기 유실 없음) |
| O-04 | P2 | 세션 만료 상태에서 컨트롤 클릭 | 에러 토스트 후 화면 유지, 새로고침 시 로그인 유도(무한 로딩 금지) |

## 14. TC — P0. 종합 리허설 (게이트)

| ID | 시나리오 | 통과 기준 |
|---|---|---|
| **P0-01** | **당일 풀체인**: 방 생성 → 링크 공유 → 게스트 입장·레디 → 배정 확정 → 게임 시작(호스트 운영/게스트 카메라) → **라운드 시작 → 공지 1건 → +1분 반영 → 일시정지 → 재개 → 종료 도달 → 재시작** → 호스트 새로고침 | 오류 0, 각 단계 3초 내 반영, 호스트 `getUserMedia` 0회, 새로고침 후 라운드 상태 복원 |
| **P0-02** | **오조작 복구**: 실수로 라운드를 시작/종료했을 때 되돌리기 | `라운드 시작` 재실행으로 20분 재설정 가능함을 실증 + 임의 시간으로 맞추는 절차(±1분 반영 반복 또는 콘솔 `durationMs` 수정)를 문서화 |
| **P0-03** | **호스트 기기 교체**: 진행 중 호스트 폰 교체(다른 기기 재로그인 → 방 재진입) | 진행 중이던 라운드의 남은 시간을 **그대로 이어받음**(서버 정본 확인) — 이번 변경의 핵심 가치 |
| **P0-04** | **rules 배포 스모크**: 배포 직후 테스트 방에서 `라운드 시작` 1회 + 공지 1건 | 둘 다 성공. 하나라도 403이면 **이벤트 시작 보류**(R-1) |

---

## 15. 알려진 한계 (버그가 아닌 현 스펙 — 오탐 방지)

1. **종료 후 재시작해도 라운드 번호는 그대로다.** 다음 라운드 재배정 플로우는 별도 작업(계획 §8).
2. **게스트 화면에는 타이머·공지 수신 UI가 없다.** 데이터(`round`·`notices`)는 게스트가 그대로 구독하면 되도록 설계됐지만 카메라 콕핏 UI는 후속.
3. **판정/기록 탭은 준비 중 안내만**. 판정 대기 건수·판정 플로우는 보류 확정(계획 §8).
4. **게임 종료(playing → waiting)는 앱에서 불가** — rules 단방향. 콘솔에서만 되돌린다.
5. **시간 조정 대기값은 창별 로컬 상태**(R-6) — 호스트 창을 2개 열면 각각 쌓인다.
6. **하단 탭 항목 높이 40px** — `BaseSegmented` 기존 스펙.
7. **뒤로가기로 대기실에 돌아갈 수 없다** — `replace` 스펙.
8. **공지 글자 수 카운터는 JS 문자열 길이 기준** — 이모지 1개를 2로 센다(rules는 문자 수 기준이라 전송은 통과).
9. **남은 시간은 뷰어 기기 시계로 계산된다**(앵커만 서버 시각) — 기기 시계가 틀리면 그만큼 어긋난다(M-04).
10. **종료 상태는 서버에 기록되지 않는다** — `round.status`는 계속 `running`이고 클라이언트가 0 도달을 종료로 표시한다.

---

## 16. 부록 — 1차(스켈레톤 단계) 검증 이력 요약

2026-07-25 1차 live-qa: **50 TC 중 ✅41 / ⚠️6 / ⏸3**. 당시 대상은 "호스트 전이 분기 + 정적 골격"이었다.

| 항목 | 1차 결과 | 2차에서의 상태 |
|---|---|---|
| 호스트 → `/round-ops`, 게스트 → `/camera` 분기(A군 10건) | ✅ 10/10 | **여전히 유효** → I-01로 회귀 확인 |
| 라우트·세션·딥링크(D군 8건) | ✅ 8/8 | 게스트 직접 진입(구 D-04)·없는 코드(구 D-06)는 **가드 신설로 기대값 변경** → D-01·D-03 |
| 운영 컨트롤 미연결·구독 없음(구 §14 한계 1~4) | — | **전부 무효** — 이번 작업이 해소 |
| 하단 탭이 모바일 첫 화면에서 안 보임(구 B-06 ⚠️) | ⚠️ 부분 | sticky 전환으로 수정 → **G-04로 해소 확인** |
| 토스트가 하단 탭을 4초간 덮음(구 C-05 ⚠️, R-6) | ⚠️ 부분 | 뷰포트 96px 상향으로 수정 → **H-04로 해소 확인** |
| `playing → waiting` 403(구 G-04) | ✅ 실증 | 유지 → F-18③ |
| 실기기·장시간·admin 필요 항목(구 E-06·G-05·G-06) | ⏸ 미검증 | **2차에서 M-05·M-06·H-05·O-02로 재도전** |
| 🔴 rules 드리프트로 배정 확정 403 사고 | 발생 후 배포로 해소 | **동일 리스크 재발 가능(R-1)** — 이번엔 갈래가 2개 늘었다 |

---

## 17. 커버리지 매트릭스

| 영역 | TC 수 | P0 | P1 | P2 | P3 | [unit] 커버 | live-qa 중점 |
|---|---|---|---|---|---|---|---|
| A. 타이머 상태 전이 | 12 | — | 6 | 4 | 2 | 1 | 새로고침 복원·연타 가드·서버 문서 대조 |
| B. 시간 조정 | 9 | — | 4 | 3 | 2 | 2 | 반영 후 재앵커·정지 중 조정·오프라인 |
| C. 공지 | 10 | — | 3 | 5 | 2 | 1 | append-only·시트 수명주기·상대 시각 |
| D. 가드·라우트 | 9 | — | 5 | 3 | 1 | 3 | 역할별 redirect(스펙 변경) |
| E. 실시간 동기화 | 7 | — | 4 | 2 | 1 | 0 | **멀티 클라이언트 — 단위로 불가** |
| F. 보안 규칙 | 18 | — | 17 | 1 | — | 0(문자열 동기화 가드만) | **REST 403/200 매트릭스 — 단위로 불가** |
| G. 렌더·탭 | 7 | — | 2 | 3 | 2 | 1 | sticky 탭·반응형 |
| H. 접근성·모바일 | 6 | — | — | 4 | 2 | 0 | 실기기 safe-area·토스트 겹침 |
| I. 회귀 | 5 | — | 4 | 1 | — | 1 | 토스트 전역 영향 |
| M. 환경 강인성 | 7 | — | 3 | 3 | 1 | 0 | **오프라인·화면 잠금·시계 오차** |
| N. 동시성·스케일 | 4 | — | — | 3 | 1 | 0 | 정원 규모·동시 쓰기 |
| O. 세션·수명주기 | 4 | — | 2 | 2 | — | 0 | **구독 에러 콜백 부재(R-2)** |
| P0. 리허설 | 4 | 4 | — | — | — | 0 | 풀체인 게이트 |
| **합계** | **102** | **4** | **50** | **34** | **14** | **9** | — |

단위 테스트 456건이 로직·렌더 분기를 덮고 있으므로, live-qa는 **F(보안 규칙) · E(멀티 클라이언트) · M(환경 강인성) · O(수명주기) · P0(리허설)** 에 집중한다.

---

## 18. 검증 결과 (live-qa, 2026-07-25 2차)

**환경**: local dev `http://localhost:5173`(현재 워크트리) 단독 — 신규 기능이라 main baseline 대조는 성립하지 않는다. Chrome for Testing 3 프로파일(호스트/게스트/호스트 2번째 기기), 실제 Firebase(`photo-royale-scc`). **`firestore.rules` 배포 완료**(`firebase deploy --only firestore:rules` — 컴파일 성공 + 릴리스). 스크린샷 35장은 `.qa-shots/`.

**1차 결과: 102 TC 중 ✅88 / ❌3 / ⚠️3 / ⏸8** → **발견 3건 수정 후 재검증 완료: ✅92 / ⚠️2 / ⏸8 (❌ 0)**

| 영역 | ✅ | ❌ | ⚠️ | ⏸ | 핵심 근거 |
|---|---|---|---|---|---|
| A. 타이머 전이 | 11 | — | — | 1 | 서버 문서(REST)와 화면을 매 전이마다 대조 |
| B. 시간 조정 | 9 | — | — | — | 재앵커·클램프·오프라인 큐까지 서버 값 확인 |
| C. 공지 | 9 | — | 1 | — | notices 2필드 append·최신 1건 구독 확인 |
| D. 가드·라우트 | 8 | — | — | 1 | 역할별 redirect 4종 실측 |
| E. 실시간 동기화 | 4 | — | 1 | 2 | 호스트 2기기 동시 관찰 |
| F. 보안 규칙 | 18 | — | — | — | REST+실토큰 28건 매트릭스 전건 일치 |
| G. 렌더·탭 | 6 | — | 1 | — | sticky 탭·반응형 실측 |
| H. 접근성·모바일 | 4 | 1 | — | 1 | 터치 타겟 수치 확인, 토스트 히트 테스트 실패 |
| I. 회귀 | 4 | 1 | — | — | 역할 분기·카메라·단위 456건 |
| M. 환경 강인성 | 4 | — | — | 3 | 오프라인·백그라운드·시계 오차 실측 |
| N. 동시성 | 4 | — | — | — | 참가자 24명·동시 쓰기 |
| O. 수명주기 | 3 | 1 | — | — | 구독 누수 없음 / **세션 상실 시 오작동 발견** |
| P0. 리허설 | 4 | — | — | — | 풀체인 75초, 오류 0 |

### 🔴 발견 사항 (3건 — **전부 수정·재검증 완료**)

> **수정 후 재검증(2026-07-25)**: 브라우저 실동작 5건 전부 통과 + 단위 회귀 테스트 3건 추가(456 → **459 passed**).
> - **O-01** → `/login?redirect=/round-ops/N4TB`로 이동, `<video>` 없음(카메라 진입 0).
> - **H-04·I-04** → 토스트 노출 중 탭 히트 타깃이 `BUTTON[radio]`로 바뀌고 실제 탭 전환 성공. 토스트 자신의 닫기 버튼은 그대로 동작. 대기실 CTA도 히트 타깃이 버튼 내부 라벨(`SPAN(팀 배정 시작)`)로 정상. **수정에는 패딩(시각적 분리)과 `pointer-events`(히트 영역) 둘 다 필요했다** — 한쪽만 적용했다가 재검증에서 한 번 더 실패한 뒤 확정했다.
> - **C-06** → 상한 초과 시 전송 `disabled` + 카운터 `rgb(255,92,107)`(danger), 상한 이내로 줄이면 다시 활성.

| # | 심각도 | TC | 증상 | 원인 | 조치 (✅ 적용됨) |
|---|---|---|---|---|---|
| **1** | **높음** | O-01 | **세션이 끊기면 호스트가 `/camera`로 튕긴다.** 다른 탭에서 로그아웃(`signOut`) → 운영 화면이 즉시 카메라 콕핏으로 이동(`path=/camera`, 배지·타이머 소멸). 이 기능이 막으려던 바로 그 화면이고, 카메라 권한 프롬프트까지 뜰 수 있다 | `RoundOpsPage.vue`의 호스트 가드가 `!isHost`를 "게스트"로만 해석한다. 세션이 사라지면 `myId === null` → `isHost === false` → 게스트 분기로 리다이렉트 | 가드에 세션 조건 추가: `if (phase !== 'ready' \|\| isHost \|\| myId === null) return`. 세션 없음은 authGuard/로그인 유도로 처리 |
| **2** | **중간** | H-04, I-04 | **토스트가 뜨는 4초 동안 하단 컨트롤이 클릭되지 않는다.** 토스트 자체는 탭 위로 비켜났지만(시각적 겹침 0), 투명한 토스트 뷰포트(`<ol>`, `bottom:0`, 높이 168px)가 탭·CTA를 덮어 `elementFromPoint`가 `OL`을 반환하고 실제 클릭도 먹지 않았다. **1차 QA C-05가 미해소**이며 차단 영역은 오히려 96px로 커졌다. 대기실 CTA도 동일 | `BaseToastProvider`의 뷰포트가 `pointer-events: auto`. 패딩으로 아이템만 밀어 올려도 컨테이너 히트 영역은 그대로 | 뷰포트에 `pointer-events-none`, 개별 토스트(`BaseToast` 루트)에 `pointer-events-auto`. 그러면 96px 패딩도 원복 가능 |
| **3** | 낮음 | C-06 | 100자를 넘긴 상태에서 `전송`이 **활성인데 눌러도 아무 일도 일어나지 않는다**(시트 유지·토스트 없음). 카운터는 `120/100`으로 표시되지만 경고색이 아니다. 키보드 입력·붙여넣기는 `maxlength`가 막으므로 재현 경로는 IME·프로그래매틱 입력에 한정 | `NoticeSheet`의 `canSend`가 하한(>0)만 보고, 상한 초과는 store가 조용히 `false` 반환 | `canSend`에 상한 조건 추가 + 초과 시 카운터를 `text-danger`로 |

### ℹ️ 알려진 한계로 문서화할 실측 거동 (버그 아님)

- **오프라인에서 라운드 시작/재개 시 타이머가 멈춰 보인다** — 서버 앵커(`startedAt`) 확정 전에는 경과가 0으로 계산된다. 실측: 오프라인 재개 후 6초 동안 `10:00` 고정 → 온라인 복귀 즉시 `09:57`로 정정(R-4 예측대로).
- **오프라인 쓰기 중에는 모든 라운드 컨트롤이 잠긴다** — `isActionPending`이 서버 ack까지 유지되어 일시정지·재개·반영이 전부 disabled. 온라인 복귀 시 자동 해제(영구 잠김 아님).
- **기기 시계 오차가 그대로 표시된다** — `Date.now`를 +5분으로 조작하니 남은 시간이 정확히 5분 적게 표시(`15:24 → 10:23`). 앵커만 서버 시각이라는 설계의 알려진 대가(한계 9).
- **손상된 라운드 상태는 클라이언트 권한으로 만들 수 없다** — `round` 필드 삭제·`status:'bogus'` 모두 rules가 403으로 거부(A-12·E-06이 ⏸인 이유). 방어가 의도대로 동작한다는 뜻.

### ⏸ 미검증 8건과 사유

| TC | 사유 |
|---|---|
| A-12, E-06, E-07 | 손상 상태·문서 삭제를 만들 수단이 없다(클라 rules 403, admin ADC 미설정). 단위 테스트가 `toRoundState`의 null 수렴을 커버 |
| D-09 | 뒤로가기 — 1차 QA D-02에서 ✅ 확인된 `replace` 스펙, 이번 회차 미재검증 |
| H-05, M-06 | iOS/Android **실기기** 필요(safe-area·실제 터치) |
| M-05 | 60분+ 장시간 체류 — 소요 시간 |
| M-07 | 카카오톡 인앱 브라우저 — 실기기 필요 |

### ⚠️ 부분 검증 3건

- **C-06**: 키보드 입력 경로는 `maxlength`로 정상 차단(30자 타이핑 = 30자) — 초과 경로만 위 발견 3.
- **E-05**: 배정 데이터 변경 후 요약이 `13팀 배정`으로 정확히 집계됨은 확인했으나, **구독을 통한 라이브 갱신**(새로고침 없이)은 분리 검증하지 못했다.
- **G-02**: 이번 라운드 배정자만 집계하는 로직은 단위 테스트(`useRoundOpsStore.spec`)가 커버. 실데이터에서 **직전 라운드 잔재가 섞인 상태**는 만들지 못했다(playing 방에서는 재배정이 rules로 막힌다).

---

## 19. 3차 변경 — 게스트 전이 시점 · 콕핏 경로 (2026-07-25)

> **왜**: 호스트가 `게임 시작`만 누르고 `라운드 시작`은 아직 안 눌렀는데 게스트 6명이 이미 카메라 화면에 있었다(실사용 관찰, 방 `4NYY`). 진행자가 인원을 세고 공지하는 동안 플레이어는 켜진 뷰파인더만 보게 되고, 그동안 자기 완장·팀원을 다시 확인할 방법도 사라진다. 또 콕핏 URL이 `/camera`라 어느 방인지 경로에 남지 않았다.

### 19.1 변경 내용

| # | 파일 | 변경 |
|---|---|---|
| 1 | `stores/useWaitingRoomStore.ts` | `isRoundStarted` 신설 — `room.round != null`. **게임 시작(playing)과 라운드 시작을 개념적으로 분리** |
| 2 | `WaitingRoomPage.vue` | 전이 watch를 `[gameStatus, isRoundStarted]`로. 호스트는 playing 즉시 운영 화면, **게스트는 라운드가 시작돼야** `{ name:'camera', params:{ roomCode } }` |
| 3 | `router/index.ts` | `/camera` → **`/camera/:roomCode`**. 방 코드 없는 구 경로는 빈 화면 대신 entry로 `redirect` |
| 4 | `RoundOpsPage.vue` | 게스트 가드의 목적지를 대기실 규칙과 일치시킴 — `isPlaying && round !== null`일 때만 콕핏, 아니면 대기실. watch 소스에 `round` 추가 |

### 19.2 크리티컬 리스크

| # | 리스크 | 담당 TC |
|---|---|---|
| **R-13** | **전이 조건이 좁아진 만큼 "안 넘어가는" 장애가 새로 생긴다** — round 스냅샷이 도착해야 콕핏으로 가므로, 구독이 끊기거나 지연되면 라운드가 시작됐는데 게스트가 대기실에 남는다. 이전에는 playing 하나로 넘어갔다 | T-02, T-09, T-11 |
| **R-14** | **구 경로(`/camera`)에 이미 들어가 있던 게스트** — 배포 직후 새로고침하면 라우트가 매칭되지 않는다. redirect로 막았지만 게임 중에 입장 화면으로 나가는 것 자체가 이탈이다 | U-03 |
| **R-15** | **콕핏은 방 구독 결과에 의존한다** — 없는 방은 `/`로, waiting·round 없음은 대기실로 수렴한다. 단 구독 응답 전 `start()`가 먼저 실행되어 카메라 권한 요청이 잠깐 시작될 수 있다 | U-04, U-05 |
| **R-16** | **호스트 전이 회귀** — watch 소스가 단일 ref에서 배열로 바뀌었다. 호스트가 운영 화면으로 못 가면 진행 자체가 막힌다 | T-03, V-01 |

### 19.3 TC — T. 게스트 전이 시점

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| T-01 [unit] | P1 | 배정 1차 확정, 게스트 대기실 | 호스트 `게임 시작` | **게스트는 이동하지 않는다** — 경로 `/waiting-room/{코드}` 유지, 라운드 배정 카드 그대로, `getUserMedia` 호출 **0회** |
| T-02 [unit] | P1 | T-01 상태(호스트는 운영 화면) | 호스트 `라운드 시작` | 게스트가 **1초 내** `/camera/{코드}`로 이동 + 뷰파인더·`촬영` 노출 |
| T-03 | P1 | 배정 1차 확정 | 호스트 `게임 시작` | 호스트는 **즉시** `/round-ops/{코드}`(회귀 — 라운드 시작을 기다리지 않는다) |
| T-04 | P2 | 라운드가 이미 진행 중인 방 | 게스트가 링크로 신규 입장 | 대기실을 거쳐 즉시 `/camera/{코드}` |
| T-05 | P2 | playing이지만 라운드 시작 전인 방 | 게스트 신규 입장 | 대기실에 머문다(배정됐으면 배정 카드, 아니면 기본 대기실 뷰) |
| T-06 | P2 | T-01 상태 | 게스트 새로고침 | 배정 카드 그대로, 카메라로 튀지 않음 |
| T-07 | P2 | 라운드 진행 중, 게스트 콕핏 | 호스트 `일시정지` | 게스트는 **콕핏 유지**(올스탑은 화면 전환이 아니다) |
| T-08 | P3 | 라운드 종료(0 도달) | 게스트 화면 | 콕핏 유지 — round 필드가 남아 있다(§15 한계 10과 같은 이유) |
| T-09 | P1 | 게스트 3명 동시 접속 | 호스트 `라운드 시작` 1회 | 3명 **동시** 전이, 각자 `/camera/{같은 코드}` |
| T-10 [unit] | P2 | 게스트 계정 | `/round-ops/{코드}` 직접 진입 | 라운드 시작 전이면 `/waiting-room/{코드}`, 시작 후면 `/camera/{코드}` — 대기실 규칙과 동일 |
| T-11 | P2 | 게스트 콕핏 대기 중 | 오프라인 30초 → 복귀 중에 호스트가 라운드 시작 | 복귀 후 전이를 놓치지 않고 콕핏으로 이동(R-13) |

### 19.4 TC — U. 콕핏 경로

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| U-01 [unit] | P1 | T-02 직후 | 주소창 확인 | `/camera/{대문자 방 코드}` — 대기실에서 쓰던 코드와 동일 |
| U-02 | P1 | 게스트 콕핏 | 새로고침 | 같은 URL 유지 + 카메라 재개(빈 화면·입장 화면으로 튕기지 않음) |
| U-03 | P2 | 임의 | `/camera`(구 경로) 직접 진입 | 빈 화면이 아니라 `/`(입장 화면)로 redirect |
| U-04 | P2 | 로그인 상태 | `/camera/ZZZ9`(없는 방) 직접 진입 | 토스트 `방을 찾을 수 없어요.` 후 `/`(입장 화면)로 replace, 콕핏·비디오 잔존 없음 |
| U-05 | P3 | 로그인 상태 | `/camera/4nyy`(소문자) | 라우트 매칭·크래시 없음. **현 스펙**: 콕핏은 코드를 표시·정규화하지 않는다 |
| U-06 | P3 | 게스트 콕핏 | 뒤로가기 | `replace` 스펙이라 대기실로 돌아가지 않는다 |

### 19.5 TC — V. 회귀

| ID | 우선 | 절차 | 기대 결과 |
|---|---|---|---|
| V-01 | P1 | 호스트 운영 화면에서 라운드 시작 → 공지 → 일시정지 → 재개 | 2차에서 검증한 A·B·C군 동작 무변화 |
| V-02 | P1 | `type-check` / `lint` / `test:unit` / `build` | 4종 통과 |
| V-03 | P2 | 호스트 세션 상실 | 2차 수정(O-01)대로 여전히 `/login?redirect=`로 이동 — 카메라로 가지 않음 |

### 19.5.1 3차 검증 결과 (live-qa, 2026-07-25) — **24/24 통과**

방 `A9KJ`(호스트 1 + 게스트 3, 실제 UI 플로우) + 방 `T3QB`(오프라인 시나리오용, REST 구성)로 검증했다.

| ID | 판정 | 근거 |
|---|---|---|
| T-01 | ✅ | `게임 시작` 후 게스트 3명 모두 `/waiting-room/A9KJ` 유지, `getUserMedia` **[0,0,0]** |
| T-01b | ✅ | 게스트 화면에 `라운드 1 배정 · 파랑 완장 A · 그룹 BLUE · 2인 1조` 카드 그대로 |
| T-02 | ✅ | `라운드 시작` → 3명 모두 `/camera/A9KJ`, 뷰파인더·`촬영` 노출 |
| T-03 | ✅ | 호스트는 `게임 시작` 즉시 `/round-ops/A9KJ`, `getUserMedia` 0회 (회귀 없음) |
| T-04 | ✅ | 라운드 진행 중 재입장한 게스트 → 즉시 `/camera/A9KJ` |
| T-05 | ✅ | playing·라운드 시작 전 방에 재입장 → `/waiting-room/A9KJ` |
| T-06 | ✅ | 대기 중 새로고침 → 대기실 유지, `getUserMedia` 0회 |
| T-07 | ✅ | 일시정지에도 3명 모두 콕핏 유지 |
| T-08 | ✅ | 라운드 종료(0 도달) 후에도 콕핏 유지 — §19.6 한계 3 확인 |
| T-09 | ✅ | **3명 동시 전이 860ms** (기준 3초) |
| T-10a/b | ✅ | 게스트의 운영 URL 진입 → 시작 전 대기실 / 시작 후 콕핏 |
| T-11 | ✅ | 오프라인 중 호스트가 라운드 시작 → 복귀 후 `/waiting-room/T3QB` → `/camera/T3QB` 전이 (R-13 해소) |
| U-01 | ✅ | `/camera/A9KJ` — 대기실 코드와 동일(대문자) |
| U-02 | ✅ | 콕핏 새로고침 → 같은 URL + 카메라 재개 |
| U-03 | ✅ | 구 경로 `/camera` → `/`로 redirect(빈 화면 아님) |
| U-04 | ✅ | 6차 재검증: `/camera/ZZZ9` → `/`, `<video>` 없음. 4차의 방 구독 추가를 반영해 기대값 정정 |
| U-05 | ✅ | 소문자 `/camera/a9kj` 라우트 매칭 |
| U-06 | ✅ | 뒤로가기 시 대기실로 복귀하지 않음(`replace` 스펙) |
| V-01 | ✅ | 운영 화면 타이머(`19:29`)·공지(`방금 · 3차 회귀 확인`) 정상, 서버 문서 `round=running` |
| V-02 | ✅ | type-check / lint / **test:unit 461 passed** / build |
| V-03 | ✅ | 호스트 세션 상실 → `/login?redirect=/round-ops/A9KJ`, `<video>` 없음 (2차 O-01 수정 유지) |

**P0 게이트**: 위 체인(방 생성 → 3인 입장·레디 → 배정 확정 → 게임 시작 → **게스트 대기 유지** → 라운드 시작 → **3인 동시 콕핏 전이** → 공지)을 끊김 없이 완주, 오류 0.

### 19.6 알려진 한계 (3차 기준 추가)

1. **콕핏은 방 코드를 받기만 한다** — 라운드 타이머·공지 수신 UI는 여전히 후속. 경로만 먼저 준비됐다.
2. **콕핏은 구독 응답 전에 카메라를 먼저 요청한다** — 없는 방·시작 전 방은 구독 후 안전 경로로 이동하지만, 느린 네트워크에서는 이동 전 권한 요청이 잠깐 시작될 수 있다.
3. **일시정지·종료에도 게스트는 콕핏에 머문다** — round 필드 존재가 기준이라 그렇다. 올스탑 표시는 콕핏 HUD가 생길 때 함께 다룬다.

---

## 20. 4차 변경 — 게임 종료 (2026-07-25)

> **왜**: 잘못 시작한 게임을 앱에서 되돌릴 방법이 없었다(status 단방향 — §16 P0-02에서 "콘솔 접근 담당자를 현장에 1명 확보"로 우회하던 항목). 진행자가 스스로 게임을 닫고 대기실로 돌아가 다음 라운드를 준비할 수 있어야 한다.

### 20.1 변경 내용

| # | 파일 | 변경 |
|---|---|---|
| 1 | `firestore.rules` | **4번째 갈래** — 호스트가 `status`(playing→waiting)와 `round`(삭제)를 **한 번에** 되돌리는 것만 허용. 나눠 쓰면 "대기 중인데 라운드가 살아 있는" 중간 상태가 참가자에게 보인다 |
| 2 | `waiting-room/api/rooms.ts` | `endGame(code)` — `{ status:'waiting', round: deleteField() }`. 배정 이력은 건드리지 않는다 |
| 3 | `round-ops/stores/useRoundOpsStore.ts` | `finishGame()` — 호스트·playing 가드 후 실행, 성공 여부 반환 |
| 4 | `round-ops/RoundOpsPage.vue` | 운영 탭 맨 아래 `게임 종료`(ghost/danger) + **확인 다이얼로그**. `playing → waiting` 스냅샷에서 호스트를 대기실로 |
| 5 | `camera/CameraPage.vue` | **방 문서 구독 신설** — 콕핏이 스스로 나갈 시점을 알기 위한 최소 연결. status가 waiting이거나 round가 사라지면 대기실로, 방이 없으면 입장 화면으로 |
| 6 | `router/index.ts` | 콕핏 라우트에 `requiresAuth: true`(방을 읽으므로 인증이 전제) |

### 20.2 검증 결과 — **rules 11/11 · E2E 7/7**

**보안 규칙(REST + 실제 사용자 토큰)**

| 케이스 | 결과 |
|---|---|
| 호스트가 status+round 함께 되돌리기 | ✅ **200** — `status=waiting`, `round` 삭제, `assignmentRound=1` 보존 |
| 라운드 시작 전 방 종료(`affectedKeys=['status']`) | ✅ **200** |
| 종료 후 재시작(waiting→playing) + 라운드 재개시 | ✅ **200 / 200** — 루프가 닫히고 다시 열린다 |
| 게스트가 종료 시도 | ✅ **403** |
| `round`를 남긴 채 status만 되돌리기 | ✅ **403** |
| 종료하면서 `assignmentRound`까지 조작 | ✅ **403** |
| 종료하면서 `round`를 유효값으로 남기기 | ✅ **403** |

**브라우저 E2E**(호스트 + 게스트 콕핏, 방 `A9KJ`)

| ID | 판정 | 근거 |
|---|---|---|
| END-01 | ✅ | `게임 종료` → 확인 다이얼로그(`게임을 종료할까요?`) — 즉시 종료되지 않는다 |
| END-02 | ✅ | 확인 후 호스트가 `/waiting-room/A9KJ`로 복귀 |
| END-03 | ✅ | **게스트가 콕핏에서 대기실로 자동 복귀** — 구독이 없으면 카메라에 갇히던 지점 |
| END-04 | ✅ | 6차 재검증: 게스트가 `라운드 1 배정` 카드로 전환되고 콕핏 비디오가 사라짐. 카메라 트랙 해제는 `CameraPage.spec`의 언마운트 테스트도 커버 |
| END-05 | ✅ | 서버 문서: `waiting` + `round` 없음 + 차수 1 보존 |
| END-06 | ✅ | 대기실에 `2차 팀 배정` 노출 — 다음 라운드를 이어서 준비 가능 |

단위 테스트 **461 → 479 passed**(+18: endGame payload·rules 동기화 가드·store 가드 4·다이얼로그 플로우 6·콕핏 구독 5).

### 20.3 알려진 한계 (4차 기준)

1. **종료는 배정을 지우지 않는다** — 의도된 설계다. 대기실로 돌아가면 `N차 팀 배정`으로 이어지고, 그때 레디가 리셋된다. 완전 초기화가 필요하면 방을 새로 만든다.
2. **게스트에게는 종료를 알리는 안내가 없다** — 화면만 바뀐다. 토스트/문구는 콕핏 HUD 작업과 함께 다루는 게 자연스럽다.
3. **콕핏 구독에 에러 콜백이 없다**(R-2와 동일) — 권한 상실 시 조용히 갱신이 멈춘다.

---

## 21. 당일 운영 체크리스트 (round-ops)

0. **🔴 배포 순서**: `firestore.rules`를 앱과 **함께** 배포한다. 이번 변경은 `round` 갈래와 `notices` 갈래가 신규라, 미배포 시 **라운드 시작·정지·시간 반영·공지가 전부 403**이다. 1차 QA에서 같은 유형의 사고가 실제로 있었다(배정 확정 403).
1. **시작 전 스모크(P0-04)**: 테스트 방에서 `라운드 시작` 1회 + 공지 1건 성공 확인. 하나라도 실패하면 **이벤트 시작 보류**.
2. **호스트 폰 시계 확인(M-04)**: 자동 시각 동기화 ON. 남은 시간은 호스트 기기 시계로 계산된다.
3. **새로고침·기기 교체 대비(P0-03)**: 타이머 정본이 서버라 호스트 폰이 죽어도 다른 기기로 재로그인하면 이어받는다. 백업 기기 1대와 호스트 계정 크리덴셜을 현장에 확보.
4. **오조작 복구**: 운영 화면 `게임 종료` → 확인으로 전원을 대기실에 되돌릴 수 있다. 라운드 타이머만 다시 시작하려면 `라운드 시작`으로 20분 재설정한다.
5. **참가자 안내**: 게스트 화면에는 아직 타이머·공지가 뜨지 않는다. 남은 시간·공지는 **구두 전달**이 필요하다(한계 2).
6. **HTTPS 전제**: 게스트 카메라 권한은 보안 컨텍스트에서만 동작.

---

## 22. 5차 최종 회귀 — PR 머지 게이트 (2026-07-25)

대상은 4차 검증 이후 추가된 `e653b1d..31d6dfd`의 pending 액션 표시, 종료 모달 문구 위계,
타이머 카드 정보 구조 변경이다. 기존 실데이터 방 `A9KJ`와 호스트 세션을 재사용했다.

| ID | 우선 | 판정 | 실측 근거 |
|---|---|---|---|
| W-01 | P1 | ✅ | 일시정지 요청 후 2.5초 동안 50ms 간격 샘플링. `게임 종료`·`+1분`·`-1분`은 계속 활성, 관계없는 컨트롤의 disabled 전환·깜빡임 0회 |
| W-02 | P2 | ✅ | 타이머 카드에 `라운드 1`(15px, 좌상단)과 `19:56`(46px)만 노출. ROOM 행의 `LIVE`와 중복되던 상태·팀 수 요약 제거 확인 |
| W-03 | P2 | ✅ | 종료 모달 설명 `text-wrap: balance`, 2줄 실측. 결과 설명과 `팀 배정은 그대로…` 캡션이 분리되어 액션과 시각 위계 확보 |
| W-04 | P1 | ✅ | type-check, oxlint, eslint, production build 통과. Vitest **48 files / 483 tests 전부 통과** |

### 최종 판정

- GitHub: PR #16은 `MERGEABLE`, merge state `CLEAN`, 충돌 없음.
- 기능 QA: 전체 141 TC 중 완료 131, 부분 2, 환경상 불가 8, 실패 0.
- P0: 풀체인·오조작 복구·기기 교체·배포 rules 스모크 **4/4 통과**.
- 미검증 8건은 iOS/Android 실기기, 인앱 브라우저, 60분 장기 체류, admin으로만 만들 수 있는 손상 상태다. 코드 결함으로 확인된 실패는 없다.
- 결론: **기능·코드 기준 머지 가능**. 단, PR이 Draft이고 GitHub CI 체크가 없으므로 Draft 해제와 배포 시 `firestore.rules` 동시 반영은 별도 운영 게이트다.

---

## 23. 6차 현장 강인성 보강 — 새 회차 전수 재검증 (2026-07-25)

기존 결과 승계만으로 머지 판정하지 않기 위해 실제 Chrome 4개 프로파일과 Firebase 실데이터로 새 방
`E6QY`, `REBQ`, `2YX9`, rules 방 `Q7WR/Q7WT/Q8WR/Q8WT`를 만들고 핵심 플로우를 다시 실행했다.

### 23.1 추가 TC — FIELD. 간헐 연결·요청 경합

| ID | 우선 | 판정 | 실측 근거 |
|---|---|---|---|
| FIELD-01 | P1 | ✅ | 호스트 오프라인에서 `+1분 → 반영 → +1분`. 요청 대기 중 `대기 변경값: +2분` 유지 |
| FIELD-02 | P2 | ✅ | 위 요청 중 `게임 종료`·`-1분`·`+1분`은 활성 유지. 관계없는 컨트롤 잠김·깜빡임 없음 |
| FIELD-03 | P1 | ✅ | 온라인 복귀 후 첫 반영분만 제거되고 추가 입력 `+1분`이 다음 반영 대상으로 보존 |
| FIELD-04 | P2 | ✅ | 보존된 두 번째 `+1분`을 다시 반영할 수 있고 서버·화면이 LIVE로 수렴 |
| FIELD-05 | P2 | ✅ | 게스트 오프라인 중 호스트가 게임 종료하면 마지막 콕핏 화면 유지(캐시된 상태) |
| FIELD-06 | P1 | ✅ | 게스트 온라인 복귀 후 놓친 종료 스냅샷을 받아 `/waiting-room/2YX9`로 자동 복구 |
| FIELD-07 | P2 | ✅ | FIELD-06 후 서버 정본 `status=waiting`, `round` 없음 확인 |
| FIELD-08 | P1 | ✅ | 호스트 세션 상실 시 `/login?redirect=/round-ops/Q8WR`, 비디오·운영 컨트롤 잔존 없음 |

### 23.2 새 회차 결과 매트릭스

| 영역 | 새 회차 결과 | 핵심 근거 |
|---|---:|---|
| 게스트 3인 전이·콕핏·딥링크 | 19/19 기대 동작 | 3명 라운드 시작 전 대기, 시작 후 **860ms** 동시 콕핏. 없는 방은 `/`로 안전 복귀하도록 TC 정정 |
| P0 실제 당일 타임라인 | **13/13** | 방 생성 → 링크 입장·레디 → 배정 → 게임 시작 → 라운드 시작 → 공지 → 조정 → 정지·재개 → 기기 교체 → 오조작 복구 → 새로고침, **70.6초** |
| 타이머·시간 조정 | **21/21** | 서버 round 직접 대조, 새로고침, 연타 1-write, running/paused 조정 |
| 공지·렌더·모바일·접근성 | **15/15** | 100자 초과 차단, 토스트 hit-through, 탭, 320/430/1440 반응형 |
| 호스트 가드·멀티 클라이언트 | **14/14** | 두 호스트 창 pause/resume/시간/공지 실시간 수렴, round 단독 삭제 403 |
| 현장 오프라인 | **7/7** | 호스트 오프라인 입력 보존 + 게스트 종료 이벤트 복구 |
| 라운드·공지 rules | **25/25** | 실제 사용자 토큰으로 허용/거부·필드 변조·상한·append-only 전수 |
| 게임 종료 rules | **11/11** | 정상 종료·재시작 허용, 게스트·중간 상태·동반 변조 거부 |
| 게임 종료 브라우저 E2E | **7/7** | 호스트·게스트 대기실 복귀, round 삭제·차수 보존, 다음 배정 CTA |
| 세션 상실 | **1/1** | 로그인 redirect, 카메라 회귀 없음 |
| 자동 게이트 | **483/483** | type-check·oxlint·eslint·production build 통과 |

### 23.3 오프라인 현장 사용 판정

- **간헐 연결 환경은 사용 가능**: 이미 로그인·방 진입한 호스트/게스트는 단절 중 마지막 화면을 유지하고,
  호스트 쓰기는 로컬 큐에 남으며 복귀 후 서버 정본으로 수렴한다. 이번 회차에서 추가 시간 입력 유실과
  게스트의 놓친 게임 종료 복구까지 확인했다.
- **완전 무통신 행사는 불가**: Firebase Auth·Firestore가 실백엔드이므로 최초 로그인, 방 생성, 참가,
  팀 배정, 타 클라이언트 동기화에는 인터넷이 필요하다. 장시간 단절 중에는 다른 기기에 변경이 전달되지 않는다.
- **현장 필수 조건**: 호스트 자동 시각 동기화 ON, 행사 시작 전 rules 스모크, 백업 호스트 기기,
  안정적인 주 회선과 테더링 예비 회선, HTTPS 카메라 권한을 확보한다.
- **미검증 물리 환경**: iOS Safari·Android Chrome 실기기, 카카오톡 인앱 브라우저, 실제 비행기 모드,
  화면 잠금 5분+, 60분 장기 체류는 자동화 밖이라 머지 후 현장 리허설 게이트로 남긴다.

---

## 24. 7차 변경 — 종료 실패 안내 (2026-08-31)

- **브랜치**: `fix/round-ops-end-game-silent-failure` (PR #25, main 머지 + 미커밋 포함)
- **한 줄**: 종료가 실패해도 확인 창이 닫혀 "눌렀는데 아무 일도 없는" 화면이 되던 것을, 창을 열어 둔 채 실패를 알리도록 바꿨다.
- **후속**: 이 절의 FAIL 군을 `live-qa`로 실검증한다.

### 24.1 작업 범위 (diff 전수)

| # | 파일 | 변경 내용 |
|---|---|---|
| 1 | `round-ops/RoundOpsPage.vue` | `endGame()` — 성공했을 때만 확인 창을 닫는다. 실패면 창 유지 + `게임을 종료하지 못했어요. 다시 시도해 주세요.`(danger) |
| 2 | `round-ops/RoundOpsPage.vue` | `finishRound()` — 같은 처리 + `라운드를 종료하지 못했어요. 다시 시도해 주세요.`(danger). **판정 대기 0건이면 확인 창 없이 바로 실행되므로 토스트가 유일한 실패 신호다** |
| 3 | `round-ops/RoundOpsPage.vue` | 문구 상수 2개(`END_GAME_ERROR_MESSAGE`·`END_ROUND_ERROR_MESSAGE`) 추가 |
| 4 | `round-ops/stores/useRoundOpsStore.ts` | `finishGame()`이 `runAction('end', …, reportError=false)` — store가 `actionError`를 세우지 않고 성공 여부만 돌려준다(판정 플로우와 같은 방식) |
| 5 | `round-ops/__tests__/RoundOpsPage.spec.ts` | 종료 실패 5건 추가 (게임 종료 2 · 라운드 종료 3) |
| 6 | `round-ops/__tests__/useRoundOpsStore.spec.ts` | 실패 시 `actionError` 미설정 검증으로 교체 + 다른 쓰기 진행 중 차단 1건 추가 |

### 24.2 코드에서 도출한 크리티컬 리스크

1. **실패 두 갈래가 한 문구로 수렴한다** — 가드 차단(요청이 안 나감)과 서버 쓰기 실패가 같은 문구다. 진행자가 할 일은 둘 다 재시도라 동작은 같지만, 반복 실패 시 원인을 화면만으로 구분할 수 없다 (FAIL-01·FAIL-05).
2. **대기 0건 경로는 토스트가 유일한 신호다** — 확인 창이 아예 없으므로 토스트가 가려지거나 사라지면 무신호로 되돌아간다 (FAIL-03·FAIL-10).
3. **`actionError` 토스트 경로가 종료에서만 끊겼다** — 시작·일시정지·재개·시간 반영의 기존 안내가 그대로인지 회귀 확인이 필요하다 (FAIL-08).
4. **실패로 창이 더 오래 열려 있다** — 확인 창의 킬샷 건수는 열림 시점 스냅샷 고정이라(의도), 재시도까지 시간이 벌어지면 낡은 건수를 보고 확인하게 된다 (FAIL-07).
5. **`await` 뒤 토스트라 화면을 떠난 뒤 뜰 수 있다** — 다른 기기가 먼저 종료하면 대기실로 이동하면서 실패 안내가 따라온다 (FAIL-09).

### 24.3 TC — FAIL. 종료 실패 안내

사전조건 공통: 호스트 계정으로 `/round-ops/{code}` 진입, 방 `playing`. "응답 없는 쓰기"는 개발자 도구 오프라인으로 만든다(Firestore는 서버 ack까지 요청이 대기하므로 `pendingAction`이 유지된다 — §15 한계 참조).

| ID | 우선 | 사전조건 | 절차 | 기대 결과 |
|---|---|---|---|---|
| FAIL-01 [unit] | P1 | 라운드 LIVE, 오프라인 | `일시정지`(응답 대기 상태 유지) → `게임 종료` → 확인 창에서 `게임 종료` | 확인 창 `게임을 종료할까요?`가 **닫히지 않고**, `게임을 종료하지 못했어요. 다시 시도해 주세요.` 토스트(danger). 서버 종료 쓰기 0건 |
| FAIL-02 | P1 | FAIL-01 직후 | 온라인 복귀 → 대기 쓰기 ack 확인 → 열려 있는 창에서 `게임 종료` 재클릭 | 종료 성공 → `게임을 종료했어요.` + 호스트가 `/waiting-room/{code}`로 복귀 |
| FAIL-03 [unit] | P1 | 타이머 `00:00`, 판정 대기 **0건**, 오프라인 | `-1분` → `반영`(응답 대기) → `라운드 종료` | 확인 창이 뜨지 않고 `라운드를 종료하지 못했어요. 다시 시도해 주세요.` 토스트. 화면 상태(00:00·라운드 종료 버튼) 불변 |
| FAIL-04 [unit] | P1 | 타이머 `00:00`, 판정 대기 1건 이상, 오프라인 | `-1분` → `반영`(응답 대기) → `라운드 종료` → `그대로 라운드 종료` | `판정하지 않은 킬샷이 있어요` 창이 **유지**되고 실패 토스트. 판정 탭 배지 건수 그대로 |
| FAIL-05 | P1 | 온라인, 다른 기기(같은 호스트 계정)에서 먼저 게임 종료 | 이 기기에서 `게임 종료` → 확인 | 쓰기가 rules에 거부되어 실패 토스트 → 곧 도착하는 스냅샷으로 `/waiting-room/{code}` 복귀 |
| FAIL-06 [unit] | P2 | — | 단위: `endGame` 쓰기가 reject될 때 store 반환값·`actionError` 확인 | `false` 반환, `actionError`는 `null` 유지(토스트 중복 없음) |
| FAIL-07 | P2 | FAIL-04로 창이 열린 채 유지 | 다른 기기에서 남은 킬샷을 전부 판정 | 창의 `킬샷 N건이 남았어요`는 **열림 시점 값 고정**(의도된 스펙, §15). 재시도 시 종료는 정상 성공 |
| FAIL-08 | P1 | 오프라인 | `라운드 시작`·`일시정지`·`반영`을 각각 실패시킨다 | 기존 문구 `요청을 처리하지 못했어요. 다시 시도해 주세요.`가 그대로 노출(종료만 문구가 갈라졌는지 회귀 확인) |
| FAIL-09 | P3 | FAIL-05 상태 | 실패 토스트 직후 대기실 이동 | 대기실 화면에 종료 실패 토스트가 남을 수 있다 — **알려진 한계**(§24.5), 오탐 금지 |
| FAIL-10 | P3 | 폭 320px | FAIL-03 재현 | 토스트 문구가 잘리지 않고, 하단 탭·주 액션 버튼을 가리지 않는다 |

### 24.4 커버리지

| 축 | 커버 | 비고 |
|---|---|---|
| 정상 경로 | FAIL-02 | 실패 후 재시도 성공까지 한 쌍으로 본다 |
| 에러 경로 | FAIL-01·03·04·05 | 가드 차단 / 서버 거부 두 갈래 모두 |
| 동시성 | FAIL-05·07 | 다른 기기 선종료·선판정 |
| 회귀 | FAIL-08 | `reportError=false`가 다른 액션 안내를 건드리지 않았는지 |
| 접근성·모바일 | FAIL-10 | 토스트 가시성 |
| 단위 커버 | FAIL-01·03·04·06 | `RoundOpsPage.spec` 5건 + `useRoundOpsStore.spec` 2건 |

**live-qa 중점**(단위가 못 잡는 것): 실제 오프라인 큐에서 `pendingAction`이 유지되는지, 다른 기기 선종료 시의 문구·복귀 순서(FAIL-05), 토스트 가시성(FAIL-10).

### 24.5 알려진 한계 (오탐 방지)

1. **실패 문구가 원인을 구분하지 않는다** — 가드 차단과 서버 거부가 같은 문구다. 진행자의 다음 행동이 같아서 의도적으로 합쳤다.
2. ~~**화면을 떠난 뒤 실패 토스트가 뜰 수 있다** — 수정 전에도 같은 성질이었다(`actionError` 토스트).~~
   **정정(24.6 실측)**: 수정 전에는 뜨지 않았다. store의 세대 가드가 화면을 떠난 뒤의 늦은 실패 보고를 막고 있었는데, 화면이 직접 띄우는 새 토스트에는 그 가드가 없다 → FAIL-09 회귀.
3. **확인 창의 킬샷 건수는 열림 시점 고정** — 실패로 창이 오래 열려 있어도 갱신되지 않는다. 열려 있는 동안 근거가 바뀌면 안 된다는 기존 결정이다.

### 24.6 검증 결과 (live-qa, 2026-08-31)

- **환경**: baseline = `main`(5feac4e) 워크트리 dev 서버 `:5180` · local = `fix/round-ops-end-game-silent-failure` + main 머지 워크트리 `:5181`. 둘 다 실 Firebase(`photo-royale-scc`).
- **데이터**: QA 전용 계정 3개(호스트 1·게스트 2)와 방 `G8T6`을 만들어 검증하고 전부 원복했다(계정 self-delete 200 ×3, `rooms/G8T6`·`nicknames/…` 재조회 404).
- **"응답 없는 쓰기"** 는 개발자 도구 오프라인으로 만들었다. Firestore는 서버 ack까지 요청이 대기하므로 `pendingAction`이 유지된다.

| ID | 우선 | 기법 | baseline (수정 전) | local (수정 후) | 판정 |
|---|---|---|---|---|---|
| FAIL-01 | P1 | act | 확인 창이 닫히고 **토스트 0건** | 창 유지 + `게임을 종료하지 못했어요. 다시 시도해 주세요.` | ✅ |
| FAIL-02 | P1 | act | — | 온라인 복귀 후 열려 있던 창에서 재클릭 → `게임을 종료했어요.` + `/waiting-room/G8T6` | ✅ |
| FAIL-03 | P1 | act | 확인 창도 토스트도 없음 — **완전 무반응** | `라운드를 종료하지 못했어요. 다시 시도해 주세요.` | ✅ |
| FAIL-04 | P1 | act | `판정하지 않은 킬샷이 있어요` 창이 닫히고 토스트 0건 | 창 유지 + 실패 토스트, 판정 배지 유지 | ✅ |
| FAIL-05 | P1 | act | 토스트 0건, 대기실 복귀 | 실패 토스트 + 대기실 복귀 | ✅(동작) / FAIL-09 참조 |
| FAIL-06 | P2 | [unit] | — | `finishGame` 실패 시 `actionError` `null` 유지 — 단위 5건 통과 | ✅ |
| FAIL-07 | P2 | act | — | 창 유지 + `킬샷 1건이 남았어요` 고정(판정 배지는 0으로 갱신) → 재시도 성공 | ✅ |
| FAIL-08 | P1 | 갈음 + [unit] | — | 종료 외 액션의 서버 거부를 결정적으로 만들 수단이 없다(아래 사유) | ⚠️ |
| FAIL-09 | P3 | 실데이터 | **토스트 없음** | 대기실 화면에 `게임을 종료하지 못했어요` 노출 | ❌ **회귀** |
| FAIL-10 | P3 | act | — | 320px에서 토스트 288×78 · 하단 탭/주 액션과 겹침 0 · 가로 스크롤 0 | ✅ |

**FAIL-08이 ⚠️인 이유**: 종료 외 라운드 액션이 **서버에 거부되는** 상황은 "다른 기기가 먼저 게임을 종료" 조합뿐인데, 그 조건에서는 스냅샷이 도착해 화면이 대기실로 떠나고 store의 세대 가드가 늦은 실패 보고를 막는다(2회 시도 모두 토스트 0건). 코드상 이번 변경은 `finishGame` 한 줄에만 `reportError=false`를 넣었고, 나머지 액션은 기본값(true)이라 `요청을 처리하지 못했어요.` 경로가 그대로다. `useRoundOpsStore.spec.ts`의 실패 안내 테스트가 이를 커버한다.

**FAIL-09 (회귀 1건)**

- 재현: 같은 호스트 계정으로 두 창을 연다 → A에서 `게임 종료` 확인 창을 열고 통신을 끊는다 → B에서 게임을 종료한다 → A를 복구하고 확인 창의 `게임 종료`를 누른다.
- 결과: A의 쓰기는 rules에 거부되고, **대기실로 이동한 화면에 `게임을 종료하지 못했어요. 다시 시도해 주세요.`가 뜬다.** 실제로는 게임이 종료됐으므로 문구가 사실과 반대다.
- baseline에는 이 토스트가 없다. store의 `runAction`이 세대 가드로 늦은 실패 보고를 버렸기 때문이다. 화면이 직접 띄우는 새 토스트에는 같은 가드가 없다.
- 제안: `endGame`/`finishRound`의 실패 분기에도 언마운트·이탈 가드를 둔다(성공 토스트에도 같은 성질이 있으므로 함께 다루는 편이 낫다).
